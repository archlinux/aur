--- a/data/meson.build	2026-09-23 01:15:41.039461070 +0200
+++ b/data/meson.build	2026-09-23 01:48:53.474171677 +0200
@@ -82,11 +82,14 @@
 
 install_data(settings_schemas, install_dir: schemas_dir)
 
-meson.add_install_script (
-    glib_compile_schemas,
-    schemas_dir,
-    install_tag: 'schemas'
-)
+# Do not run glib-compile-schemas as meson install script. This prevents building in a clean chroot and isn't needed as the task is
+# covered by a pacman hook.
+# Details in upstream issue #215 (https://github.com/tuner-labs/tuner/issues/215).
+#meson.add_install_script (
+#    glib_compile_schemas,
+#    schemas_dir,
+#    install_tag: 'schemas'
+#)
 
 message('Installed schema\n')
 
