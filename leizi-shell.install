post_install() {
  grep -qx /bin/leizi /etc/shells || echo /bin/leizi >> /etc/shells
  grep -qx /usr/bin/leizi /etc/shells || echo /usr/bin/leizi >> /etc/shells
}

post_upgrade() {
  post_install
}

post_remove() {
  sed -i -r '/^(\/usr)?\/bin\/leizi$/d' etc/shells
}

# vim:set ts=2 sw=2 et:
