#!/bin/env bash

post_install() {
  cat >&2 << 'EOF'
  ## To install nautilus-scripts for the logged-in user, Run:
  ln -sf /opt/nautilus-scripts/ $HOME/.local/share/nautilus/scripts
EOF
}

post_upgrade() {
  post_install
}
