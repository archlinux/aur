post_install() {
    #copy the config
    mkdir -p "${HOME}/.config/papp_cloud"
    cp "/usr/share/paracloud/sccs_sample.yml" "${HOME}/.config/papp_cloud/sccs.yml"
}

post_remove() {
    #delete the config
    rm -rf "${HOME}/.config/papp_cloud/"
}
