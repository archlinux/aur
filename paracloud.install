post_install() {
    #copy the config
    mkdir "${HOME}/.config/papp_cloud"
    cp "/usr/share/paracloud/sccs.yml" "${HOME}/.config/papp_cloud/"
}

post_remove() {
    #delete the config
    rm -rf "${HOME}/.config/papp_cloud/"
}
