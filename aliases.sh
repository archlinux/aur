# If not running interactively, don't do anything
[[ $- != *i* ]] && return
 
alias vi=vim
alias ls='ls --color=auto'
alias l='ls -h -l'
alias ll='l -l'
alias la='l -la'
alias df='df -h'
alias du='du -h'
alias ..='cd ..'
alias ...='cd .. && cd ..'

