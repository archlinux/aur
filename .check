#!/bin/bash
# get version info for the lastest build of micro editor from github release nightly
#cd /home/tommy/micro-nightly-bin/
#NOW=$(date +"%H-%M-%d-%m-%y")
NOW=$(date -R)
cat .version.info > .old-version.info
curl -i https://api.github.com/repos/zyedidia/micro/releases/tags/nightly | grep -P -o "\B\"micro.*linux64.tar.gz" > .name.info
grep -P -o "\d.*-" .name.info | head --bytes -2 > .version.info
# check version numbers
if [ $(<.version.info) == $(<.old-version.info) ]
then
    echo "$NOW" ' -> Micro Editor version numbers are the same, so no changes needed.' >> micro-editor-check.log
else
    echo -e "$NOW" ' -> \e[32mMicro Editor version differ - so PKGBUILD and .SRCINFO needs updating to latest version info.\e[0m' >> micro-editor-check.log
    source ./.update
fi
