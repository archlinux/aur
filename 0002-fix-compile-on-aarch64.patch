--- a/src/nntp/mime/Mime.cpp
+++ b/src/nntp/mime/Mime.cpp
@@ -532,7 +532,7 @@ list<CMimeField>::iterator CMimeHeader::FindField(const char* pszFieldName)
 #ifdef _WIN32
 	#include <io.h>
 #else
-	#if !defined(__APPLE__) && !defined(__DARWIN__)
+	#if !defined(__APPLE__) && !defined(__DARWIN__) && !defined(__aarch64__)
 		#if !defined(__FreeBSD__) && !defined(solaris) && !defined(__sun) && !defined(__OpenBSD__)
 			#include <sys/io.h>
 		#else
