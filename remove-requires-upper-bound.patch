--- compose-1.1.0/setup.py.orig	2015-02-25 19:42:28.000000000 +0100
+++ compose-1.1.0/setup.py	2015-03-06 14:17:23.431276902 +0100
@@ -25,14 +25,14 @@
 
 
 install_requires = [
-    'docopt >= 0.6.1, < 0.7',
-    'PyYAML >= 3.10, < 4',
-    'requests >= 2.2.1, < 2.6',
-    'texttable >= 0.8.1, < 0.9',
-    'websocket-client >= 0.11.0, < 1.0',
-    'docker-py >= 1.0.0, < 1.2',
-    'dockerpty >= 0.3.2, < 0.4',
-    'six >= 1.3.0, < 2',
+    'docopt >= 0.6.1',
+    'PyYAML >= 3.10',
+    'requests >= 2.2.1',
+    'texttable >= 0.8.1',
+    'websocket-client >= 0.11.0',
+    'docker-py >= 1.0.0',
+    'dockerpty >= 0.3.2',
+    'six >= 1.3.0',
 ]
 
 tests_require = [
