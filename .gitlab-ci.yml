include:
  - project: languitar-aur-packages/gitlab-templates
    ref: main
    file: PKGBUILD.gitlab-ci.yml

variables:
  PKGNAME: autosuspend
  OPT_DEPS_TEST: python-portalocker

test:
  script:
    - autosuspend -h
