include:
  - project: languitar-aur-packages/gitlab-templates
    ref: main
    file: PKGBUILD.gitlab-ci.yml

variables:
  PKGNAME: puppet-editor-services

test:
  script:
    - puppet-languageserver -h
