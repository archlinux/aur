post_install() {
	cat <<'EOF'
==> Andromeda ships a built-in `andromeda upgrade` command that overwrites
==> /usr/bin/andromeda directly, conflicting with pacman's file management.
==> Use your AUR helper to upgrade this package instead.
EOF
}

post_upgrade() {
	post_install
}
