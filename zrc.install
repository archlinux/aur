post_install() {
	if ! grep -xqF "/usr/bin/zrc" /etc/shells; then
		echo>&2 "Copying zrc to /etc/shells..."
		echo "/usr/bin/zrc" >> /etc/shells
	fi
}

post_upgrade() {
	:
}

pre_remove() {
	echo>&2 "Removing shell entry..."	
	printf "%s" 'g/\/usr\/bin\/zrc/d' | ed -s /etc/shells
}
