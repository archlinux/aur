--- a/inc/com_api.h
+++ b/inc/com_api.h
@@ -43,7 +43,7 @@ extern "C"
 {
 #endif
 
-#define BIT_DEPTH 8
+#define BIT_DEPTH 10
 
 #if (BIT_DEPTH == 8)
     typedef unsigned char     pel; /* pixel type */
@@ -111,4 +111,4 @@ struct uavs3e_com_img_t {
 #endif
 
 
-#endif // #ifndef _COM_API_H_
\ No newline at end of file
+#endif // #ifndef _COM_API_H_
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -32,8 +32,8 @@ MESSAGE(STATUS "uavs3e version \t\t: ${uavs3e_version}")
 # pkg-config
 find_package(Threads REQUIRED)
 set(prefix "${CMAKE_INSTALL_PREFIX}")
-set(includedir "include")
-set(libdir "lib")
+set(includedir "include/uavs3e-10bit")
+set(libdir "lib/uavs3e-10bit")
 set(pc_file "${CONFIG_DIR}/${LIBNAME}.pc")
 
 set(CMAKE_INSTALL_INCLUDE_DIR "${CMAKE_INSTALL_PREFIX}/${includedir}")
@@ -73,6 +73,6 @@ target_link_libraries(${LIBNAME} pthread)
 endif()
 
 install(TARGETS uavs3e LIBRARY DESTINATION ${CMAKE_INSTALL_LIB_DIR} ARCHIVE DESTINATION ${CMAKE_INSTALL_LIB_DIR})
-install(FILES ../inc/uavs3e.h DESTINATION ${CMAKE_INSTALL_INCLUDE_DIR}/uavs3e/)
-install(FILES ../inc/com_api.h DESTINATION ${CMAKE_INSTALL_INCLUDE_DIR}/uavs3e/)
+install(FILES ../inc/uavs3e.h DESTINATION ${CMAKE_INSTALL_INCLUDE_DIR})
+install(FILES ../inc/com_api.h DESTINATION ${CMAKE_INSTALL_INCLUDE_DIR})
 install(FILES ${CONFIG_DIR}/${LIBNAME}.pc DESTINATION ${CMAKE_INSTALL_PKGCONFIG_DIR})
