print_zapfast_post_install() {
  cat <<'EOF2'
FastsApp is now ZapFast. For future updates, switch to the zapfast-bin package.
Quit FastsApp before starting ZapFast; your linked session and data move automatically.

zapfast setup:

  Launch it and, on the phone, open WhatsApp > Linked devices > Link a
  device, then point the camera at the code (or link with your phone
  number instead). The chats sync to a local archive on the first link.

  Colour emoji come from noto-fonts-emoji; GIFs play through ffmpeg.

Docs: https://zapfast.rocks
EOF2
}

post_install() {
  print_zapfast_post_install
}

post_upgrade() {
  print_zapfast_post_install
}
