--- a/Cargo.toml
+++ b/Cargo.toml
@@ -153,7 +153,6 @@ icu_normalizer = { version = "2", default-features = false, features = ["compile
 icu_properties = { version = "2.2.0", features = ["compiled_data"] }
 
 glow = { version = "0.18" }
-tikv-jemallocator = { version = "0.7" }
 wgpu-29 = { package = "wgpu", version = "29.0.4", default-features = false }
 wgpu-30 = { package = "wgpu", version = "30", default-features = false }
 # Version floor only: the Metal surface code (renderer-skia) calls
--- a/api/cpp/CMakeLists.txt
+++ b/api/cpp/CMakeLists.txt
@@ -109,10 +109,6 @@ if(SLINT_FEATURE_COMPILER AND NOT SLINT_COMPILER)
     if (SLINT_FEATURE_SDF_FONTS)
         list(APPEND slint_compiler_features "sdf-fonts")
     endif()
-    # Enable jemalloc, except when cross-compiling. (#7463)
-    if(NOT CMAKE_CROSSCOMPILING)
-        list(APPEND slint_compiler_features "jemalloc")
-    endif()
     set_property(
         TARGET slint-compiler
         PROPERTY CORROSION_FEATURES
--- a/api/cpp/lib.rs
+++ b/api/cpp/lib.rs
@@ -3,7 +3,7 @@
 
 /*! This crate just exposes the function used by the C++ integration */
 
-#![no_std]
+
 extern crate alloc;
 #[cfg(feature = "std")]
 extern crate std;
--- a/api/rs/slint/lib.rs
+++ b/api/rs/slint/lib.rs
@@ -199,7 +199,7 @@ struct MyComponent { /*...*/ }
 #![warn(missing_docs)]
 #![deny(unsafe_code)]
 #![doc(html_logo_url = "https://slint.dev/logo/slint-logo-square-light.svg")]
-#![no_std]
+
 #![allow(clippy::needless_doctest_main)] // We document how to write a main function
 
 extern crate alloc;
--- a/api/slint-sc/lib.rs
+++ b/api/slint-sc/lib.rs
@@ -2,7 +2,7 @@
 // SPDX-License-Identifier: GPL-3.0-only OR LicenseRef-Slint-Software-3.0
 
 #![doc = include_str!("README.md")]
-#![no_std]
+
 #![forbid(unsafe_code)]
 #![forbid(missing_docs)]
 
--- a/api/slint-sc/tests/driver.rs
+++ b/api/slint-sc/tests/driver.rs
@@ -492,7 +492,7 @@ fn assemble_program(gen_path: &str, body: &str) -> String {
         .replace('\\', "/");
     let mut content = String::new();
     // no_std so that accidental use of std in the generated code doesn't compile
-    writeln!(content, "#![no_std]").unwrap();
+    writeln!(content, "").unwrap();
     writeln!(content, "extern crate std;").unwrap();
     writeln!(content).unwrap();
     writeln!(content, "#[macro_use]").unwrap();
--- a/demos/analogclock/rust/main.rs
+++ b/demos/analogclock/rust/main.rs
@@ -1,7 +1,7 @@
 // Copyright © SixtyFPS GmbH <info@slint.dev>
 // SPDX-License-Identifier: MIT
 
-#![no_std]
+
 #![cfg_attr(not(feature = "simulator"), no_main)]
 
 extern crate alloc;
--- a/demos/energy-monitor/src/main.rs
+++ b/demos/energy-monitor/src/main.rs
@@ -1,6 +1,6 @@
 // Copyright © SixtyFPS GmbH <info@slint.dev>
 // SPDX-License-Identifier: MIT
-#![no_std]
+
 #![cfg_attr(all(feature = "mcu-board-support", not(feature = "simulator")), no_main)]
 
 #[cfg(feature = "mcu-board-support")]
--- a/demos/printerdemo_mcu/main.rs
+++ b/demos/printerdemo_mcu/main.rs
@@ -1,7 +1,7 @@
 // Copyright © SixtyFPS GmbH <info@slint.dev>
 // SPDX-License-Identifier: MIT
 
-#![no_std]
+
 #![cfg_attr(not(feature = "simulator"), no_main)]
 
 extern crate alloc;
--- a/examples/mcu-board-support/lib.rs
+++ b/examples/mcu-board-support/lib.rs
@@ -5,7 +5,7 @@
 
 #![doc = include_str!("README.md")]
 #![doc(html_logo_url = "https://slint.dev/logo/slint-logo-square-light.svg")]
-#![no_std]
+
 
 extern crate alloc;
 
--- a/examples/mcu-board-support/pico2_st7789/rp_pico2.rs
+++ b/examples/mcu-board-support/pico2_st7789/rp_pico2.rs
@@ -49,7 +49,7 @@
 ///
 /// # Examples
 /// ```ignore
-/// #![no_std]
+/// 
 /// #![no_main]
 /// use rp_pico::entry;
 /// #[entry]
--- a/examples/mcu-embassy/slint_generated/src/lib.rs
+++ b/examples/mcu-embassy/slint_generated/src/lib.rs
@@ -1,6 +1,6 @@
 // Copyright © 2025 David Haig
 // SPDX-License-Identifier: MIT
 
-#![no_std]
+
 
 slint::include_modules!();
--- a/examples/mcu-embassy/src/bin/ui_mcu.rs
+++ b/examples/mcu-embassy/src/bin/ui_mcu.rs
@@ -8,7 +8,7 @@
 // turn the grey circle next to "Hardware User Button" blue.
 // The hello world button demonstrates animations. More details in the readme.
 
-#![no_std]
+
 #![no_main]
 
 extern crate alloc;
--- a/examples/safe-ui/app/src/lib.rs
+++ b/examples/safe-ui/app/src/lib.rs
@@ -8,7 +8,7 @@
 //! airlock sequence is application logic, so it lives here in Rust. The scene
 //! shows the state it's given and decides nothing.
 
-#![no_std]
+
 
 use core::time::Duration;
 
--- a/examples/safe-ui/esp32-s3-box/src/main.rs
+++ b/examples/safe-ui/esp32-s3-box/src/main.rs
@@ -7,7 +7,7 @@
 //! scheduler provides embassy's time driver, and the UI runs as the main
 //! embassy task through [`slint_safeui_app::app_main`].
 
-#![no_std]
+
 #![no_main]
 
 extern crate alloc;
--- a/examples/safe-ui/ffi/src/lib.rs
+++ b/examples/safe-ui/ffi/src/lib.rs
@@ -1,7 +1,7 @@
 // Copyright © SixtyFPS GmbH <info@slint.dev>
 // SPDX-License-Identifier: MIT
 
-#![no_std]
+
 
 // Enforce linking crate that provides critical section implementation.
 // (We need these functions even though the lib code never calls them.)
--- a/examples/uefi-demo/main.rs
+++ b/examples/uefi-demo/main.rs
@@ -3,7 +3,7 @@
 
 // cSpell: ignore cntfrq cntvct conpointer inited mpointer
 #![no_main]
-#![no_std]
+
 #![cfg(target_os = "uefi")]
 
 extern crate alloc;
--- a/helper_crates/const-field-offset/src/lib.rs
+++ b/helper_crates/const-field-offset/src/lib.rs
@@ -41,7 +41,7 @@ struct Foo { a: u32, b: f64 }
 The `#[pin]` attribute enforces that the struct is `!Unpin` and does not
 implement `Drop` (use `#[pin_drop]` with [`PinnedDrop`] instead).
 */
-#![no_std]
+
 
 #[cfg(test)]
 extern crate alloc;
--- a/helper_crates/vtable/src/lib.rs
+++ b/helper_crates/vtable/src/lib.rs
@@ -56,7 +56,7 @@ fn make_noise(&self, intensity: i32) -> i32 {
 */
 
 #![warn(missing_docs)]
-#![no_std]
+
 extern crate alloc;
 
 #[doc(no_inline)]
--- a/helper_crates/vtable/tests/test_vtable.rs
+++ b/helper_crates/vtable/tests/test_vtable.rs
@@ -2,7 +2,7 @@
 // SPDX-License-Identifier: MIT OR Apache-2.0
 
 // cSpell: ignore ANOTHERSTRUCT vras vrss
-#![no_std]
+
 extern crate alloc;
 use crate::alloc::borrow::ToOwned;
 use alloc::boxed::Box;
--- a/internal/core/lib.rs
+++ b/internal/core/lib.rs
@@ -10,7 +10,7 @@
 #![allow(clippy::missing_safety_doc)] // FFI surface has many exported unsafe entry points
 #![cfg_attr(slint_nightly_test, feature(non_exhaustive_omitted_patterns_lint))]
 #![cfg_attr(slint_nightly_test, warn(non_exhaustive_omitted_patterns))]
-#![no_std]
+
 #![debugger_visualizer(gdb_script_file = "gdb_pretty_printers.py")]
 
 extern crate alloc;
--- a/internal/core/platform.rs
+++ b/internal/core/platform.rs
@@ -104,7 +104,7 @@ fn new_event_loop_proxy(&self) -> Option<Box<dyn EventLoopProxy>> {
     /// This is used by the animations and timer to compute the elapsed time.
     ///
     /// When the `std` feature is enabled, this function is implemented in terms of
-    /// [`std::time::Instant::now()`], but on `#![no_std]` platform, this function must
+    /// [`std::time::Instant::now()`], but on `` platform, this function must
     /// be implemented.
     fn duration_since_start(&self) -> core::time::Duration {
         #[cfg(feature = "std")]
--- a/internal/renderers/software/lib.rs
+++ b/internal/renderers/software/lib.rs
@@ -8,7 +8,7 @@
 #![deny(unsafe_code)]
 #![cfg_attr(slint_nightly_test, feature(non_exhaustive_omitted_patterns_lint))]
 #![cfg_attr(slint_nightly_test, warn(non_exhaustive_omitted_patterns))]
-#![no_std]
+
 #![warn(missing_docs)]
 
 extern crate alloc;
--- a/tools/compiler/Cargo.toml
+++ b/tools/compiler/Cargo.toml
@@ -21,7 +21,6 @@ path = "main.rs"
 [features]
 renderer-software = ["i-slint-compiler/renderer-software"]
 image-default-formats = ["i-slint-compiler/image-default-formats"]
-jemalloc = ["dep:tikv-jemallocator"]
 sdf-fonts = ["renderer-software", "i-slint-compiler/sdf-fonts"]
 fontconfig-dlopen = ["i-slint-common/fontconfig-dlopen"]
 
@@ -34,7 +33,7 @@ python = ["i-slint-compiler/python"]
 slint-sc = ["i-slint-compiler/slint-sc"]
 
 # Keep defaults in sync with defaults applied in api/cpp/CMakeLists.txt for slint-compiler
-default = ["renderer-software", "image-default-formats", "jemalloc", "bundle-translations", "cpp", "rust", "python"]
+default = ["renderer-software", "image-default-formats",  "bundle-translations", "cpp", "rust", "python"]
 
 [dependencies]
 i-slint-compiler = { workspace = true, features = ["default", "display-diagnostics"] }
@@ -46,4 +45,3 @@ spin_on = { workspace = true }
 itertools = { workspace = true }
 
 [target.'cfg(not(any(target_os = "openbsd", target_os = "windows", all(target_arch = "aarch64", target_os = "linux"))))'.dependencies]
-tikv-jemallocator = { workspace = true, optional = true }
--- a/tools/compiler/main.rs
+++ b/tools/compiler/main.rs
@@ -9,26 +9,6 @@
 use std::io::Cursor;
 use std::io::Write;
 
-#[cfg(all(
-    feature = "jemalloc",
-    not(any(
-        target_os = "openbsd",
-        target_os = "windows",
-        all(target_arch = "aarch64", target_os = "linux")
-    ))
-))]
-use tikv_jemallocator::Jemalloc;
-
-#[cfg(all(
-    feature = "jemalloc",
-    not(any(
-        target_os = "openbsd",
-        target_os = "windows",
-        all(target_arch = "aarch64", target_os = "linux")
-    ))
-))]
-#[global_allocator]
-static GLOBAL: Jemalloc = Jemalloc;
 
 #[derive(Copy, Clone, PartialEq, Eq, PartialOrd, Ord, ValueEnum)]
 enum Embedding {
--- a/tools/lsp/Cargo.toml
+++ b/tools/lsp/Cargo.toml
@@ -119,7 +119,6 @@ tokio-tungstenite-wasm = "0.8.2"
 postcard = { version = "1.1.3", default-features = false, features = ["alloc"] }
 
 [target.'cfg(not(any(target_os = "openbsd", target_os = "windows", target_arch = "wasm32", all(target_arch = "aarch64", target_os = "linux"))))'.dependencies]
-tikv-jemallocator = { workspace = true }
 
 [target.'cfg(target_os = "macos")'.dependencies]
 objc2-app-kit = { version = "0.3.2", default-features = false, features = ["std", "NSResponder", "NSToolbar", "NSView", "NSWindow", "objc2-core-foundation"] }
--- a/tools/lsp/main.rs
+++ b/tools/lsp/main.rs
@@ -59,7 +59,6 @@
     target_arch = "wasm32",
     all(target_arch = "aarch64", target_os = "linux")
 )))]
-use tikv_jemallocator::Jemalloc;
 
 #[cfg(not(any(
     target_os = "openbsd",
@@ -67,8 +66,6 @@
     target_arch = "wasm32",
     all(target_arch = "aarch64", target_os = "linux")
 )))]
-#[global_allocator]
-static GLOBAL: Jemalloc = Jemalloc;
 
 const RECOMPILE_IDLE_TIMEOUT: Duration = Duration::from_millis(50);
 
--- a/tools/viewer/Cargo.toml
+++ b/tools/viewer/Cargo.toml
@@ -126,7 +126,6 @@ i-slint-backend-android-activity = { workspace = true, optional = true }
 jni = { version = "0.22", optional = true }
 
 [target.'cfg(not(any(target_os = "openbsd", target_os = "windows", target_os = "ios", all(target_arch = "aarch64", target_os = "linux"))))'.dependencies]
-tikv-jemallocator = { workspace = true }
 
 [lib]
 crate-type = ["cdylib"]
--- a/tools/viewer/main.rs
+++ b/tools/viewer/main.rs
@@ -26,7 +26,6 @@
     target_os = "ios",
     all(target_arch = "aarch64", target_os = "linux")
 )))]
-use tikv_jemallocator::Jemalloc;
 
 #[cfg(not(any(
     target_os = "openbsd",
@@ -34,8 +33,6 @@
     target_os = "ios",
     all(target_arch = "aarch64", target_os = "linux")
 )))]
-#[global_allocator]
-static GLOBAL: Jemalloc = Jemalloc;
 
 struct Error(Box<dyn std::error::Error>);
 impl std::fmt::Debug for Error {
