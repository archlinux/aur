diff --color --unified --recursive --text OpenRV.clean/_build/RV_DEPS_PYTHON3/src/configure OpenRV/_build/RV_DEPS_PYTHON3/src/configure
--- OpenRV.clean/_build/RV_DEPS_PYTHON3/src/configure	2023-09-02 17:26:26.030799749 -0700
+++ OpenRV/_build/RV_DEPS_PYTHON3/src/configure 	2023-09-02 08:36:33.481984024 -0700
@@ -14544,16 +14544,24 @@
 if ac_fn_c_try_compile "$LINENO"; then :
 
 
-if grep noonsees conftest.$ac_objext >/dev/null ; then
+# if $GREP noonsees conftest.$ac_objext >/dev/null ; then
+#   ax_cv_c_float_words_bigendian=yes
+# fi
+# if $GREP seesnoon conftest.$ac_objext >/dev/null ; then
+#   if test "$ax_cv_c_float_words_bigendian" = unknown; then
+#     ax_cv_c_float_words_bigendian=no
+#   else
+#     ax_cv_c_float_words_bigendian=unknown
+#   fi
+# fi
+
+
+aur_float_words_littleendian=$(echo I | tr -d [:space:] | od -to2 | head -n1 | awk '{print $2}' | cut -c6)
+if [ $aur_float_words_littleendian -eq 1 ]; then
+  ax_cv_c_float_words_bigendian=no
+elif [ $aur_float_words_littleendian -eq 0 ]; then
   ax_cv_c_float_words_bigendian=yes
 fi
-if grep seesnoon conftest.$ac_objext >/dev/null ; then
-  if test "$ax_cv_c_float_words_bigendian" = unknown; then
-    ax_cv_c_float_words_bigendian=no
-  else
-    ax_cv_c_float_words_bigendian=unknown
-  fi
-fi
 
 
 fi
