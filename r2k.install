post_install() {
	cat <<- EOF
    =======================+========================
    To configure r2k you need to edit config.h 
    And rerun the pkgbuild using ``makepkg -scif``
    =======================+========================
	EOF
}

post_upgrade() {
	post_install $1
}
