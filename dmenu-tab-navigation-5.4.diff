--- a/dmenu.c	2026-02-16 20:24:20.161307172 -0400
+++ b/dmenu.c	2026-02-16 20:24:52.218345756 -0400
@@ -514,13 +514,17 @@
 			calcoffsets();
 		}
 		break;
-	case XK_Tab:
-		if (!sel)
-			return;
-		cursor = strnlen(sel->text, sizeof text - 1);
-		memcpy(text, sel->text, cursor);
-		text[cursor] = '\0';
-		match();
+	case XK_Tab:  /* Tab = navigate down */
+		if (sel && sel->right && (sel = sel->right) == next) {
+			curr = next;
+			calcoffsets();
+		}
+		break;
+	case XK_ISO_Left_Tab:  /* Shift+Tab = navigate up */
+		if (sel && sel->left && (sel = sel->left)->right == curr) {
+			curr = prev;
+			calcoffsets();
+		}
 		break;
 	}
 
