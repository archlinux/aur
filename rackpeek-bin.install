post_install() {
  echo -e "\nTo configure the config path, you need to use RPK_YAML_DIR env variable"
  echo
  echo "Example:"
  echo '$ mkdir ~/inventory'
  echo '$ RPK_YAML_DIR=~/inventory rackpeek -v'
  echo '$ RPK_YAML_DIR=~/inventory rackpeek servers summary'
  echo
}
