diff --git a/CMakeLists.txt b/CMakeLists.txt
index 1b41885..1b3fdfc 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -20,7 +20,6 @@ include(ECMEnableSanitizers)
 set(KDE_SKIP_TEST_SETTINGS TRUE)
 include(KDECMakeSettings)
 
-
 find_package(Okular5 REQUIRED)
 find_package(MuPDF REQUIRED)
 find_package(JPEG REQUIRED)
@@ -30,6 +29,10 @@ find_package(jbig2dec REQUIRED)
 find_package(OpenJPEG REQUIRED)
 find_package(LCMS2 REQUIRED)
 
+find_package(PkgConfig REQUIRED)
+pkg_check_modules(Gumbo REQUIRED IMPORTED_TARGET gumbo)
+
+
 find_package(KF5 REQUIRED COMPONENTS
     CoreAddons
     I18n
@@ -60,6 +63,7 @@ target_link_libraries(okularGenerator_mupdf
     ${OPENJPEG_LIBRARIES}
     ${JBIG2DEC_LIBRARIES}
     ${LCMS2_LIBRARIES}
+    PkgConfig::Gumbo
 )
 include_directories(
     ${OPENJPEG_INCLUDE_DIRS}
