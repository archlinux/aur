--- origsrc/cfitsio-4.0.0/cfileio.c	2019-05-08 17:34:49.000000000 +0200
+++ src/cfitsio-4.0.0/cfileio.c	2019-09-07 16:02:22.047976900 +0200
@@ -2716,7 +2716,7 @@ int fits_copy_cell2image(
     }
 
     /* determine BITPIX value for the image */
-    if (typecode == TBYTE)
+    if (typecode == TBYTEX)
     {
         bitpix = BYTE_IMG;
         nbytes = repeat;
@@ -2943,7 +2943,7 @@ int fits_copy_image2cell(
 
     /* Determine the TFORM value for the table cell */
     if (bitpix == BYTE_IMG) {
-      typecode = TBYTE;
+      typecode = TBYTEX;
       tformchar = 'B';
       nbytes = repeat;
     } else if (bitpix == SHORT_IMG) {
@@ -3032,7 +3032,7 @@ int fits_copy_image2cell(
  
     /* Force the writing of the row of the table by writing the last byte of
         the array, which grows the table, and/or shifts following extensions */
-    ffpcl(newptr, TBYTE, colnum, rownum, repeat, 1, &dummy, status);
+    ffpcl(newptr, TBYTEX, colnum, rownum, repeat, 1, &dummy, status);
 
     /* byte offset within the row to the start of the image column */
     colptr  = (newptr->Fptr)->tableptr;   /* point to first column */
--- origsrc/cfitsio-4.0.0/editcol.c	2019-05-08 17:34:49.000000000 +0200
+++ src/cfitsio-4.0.0/editcol.c	2019-09-07 16:02:22.126094500 +0200
@@ -1452,7 +1452,7 @@ int ffmvec(fitsfile *fptr,  /* I - FITS
     /* construct the new TFORM keyword for the column */
     if (datacode == TBIT)
       strcpy(tcode,"X");
-    else if (datacode == TBYTE)
+    else if (datacode == TBYTEX)
       strcpy(tcode,"B");
     else if (datacode == TLOGICAL)
       strcpy(tcode,"L");
--- origsrc/cfitsio-4.0.0/eval_f.c	2019-05-08 17:34:49.000000000 +0200
+++ src/cfitsio-4.0.0/eval_f.c	2019-09-07 16:02:22.141718600 +0200
@@ -634,7 +634,7 @@ int ffcalc_rng( fitsfile *infptr,   /* I
             if( gParse.hdutype==BINARY_TBL ) {
 	       LONGLONG nullVal=0;
                fits_binary_tform( parInfo, &typecode, &repeat, &width, status );
-               if( typecode==TBYTE )
+               if( typecode==TBYTEX )
                   nullVal = UCHAR_MAX;
                else if( typecode==TSHORT )
                   nullVal = SHRT_MIN;
@@ -1080,9 +1080,9 @@ int parse_data( long    totalrows,     /
        /* Determine the size of each element of the returned result */
 
        switch( userInfo->datatype ) {
-       case TBIT:       /*  Fall through to TBYTE  */
-       case TLOGICAL:   /*  Fall through to TBYTE  */
-       case TBYTE:     datasize = sizeof(char);     break;
+       case TBIT:       /*  Fall through to TBYTEX  */
+       case TLOGICAL:   /*  Fall through to TBYTEX  */
+       case TBYTEX:     datasize = sizeof(char);     break;
        case TSHORT:    datasize = sizeof(short);    break;
        case TINT:      datasize = sizeof(int);      break;
        case TLONG:     datasize = sizeof(long);     break;
@@ -1119,7 +1119,7 @@ int parse_data( long    totalrows,     /
 
        switch( userInfo->datatype ) {
        case TLOGICAL: *(char  *)Null = 'U';             break;
-       case TBYTE:    *(char  *)Null = (char )jnull;    break;
+       case TBYTEX:    *(char  *)Null = (char )jnull;    break;
        case TSHORT:   *(short *)Null = (short)jnull;    break;
        case TINT:     *(int   *)Null = (int  )jnull;    break;
        case TLONG:    *(long  *)Null = (long )jnull;    break;
@@ -1222,7 +1222,7 @@ int parse_data( long    totalrows,     /
 
        case BITSTR:
           switch( userInfo->datatype ) {
-          case TBYTE:
+          case TBYTEX:
              idx = -1;
              for( kk=0; kk<ntodo; kk++ ) {
                 for( jj=0; jj<result->value.nelem; jj++ ) {
@@ -1307,7 +1307,7 @@ int parse_data( long    totalrows,     /
 
        /*  Increment Data to point to where the next block should go  */
 
-       if( result->type==BITSTR && userInfo->datatype==TBYTE )
+       if( result->type==BITSTR && userInfo->datatype==TBYTEX )
           Data = (char*)Data
                     + datasize * ( (result->value.nelem+7)/8 ) * ntodo;
        else if( result->type==STRING )
@@ -1520,7 +1520,7 @@ int ffcvtn( int   inputType,  /* I - Dat
    case TLOGICAL:
       switch( inputType ) {
       case TLOGICAL:
-      case TBYTE:
+      case TBYTEX:
          for( i=0; i<ntodo; i++ )
             if( ((unsigned char*)input)[i] )
                 ((unsigned char*)output)[i] = 1;
@@ -1567,10 +1567,10 @@ int ffcvtn( int   inputType,  /* I - Dat
       }
       break;
 
-   case TBYTE:
+   case TBYTEX:
       switch( inputType ) {
       case TLOGICAL:
-      case TBYTE:
+      case TBYTEX:
          for( i=0; i<ntodo; i++ )
             ((unsigned char*)output)[i] = ((unsigned char*)input)[i];
          break;
@@ -1618,7 +1618,7 @@ int ffcvtn( int   inputType,  /* I - Dat
    case TSHORT:
       switch( inputType ) {
       case TLOGICAL:
-      case TBYTE:
+      case TBYTEX:
          for( i=0; i<ntodo; i++ )
             ((short*)output)[i] = ((unsigned char*)input)[i];
          break;
@@ -1666,7 +1666,7 @@ int ffcvtn( int   inputType,  /* I - Dat
    case TINT:
       switch( inputType ) {
       case TLOGICAL:
-      case TBYTE:
+      case TBYTEX:
          for( i=0; i<ntodo; i++ )
             ((int*)output)[i] = ((unsigned char*)input)[i];
          break;
@@ -1701,7 +1701,7 @@ int ffcvtn( int   inputType,  /* I - Dat
    case TLONG:
       switch( inputType ) {
       case TLOGICAL:
-      case TBYTE:
+      case TBYTEX:
          for( i=0; i<ntodo; i++ )
             ((long*)output)[i] = ((unsigned char*)input)[i];
          break;
@@ -1736,7 +1736,7 @@ int ffcvtn( int   inputType,  /* I - Dat
    case TLONGLONG:
       switch( inputType ) {
       case TLOGICAL:
-      case TBYTE:
+      case TBYTEX:
          for( i=0; i<ntodo; i++ )
             ((LONGLONG*)output)[i] = ((unsigned char*)input)[i];
          break;
@@ -1772,7 +1772,7 @@ int ffcvtn( int   inputType,  /* I - Dat
    case TFLOAT:
       switch( inputType ) {
       case TLOGICAL:
-      case TBYTE:
+      case TBYTEX:
          for( i=0; i<ntodo; i++ )
             ((float*)output)[i] = ((unsigned char*)input)[i];
          break;
@@ -1807,7 +1807,7 @@ int ffcvtn( int   inputType,  /* I - Dat
    case TDOUBLE:
       switch( inputType ) {
       case TLOGICAL:
-      case TBYTE:
+      case TBYTEX:
          for( i=0; i<ntodo; i++ )
             ((double*)output)[i] = ((unsigned char*)input)[i];
          break;
@@ -2336,10 +2336,10 @@ if (gParse.hdutype != IMAGE_HDU) {
    switch( typecode ) {
    case TBIT:
       varInfo->type     = BITSTR;
-      colIter->datatype = TBYTE;
+      colIter->datatype = TBYTEX;
       type = BITCOL;
       break;
-   case TBYTE:
+   case TBYTEX:
    case TSHORT:
    case TLONG:
       /* The datatype of column with TZERO and TSCALE keywords might be 
@@ -2539,7 +2539,7 @@ static int load_column( int varNum, long
    nelem = nRows * var->repeat;
 
    switch( var->datatype ) {
-   case TBYTE:
+   case TBYTEX:
       nbytes = ((var->repeat+7)/8) * nRows;
       bytes = (unsigned char *)malloc( nbytes * sizeof(char) );
 
@@ -2727,7 +2727,7 @@ int fits_pixel_filter (PixelFilter * fil
    }
 
    switch (bitpix) {
-      case BYTE_IMG: datatype = TLONG; Info.datatype = TBYTE; break;
+      case BYTE_IMG: datatype = TLONG; Info.datatype = TBYTEX; break;
       case SHORT_IMG: datatype = TLONG; Info.datatype = TSHORT; break;
       case LONG_IMG: datatype = TLONG; Info.datatype = TLONG; break;
       case FLOAT_IMG: datatype = TDOUBLE; Info.datatype = TFLOAT; break;
--- origsrc/cfitsio-4.0.0/fitscore.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/fitscore.c	2019-09-07 16:02:22.235462000 +0200
@@ -868,7 +868,7 @@ int ffpxsz(int datatype)
    return the number of bytes per pixel associated with the datatype
 */
 {
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
        return(sizeof(char));
     else if (datatype == TUSHORT)
        return(sizeof(short));
@@ -2957,7 +2957,7 @@ int ffbnfm(char *tform,     /* I - forma
     }
     else if (form[0] == 'B')
     {
-        datacode = TBYTE;
+        datacode = TBYTEX;
         width = 1;
     }
     else if (form[0] == 'S') /* internal code to signify signed byte */
@@ -3158,7 +3158,7 @@ int ffbnfmll(char *tform,     /* I - for
     }
     else if (form[0] == 'B')
     {
-        datacode = TBYTE;
+        datacode = TBYTEX;
         width = 1;
     }
     else if (form[0] == 'S') /* internal code to signify signed byte */
@@ -3815,7 +3815,7 @@ int ffeqtyll( fitsfile *fptr,  /* I - FI
 
     switch (tcode)
     {
-      case TBYTE:   /* binary table 'rB' column */
+      case TBYTEX:   /* binary table 'rB' column */
         min_val = 0.;
         max_val = 255.0;
         break;
@@ -3855,7 +3855,7 @@ int ffeqtyll( fitsfile *fptr,  /* I - FI
         (tzero != 9223372036854775808.) &&  /* indicates unsigned long long */
        (lngzero != tzero || lngscale != tscale)) { /* not integers? */
        /* floating point scaled values; just decide on required precision */
-       if (tcode == TBYTE || tcode == TSHORT)
+       if (tcode == TBYTEX || tcode == TSHORT)
           effcode = TFLOAT;
        else
           effcode = TDOUBLE;
@@ -4133,7 +4133,7 @@ int ffgbclll( fitsfile *fptr,   /* I - F
 
         if      (abs(colptr->tdatatype) == TBIT)
             strcat(dtype, "X");
-        else if (abs(colptr->tdatatype) == TBYTE)
+        else if (abs(colptr->tdatatype) == TBYTEX)
             strcat(dtype, "B");
         else if (abs(colptr->tdatatype) == TLOGICAL)
             strcat(dtype, "L");
@@ -4513,7 +4513,7 @@ int ffpinit(fitsfile *fptr,      /* I -
 
     if (bitpix == BYTE_IMG)   /* test  bitpix and set the datatype code */
     {
-        ttype=TBYTE;
+        ttype=TBYTEX;
         bytlen=1;
     }
     else if (bitpix == SHORT_IMG)
@@ -5655,9 +5655,9 @@
         *maxelem = DBUFFSIZE;
 
 	if (*tcode < 0) {
-	  *tcode = -TBYTE; /* variable-length */
+	  *tcode = -TBYTEX; /* variable-length */
 	} else {
-	  *tcode =  TBYTE;
+	  *tcode =  TBYTEX;
 	}
       }
 
@@ -5676,7 +5676,7 @@ int ffgcprll( fitsfile *fptr, /* I - FIT
     /* Special case: interprete 'X' column as 'B' */
     if (abs(*tcode) == TBIT)
     {
-        *tcode  = *tcode / TBIT * TBYTE;
+        *tcode  = *tcode / TBIT * TBYTEX;
         *repeat = (*repeat + 7) / 8;
     }
 
@@ -8931,7 +8931,7 @@ int ffinttyp(char *cval,  /* I - formatt
 	    if (strcmp(p,"127") <= 0 ) {
 	        *dtype = TSBYTE;
 	    } else if (strcmp(p,"255") <= 0 ) {
-	        *dtype = TBYTE;
+	        *dtype = TBYTEX;
 	    } else {
 	        *dtype = TSHORT;
 	    }
--- origsrc/cfitsio-4.0.0/fitsio.h	2019-09-07 15:32:03.540152200 +0200
+++ src/cfitsio-4.0.0/fitsio.h	2019-09-07 16:02:34.781421600 +0200
@@ -218,7 +218,7 @@ SERVICES PROVIDED HEREUNDER."
 #define FLEN_STATUS    31  /* max length of a FITSIO status text string */
  
 #define TBIT          1  /* codes for FITS table data types */
-#define TBYTE        11
+#define TBYTEX       11
 #define TSBYTE       12
 #define TLOGICAL     14
 #define TSTRING      16
@@ -350,7 +350,7 @@ typedef struct        /* structure used
     char strnull[20]; /* FITS null value string for ASCII table columns */
     char tform[10];   /* FITS tform keyword value  */
     long  twidth;     /* width of each ASCII table column */
-}tcolumn;
+} tcolumn;
 
 #define VALIDSTRUC 555  /* magic value used to identify if structure is valid */
 
@@ -455,7 +455,7 @@ typedef struct         /* structure used
 {
     int HDUposition;  /* HDU position in file; 0 = first HDU */
     FITSfile *Fptr;   /* pointer to FITS file structure */
-}fitsfile;
+} fitsfile;
 
 typedef struct  /* structure for the iterator function column information */
 {  
--- origsrc/cfitsio-4.0.0/fpackutil.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/fpackutil.c	2019-09-07 16:02:22.251099100 +0200
@@ -1703,7 +1703,7 @@ int fits_read_image_speed (fitsfile *inf
          /*       (infptr->Fptr)->tilerow = 0; */
 
 		marktime(status);
-		fits_read_subset(infptr, TBYTE, fpixel, lpixel, inc, &cnull, 
+		fits_read_subset(infptr, TBYTEX, fpixel, lpixel, inc, &cnull, 
 		      carray, &anynull, status);
 		
 		/* get elapsped times */
@@ -1719,7 +1719,7 @@ int fits_read_image_speed (fitsfile *inf
 		  marktime(status);
 		  for (ii = 0; ii < naxes[1]; ii++) {
 		   fpixel[1] = ii+1;
-		   fits_read_pix(infptr, TBYTE, fpixel, naxes[0], &cnull, 
+		   fits_read_pix(infptr, TBYTEX, fpixel, naxes[0], &cnull, 
 		      carray, &anynull, status);
 		   }
 		   /* get elapsped times */
--- origsrc/cfitsio-4.0.0/getcol.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcol.c	2019-09-07 16:02:22.251099100 +0200
@@ -130,7 +130,7 @@ int ffgpxvll( fitsfile *fptr, /* I - FIT
       and the second column contains the image itself.
     */
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       if (nulval == 0)
         ffgclb(fptr, 2, 1, firstelem, nelem, 1, 1, 0,
@@ -332,7 +332,7 @@ int ffgpxfll( fitsfile *fptr, /* I - FIT
       and the second column contains the image itself.
     */
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
         ffgclb(fptr, 2, 1, firstelem, nelem, 1, 2, 0,
                (unsigned char *) array, nullarray, anynul, status);
@@ -445,7 +445,7 @@ int ffgsv(  fitsfile *fptr,   /* I - FIT
         }
     }
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       if (nulval == 0)
         ffgsvb(fptr, 1, naxis, naxes, blc, trc, inc, 0,
@@ -588,7 +588,7 @@ int ffgpv(  fitsfile *fptr,   /* I - FIT
       and the second column contains the image itself.
     */
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       if (nulval == 0)
         ffgpvb(fptr, 1, firstelem, nelem, 0,
@@ -732,7 +732,7 @@ int ffgpf(  fitsfile *fptr,   /* I - FIT
       and the second column contains the image itself.
     */
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
         ffgpfb(fptr, 1, firstelem, nelem, 
                (unsigned char *) array, nullarray, anynul, status);
@@ -827,7 +827,7 @@ int ffgcv(  fitsfile *fptr,   /* I - FIT
     {
       ffgcx(fptr, colnum, firstrow, firstelem, nelem, (char *) array, status);
     }
-    else if (datatype == TBYTE)
+    else if (datatype == TBYTEX)
     {
       if (nulval == 0)
         ffgclb(fptr, colnum, firstrow, firstelem, nelem, 1, 1, 0,
@@ -1012,7 +1012,7 @@ int ffgcf(  fitsfile *fptr,   /* I - FIT
     {
       ffgcx(fptr, colnum, firstrow, firstelem, nelem, (char *) array, status);
     }
-    else if (datatype == TBYTE)
+    else if (datatype == TBYTEX)
     {
        ffgclb(fptr, colnum, firstrow, firstelem, nelem, 1, 2, (unsigned char )
               nulval, (unsigned char *) array, nullarray, anynul, status);
--- origsrc/cfitsio-4.0.0/getcolb.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcolb.c	2019-09-07 16:02:22.266722700 +0200
@@ -39,7 +39,7 @@ int ffgpvb( fitsfile *fptr,   /* I - FIT
         /* this is a compressed image in a binary table */
          nullvalue = nulval;  /* set local variable */
 
-        fits_read_compressed_pixels(fptr, TBYTE, firstelem, nelem,
+        fits_read_compressed_pixels(fptr, TBYTEX, firstelem, nelem,
             nullcheck, &nullvalue, array, NULL, anynul, status);
         return(*status);
     }
@@ -81,7 +81,7 @@ int ffgpfb( fitsfile *fptr,   /* I - FIT
     {
         /* this is a compressed image in a binary table */
 
-        fits_read_compressed_pixels(fptr, TBYTE, firstelem, nelem,
+        fits_read_compressed_pixels(fptr, TBYTEX, firstelem, nelem,
             nullcheck, NULL, array, nularray, anynul, status);
         return(*status);
     }
@@ -162,7 +162,7 @@ int ffg3db(fitsfile *fptr,  /* I - FITS
         lpixel[2] = naxis3;
         nullvalue = nulval;  /* set local variable */
 
-        fits_read_compressed_img(fptr, TBYTE, fpixel, lpixel, inc,
+        fits_read_compressed_img(fptr, TBYTEX, fpixel, lpixel, inc,
             nullcheck, &nullvalue, array, NULL, anynul, status);
         return(*status);
     }
@@ -253,7 +253,7 @@ int ffgsvb(fitsfile *fptr, /* I - FITS f
 
         nullvalue = nulval;  /* set local variable */
 
-        fits_read_compressed_img(fptr, TBYTE, blcll, trcll, inc,
+        fits_read_compressed_img(fptr, TBYTEX, blcll, trcll, inc,
             nullcheck, &nullvalue, array, NULL, anynul, status);
         return(*status);
     }
@@ -428,7 +428,7 @@ int ffgsfb(fitsfile *fptr, /* I - FITS f
 	    trcll[ii] = trc[ii];
 	}
 
-        fits_read_compressed_img(fptr, TBYTE, blcll, trcll, inc,
+        fits_read_compressed_img(fptr, TBYTEX, blcll, trcll, inc,
             nullcheck, NULL, array, flagval, anynul, status);
         return(*status);
     }
@@ -746,7 +746,7 @@ int ffgclb( fitsfile *fptr,   /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -757,7 +757,7 @@ int ffgclb( fitsfile *fptr,   /* I - FIT
     /*  not need to use a temporary buffer to store intermediate datatype.  */
     /*----------------------------------------------------------------------*/
     convert = 1;
-    if (tcode == TBYTE) /* Special Case:                        */
+    if (tcode == TBYTEX) /* Special Case:                        */
     {                             /* no type convertion required, so read */
                                   /* data directly into output buffer.    */
 
@@ -806,7 +806,7 @@ int ffgclb( fitsfile *fptr,   /* I - FIT
 
         switch (tcode) 
         {
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, &array[next], status);
                 if (convert)
                     fffi1i1(&array[next], ntodo, scale, zero, nulcheck, 
--- origsrc/cfitsio-4.0.0/getcold.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcold.c	2019-09-07 16:02:22.266722700 +0200
@@ -795,7 +795,7 @@ int ffgcld( fitsfile *fptr,   /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -862,7 +862,7 @@ int ffgcld( fitsfile *fptr,   /* I - FIT
                            nulval, &nularray[next], anynul, 
                            &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                        status);
                 fffi1r8((unsigned char *) buffer, ntodo, scale, zero, nulcheck, 
--- origsrc/cfitsio-4.0.0/getcole.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcole.c	2019-09-07 16:02:22.282346000 +0200
@@ -796,7 +796,7 @@ int ffgcle( fitsfile *fptr,   /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -863,7 +863,7 @@ int ffgcle( fitsfile *fptr,   /* I - FIT
                            nulval, &nularray[next], anynul, 
                            &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                        status);
                 fffi1r4((unsigned char *) buffer, ntodo, scale, zero, nulcheck, 
--- origsrc/cfitsio-4.0.0/getcoli.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcoli.c	2019-09-07 16:02:22.282346000 +0200
@@ -719,7 +719,7 @@ int ffgcli( fitsfile *fptr,   /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -793,7 +793,7 @@ int ffgcli( fitsfile *fptr,   /* I - FIT
                            nulcheck, tnull, nulval, &nularray[next], 
                             anynul, &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                       status);
                 fffi1i2((unsigned char *) buffer, ntodo, scale, zero, nulcheck, 
--- origsrc/cfitsio-4.0.0/getcolj.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcolj.c	2019-09-07 16:02:22.297973200 +0200
@@ -718,7 +718,7 @@ int ffgclj( fitsfile *fptr,   /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -802,7 +802,7 @@ int ffgclj( fitsfile *fptr,   /* I - FIT
                            nulcheck, tnull, nulval, &nularray[next], 
                             anynul, &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                        status);
                 fffi1i4((unsigned char *) buffer, ntodo, scale, zero, nulcheck, 
@@ -2660,7 +2660,7 @@ int ffgcljj( fitsfile *fptr,   /* I - FI
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -2735,7 +2735,7 @@ int ffgcljj( fitsfile *fptr,   /* I - FI
                         nulcheck, (INT32BIT) tnull, nulval, &nularray[next], 
                         anynul, &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                        status);
                 fffi1i8((unsigned char *) buffer, ntodo, scale, zero, nulcheck, 
--- origsrc/cfitsio-4.0.0/getcolk.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcolk.c	2019-09-07 16:02:22.313594200 +0200
@@ -735,7 +735,7 @@ int ffgclk( fitsfile *fptr,   /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -810,7 +810,7 @@ int ffgclk( fitsfile *fptr,   /* I - FIT
                            nulcheck, tnull, nulval, &nularray[next], 
                             anynul, &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                        status);
                 fffi1int((unsigned char *) buffer, ntodo, scale, zero, nulcheck,
--- origsrc/cfitsio-4.0.0/getcoll.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcoll.c	2019-09-07 16:02:22.313594200 +0200
@@ -270,7 +270,7 @@ int ffgcx(  fitsfile *fptr,  /* I - FITS
 
     tcode = colptr->tdatatype;
 
-    if (abs(tcode) > TBYTE)
+    if (abs(tcode) > TBYTEX)
         return(*status = NOT_LOGICAL_COL); /* not correct datatype column */
 
     if (tcode > 0)
@@ -424,7 +424,7 @@ int ffgcxui(fitsfile *fptr,   /* I - FIT
     colptr  = (fptr->Fptr)->tableptr;   /* point to first column */
     colptr += (colnum - 1);     /* offset to correct column structure */
 
-    if (abs(colptr->tdatatype) > TBYTE)
+    if (abs(colptr->tdatatype) > TBYTEX)
     {
         ffpmsg("Can only read bits from X or B type columns. (ffgcxui)");
         return(*status = NOT_LOGICAL_COL); /* not correct datatype column */
@@ -440,7 +440,7 @@ int ffgcxui(fitsfile *fptr,   /* I - FIT
         ffpmsg("Too many bits. Tried to read past width of column (ffgcxui)");
         return(*status = BAD_ELEM_NUM);
     }
-    else if (colptr->tdatatype == TBYTE && lastbyte > (long) colptr->trepeat)
+    else if (colptr->tdatatype == TBYTEX && lastbyte > (long) colptr->trepeat)
     {
         ffpmsg("Too many bits. Tried to read past width of column (ffgcxui)");
         return(*status = BAD_ELEM_NUM);
@@ -561,7 +561,7 @@ int ffgcxuk(fitsfile *fptr,   /* I - FIT
     colptr  = (fptr->Fptr)->tableptr;   /* point to first column */
     colptr += (colnum - 1);     /* offset to correct column structure */
 
-    if (abs(colptr->tdatatype) > TBYTE)
+    if (abs(colptr->tdatatype) > TBYTEX)
     {
         ffpmsg("Can only read bits from X or B type columns. (ffgcxuk)");
         return(*status = NOT_LOGICAL_COL); /* not correct datatype column */
@@ -577,7 +577,7 @@ int ffgcxuk(fitsfile *fptr,   /* I - FIT
         ffpmsg("Too many bits. Tried to read past width of column (ffgcxuk)");
         return(*status = BAD_ELEM_NUM);
     }
-    else if (colptr->tdatatype == TBYTE && lastbyte > (long) colptr->trepeat)
+    else if (colptr->tdatatype == TBYTEX && lastbyte > (long) colptr->trepeat)
     {
         ffpmsg("Too many bits. Tried to read past width of column (ffgcxuk)");
         return(*status = BAD_ELEM_NUM);
--- origsrc/cfitsio-4.0.0/getcols.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcols.c	2019-09-07 16:02:22.313594200 +0200
@@ -537,7 +537,7 @@ int ffgcls( fitsfile *fptr,   /* I - FIT
                  /* this is a binary table, need to convert the format */
                   if (tcode == TBIT) {            /* 'X' */
                      strcpy(cform, "%4d");
-                  } else if (tcode == TBYTE) {    /* 'B' */
+                  } else if (tcode == TBYTEX) {    /* 'B' */
                      strcpy(cform, "%4d");
                   } else if (tcode == TSHORT) {   /* 'I' */
                      strcpy(cform, "%6d");
@@ -726,7 +726,7 @@ int ffgcdw( fitsfile *fptr,   /* I - FIT
                  /* this is a binary table */
                   if (tcode == TBIT)           /* 'X' */
                      *width = 8;
-                  else if (tcode == TBYTE)     /* 'B' */
+                  else if (tcode == TBYTEX)     /* 'B' */
                      *width = 4;
                   else if (tcode == TSHORT)    /* 'I' */
                      *width = 6;
--- origsrc/cfitsio-4.0.0/getcolsb.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcolsb.c	2019-09-07 16:02:22.329217600 +0200
@@ -722,7 +722,7 @@ int ffgclsb(fitsfile *fptr,   /* I - FIT
 
         /*  we have to reset some of the values returned by ffgcpr */
         
-        tcode = TBYTE;
+        tcode = TBYTEX;
         incre = 1;         /* each element is 1 byte wide */
         repeat = twidth;   /* total no. of chars in the col */
         twidth = 1;        /* width of each element */
@@ -759,7 +759,7 @@ int ffgclsb(fitsfile *fptr,   /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -800,7 +800,7 @@ int ffgclsb(fitsfile *fptr,   /* I - FIT
 
         switch (tcode) 
         {
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) &array[next], status);
                 fffi1s1((unsigned char *)&array[next], ntodo, scale, zero,
                         nulcheck, (unsigned char) tnull, nulval, &nularray[next], 
--- origsrc/cfitsio-4.0.0/getcolui.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcolui.c	2019-09-07 16:02:22.329217600 +0200
@@ -704,7 +704,7 @@ int ffgclui( fitsfile *fptr,   /* I - FI
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -767,7 +767,7 @@ int ffgclui( fitsfile *fptr,   /* I - FI
                            nulcheck, tnull, nulval, &nularray[next], 
                             anynul, &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                       status);
                 fffi1u2((unsigned char *) buffer, ntodo, scale, zero, nulcheck, 
--- origsrc/cfitsio-4.0.0/getcoluj.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcoluj.c	2019-09-07 16:02:22.344841400 +0200
@@ -705,7 +705,7 @@ int ffgcluj(fitsfile *fptr,   /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -778,7 +778,7 @@ int ffgcluj(fitsfile *fptr,   /* I - FIT
                            nulcheck, tnull, nulval, &nularray[next], 
                             anynul, &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                        status);
                 fffi1u4((unsigned char *) buffer, ntodo, scale, zero, nulcheck, 
@@ -2681,7 +2681,7 @@ int ffgclujj( fitsfile *fptr,   /* I - F
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -2738,7 +2738,7 @@ int ffgclujj( fitsfile *fptr,   /* I - F
                         nulcheck, (INT32BIT) tnull, nulval, &nularray[next], 
                         anynul, &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                        status);
                 fffi1u8((unsigned char *) buffer, ntodo, scale, zero, nulcheck, 
--- origsrc/cfitsio-4.0.0/getcoluk.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getcoluk.c	2019-09-07 16:02:22.360465600 +0200
@@ -722,7 +722,7 @@ int ffgcluk( fitsfile *fptr,  /* I - FIT
     else if (tcode == TSHORT && (tnull > SHRT_MAX || tnull < SHRT_MIN) )
             nulcheck = 0;            /* Impossible null value */
 
-    else if (tcode == TBYTE && (tnull > 255 || tnull < 0) )
+    else if (tcode == TBYTEX && (tnull > 255 || tnull < 0) )
             nulcheck = 0;            /* Impossible null value */
 
     else if (tcode == TSTRING && snull[0] == ASCII_NULL_UNDEFINED)
@@ -785,7 +785,7 @@ int ffgcluk( fitsfile *fptr,  /* I - FIT
                            nulcheck, tnull, nulval, &nularray[next], 
                             anynul, &array[next], status);
                 break;
-            case (TBYTE):
+            case (TBYTEX):
                 ffgi1b(fptr, readptr, ntodo, incre, (unsigned char *) buffer,
                        status);
                 fffi1uint((unsigned char *) buffer, ntodo, scale, zero,nulcheck,
--- origsrc/cfitsio-4.0.0/getkey.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/getkey.c	2019-09-07 16:02:22.360465600 +0200
@@ -296,7 +296,7 @@ int ffgky( fitsfile *fptr,     /* I - FI
     {
         ffgkys(fptr, keyname, (char *) value, comm, status);
     }
-    else if (datatype == TBYTE)
+    else if (datatype == TBYTEX)
     {
         if (ffgkyjj(fptr, keyname, &longval, comm, status) <= 0)
         {
--- origsrc/cfitsio-4.0.0/histo.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/histo.c	2019-09-07 16:02:22.407337100 +0200
@@ -111,7 +111,7 @@ int ffbins(char *binspec,   /* I - binni
     }
     else if (*ptr == 'b' )  /* binb */
     {
-        *imagetype = TBYTE;
+        *imagetype = TBYTEX;
         defaulttype = 0;
         ptr ++;
     }
@@ -565,7 +565,7 @@ int ffhist2(fitsfile **fptr,  /* IO - po
     if ((*fptr)->HDUposition != ((*fptr)->Fptr)->curhdu)
         ffmahd(*fptr, ((*fptr)->HDUposition) + 1, NULL, status);
 
-    if (imagetype == TBYTE)
+    if (imagetype == TBYTEX)
         bitpix = BYTE_IMG;
     else if (imagetype == TSHORT)
         bitpix = SHORT_IMG;
@@ -710,7 +710,7 @@ fitsfile *ffhist3(fitsfile *fptr, /* I -
     if ((fptr)->HDUposition != ((fptr)->Fptr)->curhdu)
         ffmahd(fptr, ((fptr)->HDUposition) + 1, NULL, status);
 
-    if (imagetype == TBYTE)
+    if (imagetype == TBYTEX)
         bitpix = BYTE_IMG;
     else if (imagetype == TSHORT)
         bitpix = SHORT_IMG;
@@ -868,7 +868,7 @@ int ffhist(fitsfile **fptr,  /* IO - poi
     histData.haxis      = naxis;
     histData.rowselector = selectrow;
 
-    if (imagetype == TBYTE)
+    if (imagetype == TBYTEX)
         bitpix = BYTE_IMG;
     else if (imagetype == TSHORT)
         bitpix = SHORT_IMG;
@@ -2157,7 +2157,7 @@ int fits_make_histd(fitsfile *fptr, /* I
     }
 
     if   (bitpix == BYTE_IMG)
-         imagetype = TBYTE;
+         imagetype = TBYTEX;
     else if (bitpix == SHORT_IMG)
          imagetype = TSHORT;
     else if (bitpix == LONG_IMG)
@@ -2329,7 +2329,7 @@ int ffwritehisto(long totaln, long pixof
     /* store pointer to the histogram array, and initialize to zero */
 
     switch( histData->himagetype ) {
-    case TBYTE:
+    case TBYTEX:
        histData->hist.b = (char *  ) fits_iter_get_array(imagepars);
        break;
     case TSHORT:
@@ -2502,7 +2502,7 @@ int ffcalchist(long totalrows, long offs
               histData.hist.r[ipix] += histData.weight;
             else if (histData.himagetype == TDOUBLE)
               histData.hist.d[ipix] += histData.weight;
-            else if (histData.himagetype == TBYTE)
+            else if (histData.himagetype == TBYTEX)
               histData.hist.b[ipix] += (char) histData.weight;
         }
         else if (histData.wtrecip) /* use reciprocal of the weight */
@@ -2515,7 +2515,7 @@ int ffcalchist(long totalrows, long offs
               histData.hist.r[ipix] += (float) (1./wtcol[ii]);
             else if (histData.himagetype == TDOUBLE)
               histData.hist.d[ipix] += 1./wtcol[ii];
-            else if (histData.himagetype == TBYTE)
+            else if (histData.himagetype == TBYTEX)
               histData.hist.b[ipix] += (char) (1./wtcol[ii]);
         }
         else   /* no weights */
@@ -2528,7 +2528,7 @@ int ffcalchist(long totalrows, long offs
               histData.hist.r[ipix] += wtcol[ii];
             else if (histData.himagetype == TDOUBLE)
               histData.hist.d[ipix] += wtcol[ii];
-            else if (histData.himagetype == TBYTE)
+            else if (histData.himagetype == TBYTEX)
               histData.hist.b[ipix] += (char) wtcol[ii];
         }
 
--- origsrc/cfitsio-4.0.0/imcompress.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/imcompress.c	2019-09-07 16:02:22.422961100 +0200
@@ -1554,7 +1554,7 @@ int imcomp_compress_image (fitsfile *inf
     else if ((outfptr->Fptr)->zbitpix == BYTE_IMG)
     {
 
-        datatype = TBYTE;
+        datatype = TBYTEX;
         if ( (outfptr->Fptr)->compress_type == RICE_1  ||
 	     (outfptr->Fptr)->compress_type == BZIP2_1 ||
 	     (outfptr->Fptr)->compress_type == GZIP_1  ||
@@ -1670,7 +1670,7 @@ int imcomp_compress_image (fitsfile *inf
               ffgsvi(infptr, 1, naxis, naxes, fpixel, lpixel, incre, 
                   0, (short *) tiledata,  &anynul, status);
           }
-          else if (datatype == TBYTE)
+          else if (datatype == TBYTEX)
           {
               ffgsvb(infptr, 1, naxis, naxes, fpixel, lpixel, incre, 
                   0, (unsigned char *) tiledata,  &anynul, status);
@@ -1883,7 +1883,7 @@ int imcomp_compress_tile (fitsfile *outf
     } else if (datatype == TUSHORT) {
        imcomp_convert_tile_tushort(outfptr, tiledata, tilelen, nullcheck, nullflagval,
            nullval, zbitpix, scale, zero, &intlength, status);
-    } else if (datatype == TBYTE) {
+    } else if (datatype == TBYTEX) {
        imcomp_convert_tile_tbyte(outfptr, tiledata, tilelen, nullcheck, nullflagval,
            nullval, zbitpix, scale, zero,  &intlength, status);
     } else if (datatype == TSBYTE) {
@@ -3549,7 +3549,7 @@ int fits_write_compressed_img(fitsfile *
     {
        pixlen = sizeof(int);
     }
-    else if (datatype == TBYTE || datatype == TSBYTE)
+    else if (datatype == TBYTEX || datatype == TSBYTE)
     {
        pixlen = 1;
     }
@@ -4057,7 +4057,7 @@ int fits_img_decompress (fitsfile *infpt
     /* determine datatype for image */
     if ((infptr->Fptr)->zbitpix == BYTE_IMG)
     {
-        datatype = TBYTE;
+        datatype = TBYTEX;
     }
     else if ((infptr->Fptr)->zbitpix == SHORT_IMG)
     {
@@ -4159,7 +4159,7 @@ int fits_decompress_img (fitsfile *infpt
     /* determine datatype for image */
     if ((infptr->Fptr)->zbitpix == BYTE_IMG)
     {
-        datatype = TBYTE;
+        datatype = TBYTEX;
         byte_per_pix = 1;
     }
     else if ((infptr->Fptr)->zbitpix == SHORT_IMG)
@@ -4493,7 +4493,7 @@ int fits_read_compressed_img(fitsfile *f
        if (nullval)
            testnullval = *(unsigned long *) nullval;
     }
-    else if (datatype == TBYTE || datatype == TSBYTE)
+    else if (datatype == TBYTEX || datatype == TSBYTE)
     {
        buffer =  malloc ((fptr->Fptr)->maxtilelen * sizeof (char));
        pixlen = 1;
@@ -4791,7 +4791,7 @@ int fits_read_write_compressed_img(fitsf
 	 }
        }
     }
-    else if (datatype == TBYTE || datatype == TSBYTE)
+    else if (datatype == TBYTEX || datatype == TSBYTE)
     {
        buffer =  malloc ((fptr->Fptr)->maxtilelen * sizeof (char));
        if (cnull) {
@@ -5983,7 +5983,7 @@ int imcomp_decompress_tile (fitsfile *in
             }
 
             /* read array of compressed bytes */
-            if (fits_read_col(infptr, TBYTE, (infptr->Fptr)->cn_gzip_data, nrow,
+            if (fits_read_col(infptr, TBYTEX, (infptr->Fptr)->cn_gzip_data, nrow,
                  1, (long) nelemll, &charnull, cbuf, NULL, status) > 0) {
                 ffpmsg("error reading compressed byte stream from binary table");
 	        free (cbuf);
@@ -6273,7 +6273,7 @@ int imcomp_decompress_tile (fitsfile *in
         fits_read_col(infptr, TSHORT, (infptr->Fptr)->cn_compressed, nrow,
              1, (long) nelemll, &snull, (short *) cbuf, NULL, status);
     } else {
-       fits_read_col(infptr, TBYTE, (infptr->Fptr)->cn_compressed, nrow,
+       fits_read_col(infptr, TBYTEX, (infptr->Fptr)->cn_compressed, nrow,
              1, (long) nelemll, &charnull, cbuf, NULL, status);
     }
 
@@ -6294,7 +6294,7 @@ int imcomp_decompress_tile (fitsfile *in
         if ((infptr->Fptr)->rice_bytepix == 1 ) {
             *status = fits_rdecomp_byte (cbuf, (long) nelemll, (unsigned char *)idata,
                         tilelen, blocksize);
-            tiledatatype = TBYTE;
+            tiledatatype = TBYTEX;
         } else if ((infptr->Fptr)->rice_bytepix == 2 ) {
             *status = fits_rdecomp_short (cbuf, (long) nelemll, (unsigned short *)idata,
                         tilelen, blocksize);
@@ -6371,7 +6371,7 @@ int imcomp_decompress_tile (fitsfile *in
         } else if (tilebytesize == (size_t) tilelen) {
 	    
 	    /* this is an unsigned char I*1 array */
-            tiledatatype = TBYTE;
+            tiledatatype = TBYTEX;
 
         } else {
             ffpmsg("error: uncompressed tile has wrong size");
@@ -6395,7 +6395,7 @@ int imcomp_decompress_tile (fitsfile *in
         }
 
         if ((infptr->Fptr)->zbitpix == BYTE_IMG) {
-	     tiledatatype = TBYTE;
+	     tiledatatype = TBYTEX;
         } else if ((infptr->Fptr)->zbitpix == SHORT_IMG) {
   	     tiledatatype = TSHORT;
 #if BYTESWAPPED
@@ -6475,7 +6475,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2i2((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            *(short *) nulval, bnullarray, anynul,
           (short *) buffer, status);
-        } else if (tiledatatype == TBYTE) {
+        } else if (tiledatatype == TBYTEX) {
           fffi1i2((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            *(short *) nulval, bnullarray, anynul,
           (short *) buffer, status);
@@ -6514,7 +6514,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2int((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            *(int *) nulval, bnullarray, anynul,
            (int *) buffer, status);
-        else if (tiledatatype == TBYTE)
+        else if (tiledatatype == TBYTEX)
           fffi1int((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            *(int *) nulval, bnullarray, anynul,
            (int *) buffer, status);
@@ -6552,7 +6552,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2i4((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            *(long *) nulval, bnullarray, anynul,
             (long *) buffer, status);
-        else if (tiledatatype == TBYTE)
+        else if (tiledatatype == TBYTEX)
           fffi1i4((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            *(long *) nulval, bnullarray, anynul,
             (long *) buffer, status);
@@ -6593,7 +6593,7 @@ int imcomp_decompress_tile (fitsfile *in
 	   tilelen, bscale, bzero, (infptr->Fptr)->quantize_method, nullcheck, (short) tnull,
            fnulval, bnullarray, anynul,
             (float *) buffer, status);
-         else if (tiledatatype == TBYTE)
+         else if (tiledatatype == TBYTEX)
           unquantize_i1r4(nrow + (infptr->Fptr)->dither_seed - 1, (unsigned char *)idata, 
 	   tilelen, bscale, bzero, (infptr->Fptr)->quantize_method, nullcheck, (unsigned char) tnull,
            fnulval, bnullarray, anynul,
@@ -6617,7 +6617,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2r4((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,  
            fnulval, bnullarray, anynul,
             (float *) buffer, status);
-         else if (tiledatatype == TBYTE)
+         else if (tiledatatype == TBYTEX)
           fffi1r4((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            fnulval, bnullarray, anynul,
             (float *) buffer, status);
@@ -6658,7 +6658,7 @@ int imcomp_decompress_tile (fitsfile *in
 	   tilelen, bscale, bzero, (infptr->Fptr)->quantize_method, nullcheck, (short) tnull,
            dnulval, bnullarray, anynul,
             (double *) buffer, status);
-         else if (tiledatatype == TBYTE)
+         else if (tiledatatype == TBYTEX)
           unquantize_i1r8(nrow + (infptr->Fptr)->dither_seed - 1, (unsigned char *)idata,
 	   tilelen, bscale, bzero, (infptr->Fptr)->quantize_method, nullcheck, (unsigned char) tnull,
            dnulval, bnullarray, anynul,
@@ -6682,13 +6682,13 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2r8((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            dnulval, bnullarray, anynul,
             (double *) buffer, status);
-         } else if (tiledatatype == TBYTE)
+         } else if (tiledatatype == TBYTEX)
           fffi1r8((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            dnulval, bnullarray, anynul,
             (double *) buffer, status);
 	}
     }
-    else if (datatype == TBYTE)
+    else if (datatype == TBYTEX)
     {
         pixlen = sizeof(char);
         if (tiledatatype == TINT)
@@ -6699,7 +6699,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2i1((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            *(unsigned char *) nulval, bnullarray, anynul,
             (unsigned char *) buffer, status);
-        else if (tiledatatype == TBYTE)
+        else if (tiledatatype == TBYTEX)
           fffi1i1((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            *(unsigned char *) nulval, bnullarray, anynul,
             (unsigned char *) buffer, status);
@@ -6715,7 +6715,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2s1((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            *(signed char *) nulval, bnullarray, anynul,
             (signed char *) buffer, status);
-        else if (tiledatatype == TBYTE)
+        else if (tiledatatype == TBYTEX)
           fffi1s1((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            *(signed char *) nulval, bnullarray, anynul,
             (signed char *) buffer, status);
@@ -6753,7 +6753,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2u2((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            *(unsigned short *) nulval, bnullarray, anynul,
             (unsigned short *) buffer, status);
-        else if (tiledatatype == TBYTE)
+        else if (tiledatatype == TBYTEX)
           fffi1u2((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            *(unsigned short *) nulval, bnullarray, anynul,
             (unsigned short *) buffer, status);
@@ -6792,7 +6792,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2uint((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            *(unsigned int *) nulval, bnullarray, anynul,
             (unsigned int *) buffer, status);
-        else if (tiledatatype == TBYTE)
+        else if (tiledatatype == TBYTEX)
           fffi1uint((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            *(unsigned int *) nulval, bnullarray, anynul,
             (unsigned int *) buffer, status);
@@ -6830,7 +6830,7 @@ int imcomp_decompress_tile (fitsfile *in
           fffi2u4((short *)idata, tilelen, bscale, bzero, nullcheck, (short) tnull,
            *(unsigned long *) nulval, bnullarray, anynul, 
             (unsigned long *) buffer, status);
-        else if (tiledatatype == TBYTE)
+        else if (tiledatatype == TBYTEX)
           fffi1u4((unsigned char *)idata, tilelen, bscale, bzero, nullcheck, (unsigned char) tnull,
            *(unsigned long *) nulval, bnullarray, anynul, 
             (unsigned long *) buffer, status);
@@ -8267,7 +8267,7 @@ int fits_compress_table(fitsfile *infptr
 	        if (compalgor[ii] != GZIP_1 && compalgor[ii] != GZIP_2 && compalgor[ii] != RICE_1) {
 			compalgor[ii] = RICE_1;
 		}
-	} else if ( abs(coltype[ii]) == TBYTE ) {
+	} else if ( abs(coltype[ii]) == TBYTEX ) {
 	        if (compalgor[ii] != GZIP_1 && compalgor[ii] != RICE_1 ) {
 			compalgor[ii] = GZIP_1;
 		}
@@ -8465,7 +8465,7 @@ int fits_compress_table(fitsfile *infptr
 #endif
 			  dlen = fits_rcomp ((int *)(vlamem), (int) vlalen, (unsigned char *) cvlamem,
                            (int) compmemlen, 32);
-		        } else if (-coltype[ii] == TBYTE) {
+		        } else if (-coltype[ii] == TBYTEX) {
 			  dlen = fits_rcomp_byte ((signed char *)(vlamem), (int) vlalen, (unsigned char *) cvlamem,
                            (int) compmemlen, 32);
 		        } else {
@@ -8507,17 +8507,17 @@ int fits_compress_table(fitsfile *infptr
 		    /* the uncompressed array */
 		    fits_set_tscale(outfptr, ii + 1, 1.0, 0.0, status);  /* turn off any data scaling, first */
 		    if (dlen < vlamemlen) {
-		        fits_write_col(outfptr, TBYTE, ii + 1, ll+1, 1, dlen, cvlamem, status);
+		        fits_write_col(outfptr, TBYTEX, ii + 1, ll+1, 1, dlen, cvlamem, status);
 		        compressed_size += dlen;  /* total size of the compressed VLAs */
 		        tot_compressed_size += dlen;  /* total size of the compressed file */
 		    } else {
-			if ( -coltype[ii] != TBYTE && compalgor[ii] != GZIP_1) {
+			if ( -coltype[ii] != TBYTEX && compalgor[ii] != GZIP_1) {
 			    /* it is probably faster to reread the raw bytes, rather than unshuffle or unswap them */
 			    bytepos = (infptr->Fptr)->datastart + (infptr->Fptr)->heapstart + vlastart;
 			    ffmbyt(infptr, bytepos, REPORT_EOF, status);
 			    ffgbyt(infptr, vlamemlen, vlamem, status);  /* read the bytes */
 			}
-		        fits_write_col(outfptr, TBYTE, ii + 1, ll+1, 1, vlamemlen, vlamem, status);
+		        fits_write_col(outfptr, TBYTEX, ii + 1, ll+1, 1, vlamemlen, vlamem, status);
 		        compressed_size += vlamemlen;  /* total size of the compressed VLAs */
 		        tot_compressed_size += vlamemlen;  /* total size of the compressed file */
 		    }
@@ -8565,7 +8565,7 @@ int fits_compress_table(fitsfile *infptr
 		/* write the compressed descriptors to the output column */
 		fits_set_tscale(outfptr, ii + 1, 1.0, 0.0, status);  /* turn off any data scaling, first */
 		fits_write_descript(outfptr, ii+1, ll+1, 0, 0, status); /* First, reset the descriptor */
-		fits_write_col(outfptr, TBYTE, ii + 1, ll+1, 1, dlen, cvlamem, status);
+		fits_write_col(outfptr, TBYTEX, ii + 1, ll+1, 1, dlen, cvlamem, status);
 		free(cvlamem); 
 
 		if (ll == 0) {  /* only write the ZCTYPn keyword once, while processing the first column */
@@ -8617,7 +8617,7 @@ int fits_compress_table(fitsfile *infptr
    	            dlen = fits_rcomp ((int *)(cm_buffer + cm_colstart[ii]), datasize / 4, (unsigned char *) cvlamem,
                        datasize * 2, 32);
 
-	        } else if (coltype[ii] == TBYTE) {
+	        } else if (coltype[ii] == TBYTEX) {
 
   	            dlen = fits_rcomp_byte ((signed char *)(cm_buffer + cm_colstart[ii]), datasize, (unsigned char *) cvlamem,
                        datasize * 2, 32);
@@ -8650,7 +8650,7 @@ int fits_compress_table(fitsfile *infptr
 
 	    /* write the compressed data to the output column */
 	    fits_set_tscale(outfptr, ii + 1, 1.0, 0.0, status);  /* turn off any data scaling, first */
-	    fits_write_col(outfptr, TBYTE, ii + 1, ll+1, 1, dlen, cvlamem, status);
+	    fits_write_col(outfptr, TBYTEX, ii + 1, ll+1, 1, dlen, cvlamem, status);
 	    tot_compressed_size += dlen;
 
 	    free(cvlamem);   /* don't need the compressed data any more */
@@ -9276,7 +9276,7 @@ int fits_uncompress_table(fitsfile *infp
 #if BYTESWAPPED
 				   ffswap4((int *) uncompressed_vla, (long) vlalen); 
 #endif
- 				} else if (-coltype[ii] == TBYTE) {
+ 				} else if (-coltype[ii] == TBYTEX) {
 				    dlen = fits_rdecomp_byte((unsigned char *) compressed_vla, (int) cvlalen, (unsigned char *) uncompressed_vla, 
 					(int) vlalen, 32);
 				} else {
--- origsrc/cfitsio-4.0.0/imcopy.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/imcopy.c	2019-09-07 16:02:22.422961100 +0200
@@ -162,7 +162,7 @@ int main(int argc, char *argv[])
 	  
           switch(bitpix) {
               case BYTE_IMG:
-                  datatype = TBYTE;
+                  datatype = TBYTEX;
                   break;
               case SHORT_IMG:
                   datatype = TSHORT;
--- origsrc/cfitsio-4.0.0/modkey.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/modkey.c	2019-09-07 16:02:22.469828100 +0200
@@ -30,7 +30,7 @@ int ffuky( fitsfile *fptr,     /* I - FI
     {
         ffukys(fptr, keyname, (char *) value, comm, status);
     }
-    else if (datatype == TBYTE)
+    else if (datatype == TBYTEX)
     {
         ffukyj(fptr, keyname, (LONGLONG) *(unsigned char *) value, comm, status);
     }
--- origsrc/cfitsio-4.0.0/putcol.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcol.c	2019-09-07 16:02:22.485457500 +0200
@@ -46,7 +46,7 @@
     }
     firstelem++;
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       ffpprb(fptr, group, firstelem, nelem, (unsigned char *) array, status);
     }
@@ -136,7 +136,7 @@
     }
     firstelem++;
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       ffpprb(fptr, group, firstelem, nelem, (unsigned char *) array, status);
     }
@@ -233,7 +233,7 @@
     }
     firstelem++;
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       ffppnb(fptr, group, firstelem, nelem, (unsigned char *) array, 
              *(unsigned char *) nulval, status);
@@ -341,7 +341,7 @@
     }
     firstelem++;
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       ffppnb(fptr, group, firstelem, nelem, (unsigned char *) array, 
              *(unsigned char *) nulval, status);
@@ -426,7 +426,7 @@
     if (*status > 0)           /* inherit input status value if > 0 */
         return(*status);
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       ffpprb(fptr, group, firstelem, nelem, (unsigned char *) array, status);
     }
@@ -507,7 +507,7 @@
         return(*status);
     }
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       ffppnb(fptr, group, firstelem, nelem, (unsigned char *) array, 
              *(unsigned char *) nulval, status);
@@ -599,7 +599,7 @@
     ffgidm(fptr, &naxis, status);
     ffgisz(fptr, 9, naxes, status);
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
         ffpssb(fptr, 1, naxis, naxes, blc, trc,
                (unsigned char *) array, status);
@@ -689,7 +689,7 @@
       ffpclx(fptr, colnum, firstrow, (long) firstelem, (long) nelem, (char *) array, 
              status);
     }
-    else if (datatype == TBYTE)
+    else if (datatype == TBYTEX)
     {
       ffpclb(fptr, colnum, firstrow, firstelem, nelem, (unsigned char *) array,
              status);
@@ -802,7 +802,7 @@
         return(*status);
     }
 
-    if (datatype == TBYTE)
+    if (datatype == TBYTEX)
     {
       ffpcnb(fptr, colnum, firstrow, firstelem, nelem, (unsigned char *) array,
             *(unsigned char *) nulval, status);
@@ -1143,7 +1143,7 @@
             type*=-1;
         }
 
-        if (type != 0      && type != TBYTE  &&
+        if (type != 0      && type != TBYTEX  &&
             type != TSBYTE && type != TLOGICAL && type != TSTRING &&
             type != TSHORT && type != TINT     && type != TLONG && 
             type != TFLOAT && type != TDOUBLE  && type != TCOMPLEX &&
@@ -1353,7 +1353,7 @@
            fits_get_img_type(cols[jj].fptr, &bitpix, status);
            switch(bitpix) {
              case BYTE_IMG:
-                 typecode = TBYTE;
+                 typecode = TBYTEX;
                  break;
              case SHORT_IMG:
                  typecode = TSHORT;
@@ -1407,7 +1407,7 @@
         /* Special case: interprete 'X' column as 'B' */
         if (abs(typecode) == TBIT)
         {
-            typecode  = typecode / TBIT * TBYTE;
+            typecode  = typecode / TBIT * TBYTEX;
             rept = (rept + 7) / 8;
         }
 
@@ -1430,7 +1430,7 @@
                cols[jj].repeat = 0;
 
             /* get the BLANK keyword value, if it exists */
-            if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+            if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                 || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
             {
                 tstatus = 0;
@@ -1459,7 +1459,7 @@
             cols[jj].repeat = rept;
 
             /* get the TNULL keyword value, if it exists */
-            if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+            if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                 || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
             {
                 tstatus = 0;
@@ -1516,11 +1516,11 @@
 
         switch (cols[jj].datatype)
         {
-         case TBYTE:
+         case TBYTEX:
           cols[jj].array = calloc(ntodo + 1, sizeof(char));
           col[jj].nullsize  = sizeof(char);  /* number of bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
               || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               tnull = minvalue(tnull, 255);
@@ -1537,7 +1537,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(char));
           col[jj].nullsize  = sizeof(char);  /* number of bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
               || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               tnull = minvalue(tnull, 127);
@@ -1554,7 +1554,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(short));
           col[jj].nullsize  = sizeof(short);  /* number of bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
               || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               tnull = minvalue(tnull, SHRT_MAX);
@@ -1571,7 +1571,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(unsigned short));
           col[jj].nullsize  = sizeof(unsigned short);  /* bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               tnull = minvalue(tnull, (long) USHRT_MAX);
@@ -1588,7 +1588,7 @@
           cols[jj].array = calloc(sizeof(int), ntodo + 1);
           col[jj].nullsize  = sizeof(int);  /* number of bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               tnull = minvalue(tnull, INT_MAX);
@@ -1605,7 +1605,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(unsigned int));
           col[jj].nullsize  = sizeof(unsigned int);  /* bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               tnull = minvalue(tnull, INT32_MAX);
@@ -1622,7 +1622,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(long));
           col[jj].nullsize  = sizeof(long);  /* number of bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               col[jj].null.longnull = tnull;
@@ -1637,7 +1637,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(unsigned long));
           col[jj].nullsize  = sizeof(unsigned long);  /* bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               if (tnull < 0)  /* can't use a negative null value */
@@ -1655,7 +1655,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(float));
           col[jj].nullsize  = sizeof(float);  /* number of bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               col[jj].null.floatnull = (float) tnull;
@@ -1676,7 +1676,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(double));
           col[jj].nullsize  = sizeof(double);  /* number of bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG
                || abs(typecode) == TINT || abs(typecode) == TLONGLONG)
           {
               col[jj].null.doublenull = (double) tnull;
@@ -1746,7 +1746,7 @@
           cols[jj].array = calloc(ntodo + 1, sizeof(LONGLONG));
           col[jj].nullsize  = sizeof(LONGLONG);  /* number of bytes per value */
 
-          if (abs(typecode) == TBYTE || abs(typecode) == TSHORT || abs(typecode) == TLONG ||
+          if (abs(typecode) == TBYTEX || abs(typecode) == TSHORT || abs(typecode) == TLONG ||
 	      abs(typecode) == TLONGLONG || abs(typecode) == TINT)
           {
               col[jj].null.longlongnull = tnull;

--- origsrc/cfitsio-4.0.0/putcolb.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcolb.c	2019-09-07 16:02:22.485457500 +0200
@@ -37,7 +37,7 @@ int ffpprb( fitsfile *fptr,  /* I - FITS
     {
         /* this is a compressed image in a binary table */
 
-        fits_write_compressed_pixels(fptr, TBYTE, firstelem, nelem,
+        fits_write_compressed_pixels(fptr, TBYTEX, firstelem, nelem,
             0, array, &nullvalue, status);
         return(*status);
     }
@@ -79,7 +79,7 @@ int ffppnb( fitsfile *fptr,  /* I - FITS
 
 
         nullvalue = nulval;  /* set local variable */
-        fits_write_compressed_pixels(fptr, TBYTE, firstelem, nelem,
+        fits_write_compressed_pixels(fptr, TBYTEX, firstelem, nelem,
             1, array, &nullvalue, status);
         return(*status);
     }
@@ -143,7 +143,7 @@ int ffp3db(fitsfile *fptr,   /* I - FITS
         lpixel[1] = (long) nrows;
         lpixel[2] = (long) naxis3;
        
-        fits_write_compressed_img(fptr, TBYTE, fpixel, lpixel,
+        fits_write_compressed_img(fptr, TBYTEX, fpixel, lpixel,
             0,  array, NULL, status);
     
         return(*status);
@@ -214,7 +214,7 @@ int ffpssb(fitsfile *fptr,   /* I - FITS
     {
         /* this is a compressed image in a binary table */
 
-        fits_write_compressed_img(fptr, TBYTE, fpixel, lpixel,
+        fits_write_compressed_img(fptr, TBYTEX, fpixel, lpixel,
             0,  array, NULL, status);
     
         return(*status);
@@ -402,7 +402,7 @@ int ffpclb( fitsfile *fptr,  /* I - FITS
       we must convert the raw values into the scaled and/or machine dependent
       format in a temporary buffer that has been allocated for this purpose.
     */
-    if (scale == 1. && zero == 0. && tcode == TBYTE)
+    if (scale == 1. && zero == 0. && tcode == TBYTEX)
     {
         writeraw = 1;
         if (nelem < (LONGLONG)INT32_MAX) {
@@ -438,7 +438,7 @@ int ffpclb( fitsfile *fptr,  /* I - FITS
 
         switch (tcode) 
         {
-            case (TBYTE):
+            case (TBYTEX):
               if (writeraw)
               {
                 /* write raw input bytes without conversion */
--- origsrc/cfitsio-4.0.0/putcold.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcold.c	2019-09-07 16:02:22.485457500 +0200
@@ -452,7 +452,7 @@ int ffpcld( fitsfile *fptr,  /* I - FITS
                 ffpi8b(fptr, ntodo, incre, (long *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffr8fi1(&array[next], ntodo, scale, zero, 
                         (unsigned char *) buffer, status);
--- origsrc/cfitsio-4.0.0/putcole.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcole.c	2019-09-07 16:02:22.501067200 +0200
@@ -466,7 +466,7 @@ int ffpcle( fitsfile *fptr,  /* I - FITS
                 ffpi8b(fptr, ntodo, incre, (long *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffr4fi1(&array[next], ntodo, scale, zero, 
                         (unsigned char *) buffer, status);
--- origsrc/cfitsio-4.0.0/putcoli.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcoli.c	2019-09-07 16:02:22.501067200 +0200
@@ -453,7 +453,7 @@ int ffpcli( fitsfile *fptr,  /* I - FITS
                 ffpi8b(fptr, ntodo, incre, (long *) buffer, status);
                 break;
 
-             case (TBYTE):
+             case (TBYTEX):
 
                 ffi2fi1(&array[next], ntodo, scale, zero,
                         (unsigned char *) buffer, status);
--- origsrc/cfitsio-4.0.0/putcolj.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcolj.c	2019-09-07 16:02:22.516704100 +0200
@@ -453,7 +453,7 @@ int ffpclj( fitsfile *fptr,  /* I - FITS
                 ffpi8b(fptr, ntodo, incre, (long *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffi4fi1(&array[next], ntodo, scale, zero,
                         (unsigned char *) buffer, status);
@@ -1453,7 +1453,7 @@ int ffpcljj(fitsfile *fptr,  /* I - FITS
                 ffpi4b(fptr, ntodo, incre, (INT32BIT *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffi8fi1(&array[next], ntodo, scale, zero,
                         (unsigned char *) buffer, status);
--- origsrc/cfitsio-4.0.0/putcolk.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcolk.c	2019-09-07 16:02:22.516704100 +0200
@@ -467,7 +467,7 @@ int ffpclk( fitsfile *fptr,  /* I - FITS
                 ffpi8b(fptr, ntodo, incre, (long *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffintfi1(&array[next], ntodo, scale, zero,
                         (unsigned char *) buffer, status);
--- origsrc/cfitsio-4.0.0/putcoll.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcoll.c	2019-09-07 16:02:22.516704100 +0200
@@ -276,7 +276,7 @@ int ffpclx( fitsfile *fptr,  /* I - FITS
 
     tcode = colptr->tdatatype;
 
-    if (abs(tcode) > TBYTE)
+    if (abs(tcode) > TBYTEX)
         return(*status = NOT_LOGICAL_COL); /* not correct datatype column */
 
     if (tcode > 0)
--- origsrc/cfitsio-4.0.0/putcolsb.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcolsb.c	2019-09-07 16:02:22.532315100 +0200
@@ -407,7 +407,7 @@ int ffpclsb( fitsfile *fptr,  /* I - FIT
 
         switch (tcode) 
         {
-            case (TBYTE):
+            case (TBYTEX):
 
                 /* convert the raw data before writing to FITS file */
                 ffs1fi1(&array[next], ntodo, scale, zero,
--- origsrc/cfitsio-4.0.0/putcolu.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcolu.c	2019-09-07 16:02:22.532315100 +0200
@@ -170,7 +170,7 @@ int ffpclu( fitsfile *fptr,  /* I - FITS
 
       strncpy(cstring, snull, leng);  /* copy null string to temp buffer */
     }
-    else if ( tcode == TBYTE  ||
+    else if ( tcode == TBYTEX  ||
               tcode == TSHORT ||
               tcode == TLONG  ||
               tcode == TLONGLONG) 
@@ -182,7 +182,7 @@ int ffpclu( fitsfile *fptr,  /* I - FITS
         return(*status = NO_NULL);
       }
 
-      if (tcode == TBYTE)
+      if (tcode == TBYTEX)
          i1null = (unsigned char) tnull;
       else if (tcode == TSHORT)
       {
@@ -228,7 +228,7 @@ int ffpclu( fitsfile *fptr,  /* I - FITS
 
         switch (tcode) 
         {
-            case (TBYTE):
+            case (TBYTEX):
  
                 for (ii = 0; ii < ntodo; ii++)
                   ffpbyt(fptr, 1,  &i1null, status);
@@ -412,7 +412,7 @@ int ffpcluc( fitsfile *fptr,  /* I - FIT
       strncpy(cstring, snull, leng);  /* copy null string to temp buffer */
 
     }
-    else if ( tcode == TBYTE  ||
+    else if ( tcode == TBYTEX  ||
               tcode == TSHORT ||
               tcode == TLONG  ||
               tcode == TLONGLONG) 
@@ -424,7 +424,7 @@ int ffpcluc( fitsfile *fptr,  /* I - FIT
         return(*status = NO_NULL);
       }
 
-      if (tcode == TBYTE)
+      if (tcode == TBYTEX)
          i1null = (unsigned char) tnull;
       else if (tcode == TSHORT)
       {
@@ -470,7 +470,7 @@ int ffpcluc( fitsfile *fptr,  /* I - FIT
 
         switch (tcode) 
         {
-            case (TBYTE):
+            case (TBYTEX):
  
                 for (ii = 0; ii < ntodo; ii++)
                   ffpbyt(fptr, 1,  &i1null, status);
--- origsrc/cfitsio-4.0.0/putcolui.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcolui.c	2019-09-07 16:02:22.547939100 +0200
@@ -422,7 +422,7 @@ int ffpclui( fitsfile *fptr,  /* I - FIT
                 ffpi8b(fptr, ntodo, incre, (long *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffu2fi1(&array[next], ntodo, scale, zero,
                         (unsigned char *) buffer, status);
--- origsrc/cfitsio-4.0.0/putcoluj.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcoluj.c	2019-09-07 16:02:22.547939100 +0200
@@ -422,7 +422,7 @@ int ffpcluj( fitsfile *fptr,  /* I - FIT
                 ffpi8b(fptr, ntodo, incre, (long *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffu4fi1(&array[next], ntodo, scale, zero,
                         (unsigned char *) buffer, status);
@@ -1398,7 +1398,7 @@ int ffpclujj( fitsfile *fptr,  /* I - FI
                 ffpi4b(fptr, ntodo, incre, (INT32BIT *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffu8fi1(&array[next], ntodo, scale, zero,
                         (unsigned char *) buffer, status);
--- origsrc/cfitsio-4.0.0/putcoluk.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putcoluk.c	2019-09-07 16:02:22.563562400 +0200
@@ -437,7 +437,7 @@ int ffpcluk(fitsfile *fptr,  /* I - FITS
                 ffpi8b(fptr, ntodo, incre, (long *) buffer, status);
                 break;
 
-            case (TBYTE):
+            case (TBYTEX):
  
                 ffuintfi1(&array[next], ntodo, scale, zero,
                         (unsigned char *) buffer, status);
--- origsrc/cfitsio-4.0.0/putkey.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/putkey.c	2019-09-07 16:02:22.579199800 +0200
@@ -205,7 +205,7 @@ int ffpky( fitsfile *fptr,     /* I - FI
     {
         ffpkys(fptr, keyname, (char *) value, comm, status);
     }
-    else if (datatype == TBYTE)
+    else if (datatype == TBYTEX)
     {
         ffpkyj(fptr, keyname, (LONGLONG) *(unsigned char *) value, comm, status);
     }
@@ -2730,7 +2730,7 @@ int ffphbn(fitsfile *fptr,  /* I - FITS
         }
         else if (datatype == TBIT)
            strcat(comm, ": BIT");
-        else if (datatype == TBYTE)
+        else if (datatype == TBYTEX)
            strcat(comm, ": BYTE");
         else if (datatype == TLOGICAL)
            strcat(comm, ": 1-byte LOGICAL");
--- origsrc/cfitsio-4.0.0/testprog.c	2019-05-08 17:34:50.000000000 +0200
+++ src/cfitsio-4.0.0/testprog.c	2019-09-07 16:02:22.626071400 +0200
@@ -292,8 +292,8 @@ int main()
         &status) > 0)
         printf("ffpky status = %d\n", status);
 
-    cval = TBYTE;
-    if (ffpky(fptr, TBYTE, "tbyte", &cval, "tbyte comment", &status) > 0)
+    cval = TBYTEX;
+    if (ffpky(fptr, TBYTEX, "tbyte", &cval, "tbyte comment", &status) > 0)
         printf("ffpky status = %d\n", status);
 
     oshtkey = TSHORT;
@@ -351,7 +351,7 @@ int main()
 /*  test the newer ffpx routine, instead of the older ffppr_ routines */
     firstpix[0]=1;
     firstpix[1]=1;
-    ffppx(fptr, TBYTE, firstpix, 2, &boutarray[0],  &status);
+    ffppx(fptr, TBYTEX, firstpix, 2, &boutarray[0],  &status);
     firstpix[0]=5;
     ffppx(fptr, TSHORT, firstpix, 2, &ioutarray[4],  &status);
     firstpix[0]=9;
@@ -372,7 +372,7 @@ int main()
     firstpix[0]=3;
     firstpix[1]=1;
     bnul = 4;
-    ffppxn(fptr, TBYTE, firstpix, 2, &boutarray[2], &bnul,  &status);
+    ffppxn(fptr, TBYTEX, firstpix, 2, &boutarray[2], &bnul,  &status);
     firstpix[0]=7;
     inul = 8;
     ffppxn(fptr, TSHORT, firstpix, 2, &ioutarray[6], &inul, &status);
@@ -651,7 +651,7 @@ int main()
     ffgky(fptr, TLOGICAL, "key_pkyl", &ilkey, comment, &status);
     printf("KEY_PKY L %d %s %d\n", ilkey, comment, status);
 
-    ffgky(fptr, TBYTE, "KEY_PKYJ", &cval, comment, &status);
+    ffgky(fptr, TBYTEX, "KEY_PKYJ", &cval, comment, &status);
     printf("KEY_PKY BYTE %d %s %d\n",cval, comment, status);
 
     ffgky(fptr, TSHORT, "KEY_PKYJ", &ishtkey, comment, &status);
@@ -2239,7 +2239,7 @@ int main()
     }
 
     /* write a few pixels with each datatype */
-    ffppr(fptr, TBYTE,   1,  2, &boutarray[0],  &status);
+    ffppr(fptr, TBYTEX,   1,  2, &boutarray[0],  &status);
     ffppr(fptr, TSHORT,  3,  2, &ioutarray[2],  &status);
     ffppr(fptr, TINT,    5,  2, &koutarray[4],  &status);
     ffppr(fptr, TSHORT,  7,  2, &ioutarray[6],  &status);
@@ -2256,7 +2256,7 @@ int main()
     enul = 0.;
     dnul = 0.;
 
-    ffgpv(fptr, TBYTE,   1,  14, &bnul, binarray, &anynull, &status);
+    ffgpv(fptr, TBYTEX,   1,  14, &bnul, binarray, &anynull, &status);
     ffgpv(fptr, TSHORT,  1,  14, &inul, iinarray, &anynull, &status);
     ffgpv(fptr, TINT,    1,  14, &knul, kinarray, &anynull, &status);
     ffgpv(fptr, TLONG,   1,  14, &jnul, jinarray, &anynull, &status);
@@ -2391,7 +2391,7 @@ int main()
 
     for (ii = 2; ii < 6; ii++)   /* loop over cols 2 - 5 */
     {
-        ffpcl(fptr, TBYTE,   ii, 1, 1, 2, boutarray,     &status); 
+        ffpcl(fptr, TBYTEX,   ii, 1, 1, 2, boutarray,     &status); 
         ffpcl(fptr, TSHORT,  ii, 3, 1, 2, &ioutarray[2], &status);  
         ffpcl(fptr, TLONG,   ii, 5, 1, 2, &joutarray[4], &status);  
         ffpcl(fptr, TFLOAT,  ii, 7, 1, 2, &eoutarray[6], &status);
@@ -2400,7 +2400,7 @@ int main()
     printf("ffpcl status = %d\n", status);
 
     /* read back the pixels with each datatype */
-    ffgcv(fptr, TBYTE,   2, 1, 1, 10, &bnul, binarray, &anynull, &status);
+    ffgcv(fptr, TBYTEX,   2, 1, 1, 10, &bnul, binarray, &anynull, &status);
     ffgcv(fptr, TSHORT,  2, 1, 1, 10, &inul, iinarray, &anynull, &status);
     ffgcv(fptr, TINT,    3, 1, 1, 10, &knul, kinarray, &anynull, &status);
     ffgcv(fptr, TLONG,   3, 1, 1, 10, &jnul, jinarray, &anynull, &status);
