post_install() {
  #dkms install -m netatop -v "$2" --no-depmod
  dkms autoinstall --no-depmod
  depmod -a
  modprobe netatop
}
post_upgrade() {
  post_install "$@"
}
pre_remove() {
  rmmod netatop   || true
  dkms remove -m netatop -v "$2" --all || true
  depmod -a
}
