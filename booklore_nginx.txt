server {
    listen 8081;
    server_name localhost;

    root /usr/share/webapps/booklore/booklore-ui/;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://localhost:8095/api/;
    }
}
