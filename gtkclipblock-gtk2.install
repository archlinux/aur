post_install() {
  echo "Make sure to:"
  echo "  1. add /usr/lib/libgtkclipblock-gtk2.so to /etc/ld.so.preload (or use LD_PRELOAD)"
  echo "  2. add GTKCLIPBLOCK_HOOK=1 to /etc/environment or ~/.config/environment.d/gtkclipblock.conf"
}

post_upgrade() {
  post_install
}
