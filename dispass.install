post_install() {
    if [ -x usr/bin/xdg-icon-resource ]; then
        xdg-icon-resource forceupdate --theme hicolor > /dev/null 2>&1
    fi
}

post_upgrade() {
    post_install "$1"
}

post_remove() {
    post_install "$1"
}
