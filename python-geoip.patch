diff -ura cvmfs-2.3.5.orig/externals/python-geoip/src/makeHook.sh cvmfs-2.3.5.new/externals/python-geoip/src/makeHook.sh
--- cvmfs-2.3.5.orig/externals/python-geoip/src/makeHook.sh	2017-03-23 09:16:22.000000000 +0100
+++ cvmfs-2.3.5.new/externals/python-geoip/src/makeHook.sh	2017-04-07 13:33:39.303875903 +0200
@@ -13,7 +13,8 @@
   (cd .tmp; unzip -o *.egg GeoIP.so)
 elif [ -d .tmp/*.egg ]; then
   # for some reason SLC6 produces an uncompressed *.egg directory containing GeoIP.so
-  cp .tmp/*.egg/GeoIP.so .tmp
+  # recently on ArchLinux does the same, additionally using GeoIP.<system tag>.so
+  cp .tmp/*.egg/GeoIP.*.so .tmp/GeoIP.so
 elif [ -f .tmp/GeoIP.*.so ]; then
   # on ArchLinux no *.egg is created but a GeoIP.<system tag>.so file
   cp .tmp/GeoIP.*.so .tmp/GeoIP.so
