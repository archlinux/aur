post_install() {
  cat <<'EOF'
xdg-desktop-portal-omarchy installed.

Each user should run once:

  xdg-desktop-portal-omarchy-setup

Dialogs use the built-in egui picker.
ScreenCast capture stays on xdg-desktop-portal-hyprland.
EOF
}

post_upgrade() {
  post_install
}
