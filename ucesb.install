post_install() {
	version=$1
	echo "installdir=${installdir}"
	echo "pwd=$(pwd)"
	echo "pkgname=${pkgname%-git}"
	cd "${installdir}/${pkgname%-git}"
	echo "pwd=$(pwd)"
	make -j$(nproc) empty
	make -j$(nproc) xtst
}
