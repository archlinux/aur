post_upgrade() {
    if [[ "$(vercmp "$1" 2.0.0)" -lt 0 ]] && [[ "$(vercmp "$2" 2.0.0)" -gt 0 ]]
    then
        echo "vnt is upgraded to a version that has breaking changes."
        echo "Please check upstream release log to migrate from legacy version."
    fi
}
