diff -ura a/sound/pci/hdsp/hdspe/hdspe_core.c b/sound/pci/hdsp/hdspe/hdspe_core.c
--- a/sound/pci/hdsp/hdspe/hdspe_core.c	2024-09-14 00:45:37.818364231 +0200
+++ b/sound/pci/hdsp/hdspe/hdspe_core.c	2024-09-14 00:48:25.913218159 +0200
@@ -422,9 +422,9 @@
 	if (err < 0)
 		return err;
 
-	err = pci_set_dma_mask(pci, DMA_BIT_MASK(32));
+	err = dma_set_mask(&pci->dev, DMA_BIT_MASK(32));
 	if (!err)
-		err = pci_set_consistent_dma_mask(pci, DMA_BIT_MASK(32));
+		err = dma_set_coherent_mask(&pci->dev, DMA_BIT_MASK(32));
 	if (err != 0) {
 		dev_err(card->dev, "No suitable DMA addressing support.\n");
 		return -ENODEV;
