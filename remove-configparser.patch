--- dvc-0.80.0/setup.py
+++ dvc-0.80.0/setup.py

Description of configparser:  Updated configparser from Python 3.7 for Python 2.6+.
dvc is python3, so no need for the backport. Especially since the module must be
imported from backports, which isn't done.
Script where it is used works fine as it.

@@ -46,7 +46,6 @@
 install_requires = [
     "python-dateutil<2.8.1,>=2.1",  # Consolidates azure-blob-storage and boto3
     "ply>=3.9",  # See https://github.com/pyinstaller/pyinstaller/issues/1945
-    "configparser>=3.5.0",
     "colorama>=0.3.9",
     "configobj>=5.0.6",
     "gitpython>=2.1.8",

