post_install() {
  cat << EOF
To start and enable the service on boot:
  systemctl enable --now deeplx.service

The default listen address should be http://localhost:1188
EOF
}

post_upgrade() {
  post_install
}
