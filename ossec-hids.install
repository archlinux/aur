post_install(){
 cat <<INFO

To complete the installation process, create a new OSSEC agent

    sudo -u ossec /var/lib/ossec-hids/bin/manage_agents -a 127.0.0.1 -n localagent
    sudo -u ossec cat /var/lib/ossec-hids/etc/client.keys
    
Now, start the server

    sudo systemctl start ossec-server.target
    
Then, start the agent

    sudo systemctl start ossec-agent.target

INFO
}
    
    
