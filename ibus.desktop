[Desktop Entry]
Name=IBus
Type=Application
Exec= if [ "$XDG_SESSION_TYPE" != "wayland" ]; then         ibus-daemon -drx; else if [ "$XDG_CURRENT_DESKTOP" != "KDE" ]; then         ibus-daemon -drx; fi; fi
Terminal=false
