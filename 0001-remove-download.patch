From 7643cc36597ff996453027c02680c4232b35dde9 Mon Sep 17 00:00:00 2001
From: Brli <brli@chakralinux.org>
Date: Sat, 6 Sep 2025 00:53:20 +0800
Subject: [PATCH] remove download

---
 src/merge/merge_dictionaries.py | 55 ++++++++++-----------------------
 1 file changed, 16 insertions(+), 39 deletions(-)

diff --git a/src/merge/merge_dictionaries.py b/src/merge/merge_dictionaries.py
index 1cdbf37..7da1d8b 100644
--- a/src/merge/merge_dictionaries.py
+++ b/src/merge/merge_dictionaries.py
@@ -6,12 +6,9 @@
 
 import bz2
 import html
-import os
-import subprocess
 import sys
-import urllib.request
 from unicodedata import normalize
-from zipfile import ZipFile
+from pathlib import Path
 
 
 def main():
@@ -50,37 +47,21 @@ def get_ut_dic(file_ut):
 
 
 def get_mozc_dic():
-    # Mozc の最終コミット日を取得
-    url = 'https://github.com/google/mozc/commits/master/'
-
-    with urllib.request.urlopen(url) as response:
-        date = response.read().decode()
-        date = date.split('"committedDate":"')[1]
-        date = date[:10]
-        date = date.replace('-', '')
-
-    # Mozc のアーカイブが古い場合は取得
-    url = 'https://github.com/google/mozc/archive/refs/heads/master.zip'
-
-    if os.path.exists(f'mozc-{date}.zip') is False:
-        urllib.request.urlretrieve(
-                url, f'mozc-{date}.zip')
-
-    with ZipFile(f'mozc-{date}.zip') as zip_ref:
-        # 一般名詞のIDを取得
-        with zip_ref.open(
-                'mozc-master/src/data/dictionary_oss/id.def') as file:
-            id_mozc = file.read().decode()
-            id_mozc = id_mozc.split(' 名詞,一般,')[0].split('\n')[-1]
-
-        # Mozc 公式辞書を取得
-        mozc_dic = []
-
-        for i in range(10):
-            with zip_ref.open(
-                    'mozc-master/src/data/dictionary_oss/' +
-                    f'dictionary0{i}.txt') as file:
-                mozc_dic += file.read().decode().splitlines()
+
+    zip_ref = Path.cwd()
+    id = zip_ref / 'mozc-master/src/data/dictionary_oss/id.def'
+
+    # 一般名詞のIDを取得
+    with id.open() as file:
+        id_mozc = file.read()
+        id_mozc = id_mozc.split(' 名詞,一般,')[0].split('\n')[-1]
+
+    # Mozc 公式辞書を取得
+    mozc_dic = []
+
+    for dict in Path(zip_ref, '/mozc-master/src/data/dictionary_oss/').glob('dictionary0*.txt'):
+        with dict.open() as file:
+            mozc_dic += file.read().splitlines()
 
     mozc_dic.append(id_mozc)
     return (mozc_dic)
@@ -121,10 +102,6 @@ def remove_duplicates(mozc_dic):
 
 
 def count_word_hits():
-    subprocess.run(
-        ['wget', '-N', 'https://dumps.wikimedia.org/jawiki/latest/' +
-            'jawiki-latest-pages-articles-multistream-index.txt.bz2'],
-        check=True)
 
     with bz2.open(
             'jawiki-latest-pages-articles-multistream-index.txt.bz2',
-- 
2.51.0

