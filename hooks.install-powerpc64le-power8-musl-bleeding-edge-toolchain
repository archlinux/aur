post_install() {
	echo "bootlin toolchain installed to /opt/powerpc64le-power8-musl-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/powerpc64le-power8-musl-bleeding-edge.sh"
	echo ""
}
