diff --git a/crates/splinterm-mcp/tests/stdio_protocol.rs b/crates/splinterm-mcp/tests/stdio_protocol.rs
index d6c7f45..709642e 100644
--- a/crates/splinterm-mcp/tests/stdio_protocol.rs
+++ b/crates/splinterm-mcp/tests/stdio_protocol.rs
@@ -4136,24 +4136,28 @@ fn controller_modes_overlay_control_resources_and_clear_on_release() {
         .as_str()
         .unwrap()
         .to_owned();
-    while server
-        .seen()
-        .iter()
-        .filter(|message| message["method"] == "notifications/resources/updated")
-        .count()
-        < 2
-    {
-        assert_eq!(
-            server.receive()["method"],
-            "notifications/resources/updated"
+    let acquired_deadline = Instant::now() + Duration::from_secs(3);
+    let mut read_id = 100;
+    let document = loop {
+        server.send(&request(read_id, "resources/read", json!({"uri":uri})));
+        let read = server.receive_id(read_id);
+        let document: Value =
+            serde_json::from_str(read["result"]["contents"][0]["text"].as_str().unwrap()).unwrap();
+        if document["data"]["locally_owned"] == true
+            && document["data"]["modes"] == json!(["input"])
+        {
+            break document;
+        }
+        assert!(
+            Instant::now() < acquired_deadline,
+            "controller overlay never became visible"
         );
-    }
-    server.send(&request(12, "resources/read", json!({"uri":uri})));
-    let read = server.receive_id(12);
-    let document: Value =
-        serde_json::from_str(read["result"]["contents"][0]["text"].as_str().unwrap()).unwrap();
+        read_id += 1;
+        thread::sleep(Duration::from_millis(10));
+    };
     assert_eq!(document["data"]["locally_owned"], true);
     assert_eq!(document["data"]["modes"], json!(["input"]));
+    read_id += 1;
     server.send(&request(
         13,
         "tools/call",
@@ -4163,22 +4167,22 @@ fn controller_modes_overlay_control_resources_and_clear_on_release() {
         server.receive_id(13)["result"]["structuredContent"]["data"]["released"],
         true
     );
-    while server
-        .seen()
-        .iter()
-        .filter(|message| message["method"] == "notifications/resources/updated")
-        .count()
-        < 4
-    {
-        assert_eq!(
-            server.receive()["method"],
-            "notifications/resources/updated"
+    let released_deadline = Instant::now() + Duration::from_secs(3);
+    let document = loop {
+        server.send(&request(read_id, "resources/read", json!({"uri":uri})));
+        let read = server.receive_id(read_id);
+        let document: Value =
+            serde_json::from_str(read["result"]["contents"][0]["text"].as_str().unwrap()).unwrap();
+        if document["data"]["locally_owned"] == false && document["data"]["modes"] == json!([]) {
+            break document;
+        }
+        assert!(
+            Instant::now() < released_deadline,
+            "controller overlay never cleared"
         );
-    }
-    server.send(&request(14, "resources/read", json!({"uri":uri})));
-    let read = server.receive_id(14);
-    let document: Value =
-        serde_json::from_str(read["result"]["contents"][0]["text"].as_str().unwrap()).unwrap();
+        read_id += 1;
+        thread::sleep(Duration::from_millis(10));
+    };
     assert_eq!(document["data"]["locally_owned"], false);
     assert_eq!(document["data"]["modes"], json!([]));
     assert!(server.seen().iter().any(|message| {
