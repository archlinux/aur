[Unit]
Description=Lidarr Service
After=syslog.target network.target

[Service]
User=lidarr
Group=lidarr
ExecStart=/usr/bin/mono /usr/lib/lidarr/Lidarr.exe -nobrowser -data=/var/lib/lidarr
Type=simple
TimeoutStopSec=20
KillMode=process
Restart=always

[Install]
WantedBy=multi-user.target
