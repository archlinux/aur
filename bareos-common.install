# arg 1:  the old package version
post_remove() {
   # userdel is usually not done on upgrade, and often left on remove to preserve file ownership.
   # Arch guidelines say: do not remove users/groups.
   rm -f /etc/bareos/.rndpwd
}