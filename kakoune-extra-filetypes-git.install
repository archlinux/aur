post_install() {
  cat << 'EOF'

You can now load the extra Kakoune highlighters by appending the following
code to your `kakrc` configuration:

evaluate-commands %sh{
    find "${kak_runtime}"/../kakoune-extra-filetypes/rc -type f -name \*\\.kak -exec printf 'source %s\n' '{}' \;
}

EOF
}
