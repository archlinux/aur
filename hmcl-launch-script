#!/bin/sh

if [[ "$1" != "-p" ]]; then
    WORKDIR=${XDG_CONFIG_HOME:-$HOME/.config}/hmcl
    mkdir -p $WORKDIR
    cd $WORKDIR
fi

exec `command -v java` -jar "/usr/share/java/hmcl-bin/hmcl-clean-bin.jar" "$@"
