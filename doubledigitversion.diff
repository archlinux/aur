diff --git a/pulledpork.pl b/pulledpork.pl
--- a/pulledpork.pl
+++ b/pulledpork.pl
@@ -1512,10 +1512,10 @@ sub snort_version {
     open( FH, "$cmd 2>&1 |" );
     while (<FH>) {
         next unless $_ =~ /Version/;
-        if ( $_ =~ /\d\.\d\.\d\.\d/ ) {
+        if ( $_ =~ /\d+\.\d+\.\d+\.(\d|\d+)/ ) {
             $version = $&;
         }
-        elsif ( $_ =~ /\d\.\d\.\d/ ) {
+        elsif ( $_ =~ /\d+\.\d+\.\d+(?!\.)/ ) {
             $version = $& . ".0";
         }
     }
