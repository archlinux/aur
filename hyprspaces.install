# shellcheck shell=bash

post_install() {
	echo "The hyprspaces AUR package is now a transition package."
	echo "HyprPM owns the plugin and hyprspaces-tools owns the helper CLI/config automation."
	echo "You may remove this transition package after hyprspaces-tools is installed."
}

post_upgrade() {
	post_install
}
