[Unit]
Description=Cyrus IMAP mail server
After=network.target

[Service]
Type=exec
Restart=on-failure
ExecStart=/usr/lib/cyrus/master -p /run/cyrus-master.pid
ExecReload=/usr/bin/kill -HUP $MAINPID

[Install]
WantedBy=multi-user.target
