From: Matthew Gamble <git@matthewgamble.net>
Date: Sat, 16 Apr 2016 12:15:45 +1000
Subject: [PATCH] Remove hard dependency on pip

---
There's some magic in this package that attempts to automatically install
packages from PyPI if they're not already installed. This doesn't work
unless you're running your code with root privileges, so there's no point
in having it here. These packages should instead be installed using
your system's package manager.

Also, we make the assumption that importlib will always exist because
this package is targeting python3 on Arch Linux.

diff -u -r luckydonald-utils-0.43/luckydonaldUtils/dependencies/install.py luckydonald-utils-0.43_mod/luckydonaldUtils/dependencies/install.py
--- luckydonald-utils-0.43/luckydonaldUtils/dependencies/install.py	2016-04-02 16:06:40.000000000 +1100
+++ luckydonald-utils-0.43_mod/luckydonaldUtils/dependencies/install.py	2016-04-16 12:10:45.479951244 +1000
@@ -1,18 +1,11 @@
 # -*- coding: utf-8 -*-
-from .pip_interface import pip_install
 from luckydonaldUtils.logger import logging
 from setuptools import find_packages
 
 __author__ = 'luckydonald'
 logger = logging.getLogger(__name__)
 
-try:
-    import importlib
-except ImportError:
-    # pip install importlib
-    pip_install(["importlib"])
-    import importlib
-# end try
+import importlib
 
 logger = logging.getLogger(__name__)
 
diff -u -r luckydonald-utils-0.43/setup.py luckydonald-utils-0.43_mod/setup.py
--- luckydonald-utils-0.43/setup.py	2016-04-02 16:06:40.000000000 +1100
+++ luckydonald-utils-0.43_mod/setup.py	2016-04-16 12:12:42.922743383 +1000
@@ -12,12 +12,6 @@
 
 main_package = 'luckydonaldUtils'
 
-install_requires = ["pip", "setuptools"]  # both should be installed by default.
-try:
-    import importlib
-except ImportError:
-    install_requires.append('importlib')
-
 setup(
     name="luckydonald-utils",
     packages=find_submodules(main_package),
@@ -28,7 +22,6 @@
     license="GPLv2+",
     keywords="luckydonald utils utilities utility python dependencies dependency download progress bar encoding files interactions json update store text xml time network logger color images webserver django CSRF headers eastereggs",
     url="https://github.com/luckydonald/luckydonald-utils",
-    install_requires=install_requires,
     long_description=long_description,
     # test_suite = 'nose.collector',
     classifiers=[
