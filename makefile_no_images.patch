diff -Naur ukopp-6.5.orig/Makefile ukopp-6.5.new/Makefile
--- ukopp-6.5.orig/Makefile	2017-12-03 23:13:05.419742767 +0100
+++ ukopp-6.5.new/Makefile	2017-12-03 23:19:23.434628772 +0100
@@ -33,26 +33,21 @@
 	$(CXX) $(CFLAGS) zfuncs.cc    \
           -D PREFIX=\"$(PREFIX)\" -D DOCDIR=\"$(DOCDIR)\"
 
-install: $(PROGRAM) uninstall
-	mkdir -p  $(DESTDIR)$(BINDIR)
-	mkdir -p  $(DESTDIR)$(DATADIR)
-	mkdir -p  $(DESTDIR)$(ICONDIR)
-	mkdir -p  $(DESTDIR)$(IMAGEDIR)
-	mkdir -p  $(DESTDIR)$(LOCALESDIR)
-	mkdir -p  $(DESTDIR)$(DOCDIR)
-	mkdir -p  $(DESTDIR)$(MANDIR)
-	mkdir -p  $(DESTDIR)$(PREFIX)/share/applications
-	mkdir -p  $(DESTDIR)$(APPDATADIR)
-	cp -f     $(PROGRAM) $(DESTDIR)$(BINDIR)
+install: $(PROGRAM)
+	install -d  $(DESTDIR)$(BINDIR)
+	install -d  $(DESTDIR)$(DATADIR)
+	install -d  $(DESTDIR)$(ICONDIR)
+	install -d  $(DESTDIR)$(LOCALESDIR)
+	install -d  $(DESTDIR)$(DOCDIR)
+	install -d  $(DESTDIR)$(MANDIR)
+	install -d  $(DESTDIR)$(PREFIX)/share/applications
+	install -d  $(DESTDIR)$(APPDATADIR)
+	install -m755 $(PROGRAM) $(DESTDIR)$(BINDIR)
 	cp -f -R  data/* $(DESTDIR)$(DATADIR)
-	cp -f -R  images/* $(DESTDIR)$(IMAGEDIR)
 	cp -f -R  doc/* $(DESTDIR)$(DOCDIR)
 	gzip -f -9 $(DESTDIR)$(DOCDIR)/changelog
 	# man page
-	cp -f doc/$(PROGRAM).man $(PROGRAM).1
-	gzip -f -9 $(PROGRAM).1
-	cp $(PROGRAM).1.gz $(DESTDIR)$(MANDIR)
-	rm -f $(PROGRAM).1.gz
+	install -Dm644 doc/$(PROGRAM).man $(DESTDIR)$(MANDIR)/$(PROGRAM).1
 	# menu (desktop) file
 	cp -f $(PROGRAM).desktop $(DESTDIR)$(MENUFILE)
 	cp -f $(PROGRAM).png $(DESTDIR)$(ICONDIR)
