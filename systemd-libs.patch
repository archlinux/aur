diff --git a/config.mk b/config.mk
index a7de679..9c65018 100644
--- a/config.mk
+++ b/config.mk
@@ -9,12 +9,12 @@ X11INC = /usr/X11R6/include
 X11LIB = /usr/X11R6/lib
 
 # systemd dbus support (to lock on suspend, for systemd systems)
-#SDBUSLIBS  = -lsystemd
-#SDBUSFLAGS = -DUSE_SYSTEMD
+SDBUSLIBS  = -lsystemd
+SDBUSFLAGS = -DUSE_SYSTEMD
 
 # elogind dbus support (to lock on suspend, for non-systemd systems)
-SDBUSLIBS  = -lelogind
-SDBUSFLAGS = -DUSE_ELOGIND
+#SDBUSLIBS  = -lelogind
+#SDBUSFLAGS = -DUSE_ELOGIND
 
 # includes and libs
 INCS = -I${X11INC}
