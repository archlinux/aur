Use HTTPS in request and result URLs.

diff -ru a/imgurbash.sh b/imgurbash.sh
--- a/imgurbash.sh	2015-12-09 22:10:57.230062478 +0200
+++ b/imgurbash.sh	2015-12-09 22:12:47.272830440 +0200
@@ -76,7 +76,7 @@
 
 	# upload the image
 	response=$(curl -F "key=$apikey" -H "Expect: " -F "image=@$file" \
-		http://imgur.com/api/upload.xml 2>/dev/null)
+		https://imgur.com/api/upload.xml 2>/dev/null)
 	# the "Expect: " header is to get around a problem when using this through 
 	# the Squid proxy. Not sure if it's a Squid bug or what.
 	if [ $? -ne 0 ]; then
@@ -91,8 +91,8 @@
 	fi
 
 	# parse the response and output our stuff
-	url=$(echo $response | sed -r 's/.*<original_image>(.*)<\/original_image>.*/\1/')
-	deleteurl=$(echo $response | sed -r 's/.*<delete_page>(.*)<\/delete_page>.*/\1/')
+	url=$(echo $response | sed -r 's/.*<original_image>(.*)<\/original_image>.*/\1/;s/^http:/https:/')
+	deleteurl=$(echo $response | sed -r 's/.*<delete_page>(.*)<\/delete_page>.*/\1/;s/^http:/https:/')
 	echo $url
 	echo "Delete page: $deleteurl" >&2
 
