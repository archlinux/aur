post_install() {
    echo "==================================== Tips ===================================="
    echo "如果你使用 shitama 来启动游戏，应当选择一个启动「非想天则」的脚本，而不是可执行文件。"
    echo "通常脚本的内容如下："
    echo "    #!/bin/bash"
    echo "    LANG=ja_JP.UTF-8 wine /path/to/th123.exe"
    echo "==================================== spiT ===================================="
}