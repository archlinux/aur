diff --git a/pyproject.toml b/pyproject.toml
index 8f61751..f9dbd04 100644
--- a/pyproject.toml
+++ b/pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=75.6"]
+requires = ["setuptools>=75.2"]
 build-backend = "setuptools.build_meta"
 
 [project]
@@ -9,7 +9,7 @@ authors = [{ name = "Rory Sullivan", email = "codingrory@gmail.com" }]
 description = "A Python interface for MET Norway's Locationforecast 2.0 service."
 readme = "README.md"
 requires-python = ">=3.9"
-dependencies = ["requests>=2.25.1", "tzdata>=2020.5"]
+dependencies = ["requests>=2.25.1"]
 license = { file = "LICENSE" }
 keywords = [
     "met",
