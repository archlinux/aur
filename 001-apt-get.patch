diff --git a/steps/01-install.sh b/steps/01-install.sh
index 4883785..081fc90 100755
--- a/steps/01-install.sh
+++ b/steps/01-install.sh
@@ -37,8 +37,8 @@ case "$TARGET_OS" in
     ;;
 
   linux)
-    sudo apt-get update
-    sudo apt-get install -y cmake pkg-config
+    #sudo apt-get update
+    #sudo apt-get install -y cmake pkg-config
 
     if [ "$TARGET_ENVIRONMENT" == "musl" ]; then
 
@@ -67,7 +67,7 @@ case "$TARGET_OS" in
       [ -d "$MUSL_VERSION" ] || curl -L "$MUSL_URL/$MUSL_VERSION.tgz" | tar xz
       echo "$PWD/$MUSL_VERSION/bin" >> "$PATH_FILE"
 
-      sudo apt-get install -y $PACKAGES
+      #sudo apt-get install -y $PACKAGES
 
     else
 
@@ -81,11 +81,11 @@ case "$TARGET_OS" in
           ;;
 
         x86)
-          sudo apt-get install -y g++-multilib
+          #sudo apt-get install -y g++-multilib
           ;;
 
         x64)
-          sudo apt-get install -y g++
+          #sudo apt-get install -y g++
           ;;
       esac
 
