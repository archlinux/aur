#!/bin/bash

if [ ! -d "$HOME"/.resource-hacker ]; then
    mkdir -p "$HOME"/.resource-hacker
    ln -s /usr/share/resource-hacker/ResourceHacker.exe "$HOME"/.resource-hacker/ResourceHacker.exe
fi

WINEDEBUG=-all wine "$HOME"/.resource-hacker/ResourceHacker.exe "$@"