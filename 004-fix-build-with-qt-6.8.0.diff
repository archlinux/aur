diff -ur a/gpt4all-chat/qml/MySettingsStack.qml b/gpt4all-chat/qml/MySettingsStack.qml
--- a/gpt4all-chat/qml/MySettingsStack.qml	2024-10-12 05:06:32.000000000 +0800
+++ b/gpt4all-chat/qml/MySettingsStack.qml	2024-10-16 18:49:57.600744469 +0800
@@ -2,6 +2,7 @@
 import QtQuick
 import QtQuick.Controls
 import QtQuick.Controls.Basic
+import QtQuick.Controls.impl
 import QtQuick.Layouts
 import QtQuick.Dialogs
 import Qt.labs.folderlistmodel
diff -ur a/gpt4all-chat/src/server.cpp b/gpt4all-chat/src/server.cpp
--- a/gpt4all-chat/src/server.cpp	2024-10-12 05:06:32.000000000 +0800
+++ b/gpt4all-chat/src/server.cpp	2024-10-16 17:30:09.768607619 +0800
@@ -16,6 +16,11 @@
 #include <QHostAddress>
 #include <QHttpServer>
 #include <QHttpServerResponder>
+
+#if QT_VERSION >= QT_VERSION_CHECK(6, 8, 0)
+#include <QTcpServer>
+#endif
+
 #include <QJsonArray>
 #include <QJsonDocument>
 #include <QJsonObject>
@@ -457,10 +462,19 @@
 void Server::start()
 {
     m_server = std::make_unique<QHttpServer>(this);
+#if QT_VERSION < QT_VERSION_CHECK(6, 8, 0)
     if (!m_server->listen(QHostAddress::LocalHost, MySettings::globalInstance()->networkPort())) {
         qWarning() << "ERROR: Unable to start the server";
         return;
     }
+#else
+    auto tcp = std::make_unique<QTcpServer>();
+    if (!tcp->listen(QHostAddress::LocalHost, MySettings::globalInstance()->networkPort()) || !m_server->bind(tcp.get())) {
+        qWarning() << "ERROR: Unable to start the server";
+        return;
+    }
+    tcp.release();
+#endif
 
     m_server->route("/v1/models", QHttpServerRequest::Method::Get,
         [](const QHttpServerRequest &) {
@@ -600,11 +614,23 @@
         }
     );
 
+#if QT_VERSION < QT_VERSION_CHECK(6, 8, 0)
     m_server->afterRequest([] (QHttpServerResponse &&resp) {
         resp.addHeader("Access-Control-Allow-Origin", "*");
         return std::move(resp);
     });
 
+#else
+    m_server->addAfterRequestHandler(
+        m_server.get(), 
+        [](const QHttpServerRequest &, QHttpServerResponse &resp) {
+            auto headers = resp.headers();
+            headers.replaceOrAppend("Access-Control-Allow-Origin", "*");
+            resp.setHeaders(std::move(headers));
+        }
+    );
+
+#endif
     connect(this, &Server::requestServerNewPromptResponsePair, m_chat,
         &Chat::serverNewPromptResponsePair, Qt::BlockingQueuedConnection);
 }
