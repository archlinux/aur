diff --git a/drivers/aic8800/aic8800_fdrv/aicwf_pcie_api.c b/drivers/aic8800/aic8800_fdrv/aicwf_pcie_api.c
--- a/drivers/aic8800/aic8800_fdrv/aicwf_pcie_api.c
+++ b/drivers/aic8800/aic8800_fdrv/aicwf_pcie_api.c
@@ -3,6 +3,7 @@
 #include <linux/pci.h>
 #include <linux/delay.h>
 #include <asm/delay.h>
+#include "aicwf_debug.h"
 #include "aicwf_pcie_api.h"
 
 
@@ -169,17 +170,17 @@ int  aicwf_pcie_dma(struct aic_pci_dev *adev, void *dst, void *src, u32 blen, u8
         udelay(20);
 #endif
         rd = readl(adev->emb_hdma + lli_off);
-        LOG_INFO("    aic dma cnt = %x", rd);
+        AICWFDBG(LOGDEBUG, "aicpcie: aic dma cnt = %x\n", rd);
     } while((rd < lli_tar) && (k < 500 * num));
 
     if(rd == lli_tar)
     {
-        LOG_INFO("    aic dma done, dma irq success, lli_cnt %d -> %d(%d)", lli_old, rd, lli_tar);
+        AICWFDBG(LOGDEBUG, "aicpcie: aic dma done, dma irq success, lli_cnt %d -> %d(%d)\n", lli_old, rd, lli_tar);
         return 0;
     }
     else
     {
-        LOG_WARN("    aic dma done, dma irq failed!, lli_cnt %d -> %d(%d)", lli_old, rd, lli_tar);
+        AICWFDBG(LOGERROR, "aicpcie: aic dma done, dma irq failed!, lli_cnt %d -> %d(%d)\n", lli_old, rd, lli_tar);
         return -1;
     }
 }
