diff --git a/src-tauri/src/bootstrap.rs b/src-tauri/src/bootstrap.rs
--- a/src-tauri/src/bootstrap.rs
+++ b/src-tauri/src/bootstrap.rs
@@ -257,7 +257,7 @@ pub fn run_mcp_stdio() {
                 .await
                 .expect("Failed to init store"),
         );
-        let prompts_dir = PathBuf::from(env!("CARGO_MANIFEST_DIR")).join("resources/prompts");
+        let prompts_dir = PathBuf::from("/usr/lib/DataZen/prompts");
         let app_state = match build_app_state(store, Some(prompts_dir)).await {
             Ok(s) => s,
             Err(e) => {
