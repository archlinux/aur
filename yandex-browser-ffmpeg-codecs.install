post_install() {
    cat << EOF
==> NOTE: After install run install-yb-codecs as root.

EOF
}

post_upgrade() {
    cat << EOF
==> NOTE: After install run install-yb-codecs as root.

EOF
}

post_remove() {
    mv /opt/yandex/browser-beta/lib/libffmpeg.so.bak /opt/yandex/browser-beta/lib/libffmpeg.so
}
