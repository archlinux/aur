Description: Fix undefined symbol error by making NIL_VALUE constexpr
 The static const member NIL_VALUE was causing linkage errors because it was
 used by reference but not defined. Changing it to constexpr resolves this.

--- a/src/cluster/cluster.h
+++ b/src/cluster/cluster.h
@@ -78,7 +78,7 @@
 
 struct Mapback : public Consumer {
-	static const OId NIL_VALUE = (OId)-1;
+	static constexpr OId NIL_VALUE = (OId)-1;
 	Mapback(int64_t count) :
 		centroid_id(count, NIL_VALUE)
 	{}
