post_install() {
  cat << EOF

>>> You must add fakeuptime to HOOKS in /etc/mkinitcpio.conf for
    this hook to work, the kernel parameter fakeuptimesec sets
    the initial uptime in seconds.

EOF
}
