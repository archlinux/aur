post_install() {
	echo
	echo "    Enable the radicle node with \`systemctl --user enable --now radicle-node.service\`"
	echo
}

post_upgrade() {
	echo
	echo "    Restart the radicle node with \`systemctl --user restart radicle-node.service\` to update"
}
