post_install() {
	echo
	echo "    Enable the radicle http daemon with \`systemctl --user enable --now radicle-httpd.service\`"
	echo
}

post_upgrade() {
	echo
	echo "    Restart the radicle http daemon with \`systemctl --user restart radicle-httpd.service\` to update"
	echo
}
