#!/bin/bash
unset WINEPREFIX
if [ ! -d "$HOME"/.eternally-us ] ; then
   mkdir -p "$HOME"/.eternally-us
   #prepare the environment here
fi

WINEDEBUG=-all wine /usr/share/eternally-us/EternallyUs.exe
