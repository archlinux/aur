diff --git a/src/hdf5_drv/silo_hdf5.c b/src/hdf5_drv/silo_hdf5.c
index eec5fed..4a74c45 100644
--- a/src/hdf5_drv/silo_hdf5.c
+++ b/src/hdf5_drv/silo_hdf5.c
@@ -930,7 +930,6 @@ static hid_t    P_ckrdprops = -1;
     }                                                                         \
 }
 
-#if H5_VERS_MAJOR>=1 && H5_VERS_MINOR>=4
 #define MEMBER_3(TYPE,NAME) {                                                 \
     _tmp_m = T_##TYPE; /*possible function call*/                             \
     if (_tmp_m>=0) {                                                          \
@@ -947,19 +946,6 @@ static hid_t    P_ckrdprops = -1;
         }                                                                     \
     }                                                                         \
 }
-#else
-#define MEMBER_3(TYPE,NAME) {                                                 \
-    _tmp_m = T_##TYPE; /*possible function call*/                             \
-    if (_tmp_m>=0) {                                                          \
-        _size = 3;                                                            \
-        db_hdf5_put_cmemb(_mt, #NAME, OFFSET(_m, NAME), 1, &_size, _tmp_m);   \
-        if (_f && (_tmp_f=_f->T_##TYPE)>=0) {                                 \
-            db_hdf5_put_cmemb(_ft, #NAME, _f_off, 1, &_size, _tmp_f);         \
-            _f_off += 3*H5Tget_size(_tmp_f);                                  \
-        }                                                                     \
-    }                                                                         \
-}
-#endif
 
 #define MEMBER_R(TYPE,NAME,N) {                                               \
     for (_j=0; _j<N; _j++) {                                                  \
@@ -1895,7 +1881,6 @@ db_hdf5_get_cmemb(hid_t compound_type, int membno, int *ndims/*out*/,
 
     if ((type=H5Tget_member_type(compound_type, membno))<0) return -1;
     
-#if (H5_VERS_MAJOR==1 && H5_VERS_MINOR>=4) || H5_VERS_MAJOR>1
     if (H5T_ARRAY==H5Tget_class(type)) {
         hsize_t bigdims[3];
         int i;
@@ -1907,9 +1892,6 @@ db_hdf5_get_cmemb(hid_t compound_type, int membno, int *ndims/*out*/,
     } else {
         *ndims = 0;
     }
-#else
-    *ndims = H5Tget_member_dims(compound_type, membno, size, NULL);
-#endif
     return type;
 }
 
@@ -1935,7 +1917,6 @@ db_hdf5_put_cmemb(hid_t compound_type, char const *name, size_t offset,
 {
     int         retval;
     
-#if (H5_VERS_MAJOR==1 && H5_VERS_MINOR>=4) || H5_VERS_MAJOR>1
     if (ndims) {
         hsize_t bigdims[16];
         int i;
@@ -1944,10 +1925,6 @@ db_hdf5_put_cmemb(hid_t compound_type, char const *name, size_t offset,
     }
     retval = H5Tinsert(compound_type, name, offset, type);
     if (ndims) H5Tclose(type);
-#else
-    retval = H5Tinsert_array(compound_type, name, offset, ndims, dim, NULL,
-                             type);
-#endif
     return retval;
 }
 
@@ -3324,7 +3301,7 @@ db_hdf5_set_compression(DBfile *dbfile, int flags)
        return (-1);
     }
     for (i=0; i<nfilters; i++) {           
-#if defined H5_USE_16_API || (H5_VERS_MAJOR == 1 && H5_VERS_MINOR < 8)
+#if defined H5_USE_16_API
             filtn = H5Pget_filter(P_ckcrprops,(unsigned)i,0,0,0,0,0);
 #else
             filtn = H5Pget_filter(P_ckcrprops,(unsigned)i,0,0,0,0,0,NULL);
@@ -4479,7 +4456,7 @@ db_hdf5_compwrz(DBfile_hdf5 *dbfile, int dtype, int rank, int const _size[],
             int i;
             for (i=0; i<H5Pget_nfilters(P_crprops); i++)
             {
-#if defined H5_USE_16_API || (H5_VERS_MAJOR == 1 && H5_VERS_MINOR < 8)
+#if defined H5_USE_16_API
                 if (H5Pget_filter(P_crprops,(unsigned)i,0,0,0,0,0) == DB_HDF5_HZIP_ID)
 #else
                 if (H5Pget_filter(P_crprops,(unsigned)i,0,0,0,0,0,NULL) == DB_HDF5_HZIP_ID)
@@ -4553,7 +4530,7 @@ db_hdf5_compckz(DBfile_hdf5 *dbfile, char *name)
             for (i=0; i<H5Pget_nfilters(plist); i++)
             {
                 char name[256];
-#if defined H5_USE_16_API || (H5_VERS_MAJOR == 1 && H5_VERS_MINOR < 8)
+#if defined H5_USE_16_API
                 H5Pget_filter(plist,(unsigned)i,0,0,0,sizeof(name),name);
 #else
                 H5Pget_filter(plist,(unsigned)i,0,0,0,sizeof(name),name,NULL);
@@ -7409,13 +7386,9 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             hsize_t _size = 3;
                             hid_t _f_ary;
                             msize = ALIGN(msize, sizeof(dummy)) + sizeof(dummy);
-#if H5_VERS_MAJOR>=1 && H5_VERS_MINOR>=4
                             _f_ary = H5Tarray_create(dbfile->T_int, 1, &_size);
                             fsize += H5Tget_size(_f_ary);
                             H5Tclose(_f_ary);
-#else
-                            fsize += 3*H5Tget_size(dbfile->T_int);
-#endif
                             break;
                         }
                         case DB_FLOAT:
@@ -7424,13 +7397,9 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             hsize_t _size = 3;
                             hid_t _f_ary;
                             msize = ALIGN(msize, sizeof(dummy)) + sizeof(dummy);
-#if H5_VERS_MAJOR>=1 && H5_VERS_MINOR>=4
                             _f_ary = H5Tarray_create(dbfile->T_float, 1, &_size);
                             fsize += H5Tget_size(_f_ary);
                             H5Tclose(_f_ary);
-#else
-                            fsize += 3*H5Tget_size(dbfile->T_float);
-#endif
                             break;
                         }
                         case DB_DOUBLE:
@@ -7439,13 +7408,9 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             hsize_t _size = 3;
                             hid_t _f_ary;
                             msize = ALIGN(msize, sizeof(dummy)) + sizeof(dummy);
-#if H5_VERS_MAJOR>=1 && H5_VERS_MINOR>=4
                             _f_ary = H5Tarray_create(dbfile->T_double, 1, &_size);
                             fsize += H5Tget_size(_f_ary);
                             H5Tclose(_f_ary);
-#else
-                            fsize += 3*H5Tget_size(dbfile->T_double);
-#endif
                             break;
                         }
                         default:
@@ -7574,7 +7539,6 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             hid_t _m_ary, _f_ary;
                             hsize_t _size = 3;
                             moffset = ALIGN(moffset, sizeof(dummy));
-#if H5_VERS_MAJOR>=1 && H5_VERS_MINOR>=4
                             _m_ary = H5Tarray_create(H5T_NATIVE_INT, 1, &_size);
                             _f_ary = H5Tarray_create(dbfile->T_int, 1, &_size);
                             if (H5Tinsert(mtype, obj->comp_names[i], moffset, _m_ary)<0 ||
@@ -7587,12 +7551,6 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             foffset += H5Tget_size(_f_ary);
                             H5Tclose(_f_ary);
                             H5Tclose(_m_ary);
-#else
-                            free(object);
-                            object = 0;
-                            db_perror("Cannot customize standard object", E_CALLFAIL, me);
-                            UNWIND();
-#endif
                             memcpy(object+moffset, &obj->h5_vals[obj->h5_offs[j]], sizeof(dummy));
                             moffset += sizeof(dummy);
                             break;
@@ -7603,7 +7561,6 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             hid_t _m_ary, _f_ary;
                             hsize_t _size = 3;
                             moffset = ALIGN(moffset, sizeof(dummy));
-#if H5_VERS_MAJOR>=1 && H5_VERS_MINOR>=4
                             _m_ary = H5Tarray_create(H5T_NATIVE_FLOAT, 1, &_size);
                             _f_ary = H5Tarray_create(dbfile->T_float, 1, &_size);
                             if (H5Tinsert(mtype, obj->comp_names[i], moffset, _m_ary)<0 ||
@@ -7616,12 +7573,6 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             foffset += H5Tget_size(_f_ary);
                             H5Tclose(_f_ary);
                             H5Tclose(_m_ary);
-#else
-                            free(object);
-                            object = 0;
-                            db_perror("Cannot customize standard object", E_CALLFAIL, me);
-                            UNWIND();
-#endif
                             memcpy(object+moffset, &obj->h5_vals[obj->h5_offs[j]], sizeof(dummy));
                             moffset += sizeof(dummy);
                             break;
@@ -7632,7 +7583,6 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             hid_t _m_ary, _f_ary;
                             hsize_t _size = 3;
                             moffset = ALIGN(moffset, sizeof(dummy));
-#if H5_VERS_MAJOR>=1 && H5_VERS_MINOR>=4
                             _m_ary = H5Tarray_create(H5T_NATIVE_DOUBLE, 1, &_size);
                             _f_ary = H5Tarray_create(dbfile->T_double, 1, &_size);
                             if (H5Tinsert(mtype, obj->comp_names[i], moffset, _m_ary)<0 ||
@@ -7645,12 +7595,6 @@ db_hdf5_WriteObject(DBfile *_dbfile,    /*File to write into */
                             foffset += H5Tget_size(_f_ary);
                             H5Tclose(_f_ary);
                             H5Tclose(_m_ary);
-#else
-                            free(object);
-                            object = 0;
-                            db_perror("Cannot customize standard object", E_CALLFAIL, me);
-                            UNWIND();
-#endif
                             memcpy(object+moffset, &obj->h5_vals[obj->h5_offs[j]], sizeof(dummy));
                             moffset += sizeof(dummy);
                             break;
