--- unifi-video.orig	2017-01-16 10:42:40.000000000 -0800
+++ unifi-video	2017-01-22 17:14:16.801263396 -0800
@@ -274,7 +274,6 @@
 case $ACTION in
 	start)
 		require_root
-        ulimit -H -c 200
         echo 0x10 > /proc/self/coredump_filter
 		if is_service_running "${PIDFILE}" >/dev/null; then
 			log_verbose "${NAME} is already running..."
@@ -291,7 +290,6 @@
 		;;
 	stop)
 		require_root
-        ulimit -H -c 200
         echo 0x10 > /proc/self/coredump_filter
 		rc=0
 		pid=$(is_service_running ${PIDFILE}) || rc=$?
