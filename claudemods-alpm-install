pre_install() {
  # Remove the existing /usr/bin/pacman binary if it exists
  if [ -f "/usr/bin/pacman" ]; then
    echo "Removing existing /usr/bin/pacman..."
    rm -f /usr/bin/pacman
  fi
}

post_install() {
  echo "Custom pacman binary has been installed successfully."
}

pre_remove() {
  echo "Removing custom pacman binary..."
}

post_remove() {
  echo "Custom pacman binary has been removed."
}
