post_upgrade() {
    echo 
    echo "Remove ~/.deepinwine/Deepin-WeSing to generate a new bottle."
    echo 
}

post_remove() {
    echo "Deleting existing WeSing bottles..."
    find /home -maxdepth 2 -name ".deepinwine" -exec rm -rf \{\}/Deepin-WeSing/ \;
}