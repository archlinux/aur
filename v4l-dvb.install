_kernver=4.1.5-1-ARCH

post_install() {
    echo ">>> Running depmod..."
    depmod ${_kernver}
}

post_upgrade() {
    echo ">>> Running depmod..."
    depmod ${_kernver}
}

post_remove() {
    echo ">>> Running depmod..."
    depmod ${_kernver}
}
