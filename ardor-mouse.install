post_install() {
    udevadm control --reload-rules 2>/dev/null
    udevadm trigger --subsystem-match=hidraw 2>/dev/null
    echo ">> If the mouse receiver is already plugged in, replug it to get access."
}
post_upgrade() { post_install; }
