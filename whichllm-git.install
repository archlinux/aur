post_install() {
    cat <<'EOF'

==> whichllm-git: NVIDIA GPU detection requires:
    pacman -S python-nvidia-ml-py
    (this pulls in nvidia-utils ~900MB)

EOF
}

post_upgrade() {
    post_install
}
