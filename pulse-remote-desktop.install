post_install() {
	echo "==> pulse-remote-desktop is now a transitional package"
	echo "==> The desktop wrapper has been merged into pulse-remote-git"
	echo "==> pulse-remote-git provides both the server and the desktop app"
}

post_upgrade() {
	post_install
}
