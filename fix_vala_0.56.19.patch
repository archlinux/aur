diff --git a/meson.build b/meson.build
index 61162de..c3f4f58 100644
--- a/meson.build
+++ b/meson.build
@@ -97,6 +97,10 @@ if get_option('automation').enabled()
   add_project_arguments(['-D', 'ENABLE_AUTOMATION'], language: 'vala')
 endif
 
+if valac.version().version_compare('>= 0.56.19')
+  add_project_arguments(['-D', 'VALA_0_56_19'], language: 'vala')
+endif
+
 application_id = 'io.github.focustimerhq.FocusTimer'
 application_name = 'Focus Timer'
 package_name = meson.project_name()
diff --git a/src/ui/preferences/automation/action/action-listboxrow.vala b/src/ui/preferences/automation/action/action-listboxrow.vala
index cc5936a..3a63441 100644
--- a/src/ui/preferences/automation/action/action-listboxrow.vala
+++ b/src/ui/preferences/automation/action/action-listboxrow.vala
@@ -91,7 +91,12 @@ namespace Ft
             this.drag_widget.append (row);
             this.drag_widget.drag_highlight_row (row);
 
+            #if VALA_0_56_19
+            var drag_icon = new Gtk.DragIcon.get_for_drag (drag);
+            #else
             var drag_icon = (Gtk.DragIcon) Gtk.DragIcon.get_for_drag (drag);
+            #endif
+
             drag_icon.child = this.drag_widget;
 
             drag.set_hotspot ((int) this.drag_x, (int) this.drag_y);
