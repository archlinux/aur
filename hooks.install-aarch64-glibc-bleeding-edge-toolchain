post_install() {
	echo "bootlin toolchain installed to /opt/aarch64-glibc-bleeding-edge"
	echo "The PATH variable will be updated on re-login. To immediately load it:"
	echo ""
	echo "	source /etc/profile.d/aarch64-glibc-bleeding-edge.sh"
	echo ""
}
