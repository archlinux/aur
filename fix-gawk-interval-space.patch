--- a/scripts/parse-table.awk	2026-09-09 12:45:17.454670410 +0700
+++ b/scripts/parse-table.awk	2026-09-09 12:45:22.721834661 +0700
@@ -135,7 +135,7 @@
             } else {
                 trimed = trimStarts(line)
                 if (length(trimed) > 0) {
-                    if (match(trimed, /^([^ ,[\]]+ ){4, }/) && !match(trimed, /(  |\t)/)) {
+                    if (match(trimed, /^([^ ,[\]]+ ){4,}/) && !match(trimed, /(  |\t)/)) {
                     } else if (match(trimed, /^[a-z0-9_][A-Za-z0-9_:.-]*(\*)?($|\s|,)/)) {
                         commands[length(commands) + 1] = trimed
                     } else if (match(trimed, /^\\-/)) {
