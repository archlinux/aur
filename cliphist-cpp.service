[Unit]
Description=Wayland Clipboard Manager Daemon (C++ version)
After=wayland.target

[Service]
Type=simple
ExecStart=/usr/bin/cliphist-cpp --daemon
Restart=always
RestartSec=3

[Install]
WantedBy=default.target
