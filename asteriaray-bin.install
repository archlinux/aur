# TUN without interactive sudo: sing-box needs cap_net_admin (+ bind for mixed inbound).
post_install() {
  setcap cap_net_admin,cap_net_bind_service+ep /opt/asteriaray/sing-box 2>/dev/null || true
}

post_upgrade() {
  post_install
}
