--- a/pyproject.toml
+++ b/pyproject.toml
@@ -59,10 +59,10 @@
 
 dependencies = [
     # Core ML
-    "torch>=2.7,<2.12.0",
-    "torchvision>=0.22.0,<0.27.0",
-    "numpy>=2.0.0,<2.3.0", # NOTE: Explicitly listing numpy helps the resolver converge faster. Upper bound imposed by opencv-python-headless.
-    "opencv-python-headless>=4.9.0,<4.14.0",
+    "torch>=2.7",
+    "torchvision>=0.22.0",
+    "numpy>=2.0.0", # Explicitly listing numpy helps the resolver converge faster.
+    "opencv-python-headless>=4.9.0",
     "Pillow>=10.0.0,<13.0.0",
     "einops>=0.8.0,<0.9.0",
 
@@ -81,13 +81,13 @@
     "safetensors>=0.4.3,<1.0.0",
 
     # Lightweight utilities
-    "packaging>=24.2,<26.0",
+    "packaging>=24.2",
     "termcolor>=2.4.0,<4.0.0",
     "tqdm>=4.66.0,<5.0.0",
 
     # Build tools (required by opencv-python-headless on some platforms)
-    "cmake>=3.29.0.1,<4.2.0",
-    "setuptools>=71.0.0,<82.0.0",  # torch 2.11 requires setuptools<82; a higher cap makes the resolver downgrade torch
+    "cmake>=3.29.0.1",
+    "setuptools>=71.0.0",
 ]
 
 # Optional dependencies
