pre_install() {
    # Remove existing /usr/share/navi directory if it exists
    rm -rf /usr/share/navifm
    rm -rf /usr/share/applications/navifm.desktop
}

post_remove() {
    # Remove any leftover files in /usr/share/navi
    sh "uninstall.sh"
}
