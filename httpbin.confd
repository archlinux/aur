# Bind port. Change this to something more useful given your particular
# configuration.
PORT=8000

# Bind host. Ideally, gunicorn should be isolated behind a service such as
# nginx and should not listen on public interfaces. Change this is you're
# adventurous.
HOST=127.0.0.1
