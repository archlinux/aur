post_install() {
  cat <<'EOF'
To reload and activate udev rules and to activate the ssh agent
service run:

sudo udevadm control --reload
sudo udevadm trigger  
systemctl --user enable --now tkey-ssh-agent.service

EOF
}

