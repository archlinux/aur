[Unit]
Description=Trackpad is too damn big

[Service]
ExecStart=/bin/bash -c 'DEVICE=$$(libinput list-devices | grep -A 10 -B 2 -i Touchpad | grep -oP "/dev/input/event\\d+"); /usr/bin/titdb -d "$$DEVICE"'
Restart=on-failure

[Install]
WantedBy=multi-user.target
