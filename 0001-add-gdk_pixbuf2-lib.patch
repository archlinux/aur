From 317c4925d3189e124705cb2d52395dbb47281384 Mon Sep 17 00:00:00 2001
From: Sven Fischer <git-dev@linux4tw.de>
Date: Sun, 15 May 2016 20:51:57 +0200
Subject: [PATCH] add gdk_pixbuf2 lib

---
 CMakeLists.txt     | 5 +++++
 src/CMakeLists.txt | 1 +
 2 files changed, 6 insertions(+)

diff --git a/CMakeLists.txt b/CMakeLists.txt
index ac28e28..35201d3 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -83,6 +83,11 @@ ENDIF( NOT LIBDBUS_LIBRARIES )
 #	MESSAGE( FATAL_ERROR "libflac not found!" )
 #ENDIF( NOT LIBFLAC_LIBRARIES )
 
+PKG_CHECK_MODULES( LIBGDKPIXBUF20 gdk-pixbuf-2.0 )
+IF( NOT LIBGDKPIXBUF20_LIBRARIES )
+	MESSAGE( FATAL_ERROR "gdk-pixbuf not found!" )
+ENDIF( NOT LIBGDKPIXBUF20_LIBRARIES )
+
 PKG_CHECK_MODULES( LIBGIO gio-2.0 )
 IF( NOT LIBGIO_LIBRARIES )
 	MESSAGE( FATAL_ERROR "gio not found!" )
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 2fe7efd..89d45e2 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -308,6 +308,7 @@ TARGET_LINK_LIBRARIES( guayadeque
 	${LIBCURL_LIBRARIES}
         ${LIBTAG_LIBRARIES}
         ${LIBDBUS_LIBRARIES}
+	${LIBGDKPIXBUF20_LIBRARIES}
 	${LIBGIO_LIBRARIES}
 	${LIBGPOD_LIBRARIES}
 	${LIBINDICATE_LIBRARIES}
-- 
2.8.2

