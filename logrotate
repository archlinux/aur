/var/log/tengine/*log {
    missingok
    notifempty
    create 640 http root
    sharedscripts
    compress
    postrotate
        if systemctl -q is-active tengine; then systemctl -q kill --signal=USR1 --kill-whom=main tengine; fi
    endscript
}
