--- config/_arch-n-opsys.orig	2015-05-02 07:52:25.394612627 +0000
+++ config/_arch-n-opsys	2015-05-02 07:52:29.950900994 +0000
@@ -57,15 +57,6 @@
       *86)
 	ARCH=x86
       # we no longer support Linux before the 2.2 kernel.
-	case `uname -r` in
-	  2.2.*) ;;
-	  2.3.*) ;;
-	  2.4.*) ;;
-	  2.5.*) ;;
-	  2.6.*) ;;
-	  3.*) ;;
-	  *) exit 1 ;;
-	esac
 	;;
     # As long as we do not natively support the amd64 architecture,
     # we should fallback to the x86 compatibility mode.  --Stef
