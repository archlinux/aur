diff --git a/webpack.prod.js b/webpack.prod.js
index 578d8652..b14c38b9 100644
--- a/webpack.prod.js
+++ b/webpack.prod.js
@@ -2,7 +2,11 @@ const { merge } = require('webpack-merge');
 const common = require('./webpack.common.js');
 
 module.exports = (env) =>
-  common(env).map((config) =>
+  common({
+    ...env,
+    onlyTranspileTypescript: true,
+    experimentalFeatures: true,
+  }).map((config) =>
     merge(config, {
       mode: 'production',
       devtool: 'source-map',
