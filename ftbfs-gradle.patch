--- a/build.gradle
+++ b/build.gradle
@@ -1,6 +1,7 @@
 // Used for building a runnable jar
 plugins {
     id 'java'
+    id 'base'
 }
 
 // Where the code is
@@ -29,20 +30,17 @@ jar {
     manifest {
         attributes 'Main-Class': 'bot.Main'
     }
-    // Name of the .jar
-    baseName = "IdleRSC"
 
     // Include all the source files
     from {
-        //configurations.compile.collect { it.isDirectory() ? it : zipTree(it) } //removed to support newer versions of gradle
         configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
     }
 }
 
 // Moves the compiled jar at build/libs/IdleRSC.jar to the root folder
-task copyJar(type: Copy) {
-    from "./build/libs"
-    into "./"
+base {
+  archivesName = "IdleRSC"
+  libsDirectory = getProjectDir()
 }
 
 // Deletes the build directory
@@ -50,8 +48,5 @@ task cleanBuildDir(type: Delete) {
     delete 'build'
 }
 
-// cleanBuildJar depends on copyJar running first
-cleanBuildDir.dependsOn(copyJar)
-
 // "gradle build" depends on cleanBuildJar running first
 build.dependsOn(cleanBuildDir)
