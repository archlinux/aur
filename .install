post_install(){
   printf "The code on github is currently out of date, and I can't find a way to bypass the dev checks, so I can't use system electron to reduce the size.\n"
}
post_update(){
   post_install
}
