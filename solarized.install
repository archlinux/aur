#!/bin/sh

post_install() {
 cat << EOF
==> To start using this package, add the following lines to your .emacs
(require 'color-theme)
(require 'color-theme-solarized)

EOF
}

post_upgrade() {
  post_install $1
}
