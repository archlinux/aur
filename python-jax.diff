--- a/build/build.py	2023-07-28 00:11:05.000000000 +0300
+++ b/build/build.py	2023-07-28 03:46:42.158795877 +0300
@@ -553,7 +553,12 @@
     command += ["--editable"]
   print(" ".join(command))
   shell(command)
-  shell([bazel_path] + args.bazel_startup_options + ["shutdown"])
+  try:
+    subprocess.run([bazel_path] + args.bazel_startup_options + ["shutdown"], check=True, capture_output=True)
+  except subprocess.CalledProcessError as e:
+      print(e.stdout)
+      print(e.stderr)
+      raise
 
 
 if __name__ == "__main__":
