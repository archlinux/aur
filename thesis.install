# arg 1:  the new package version
post_install() {
  texconfig-sys rehash
}

post_upgrade() {
  texconfig-sys rehash
}

op=$1
shift
[ "$(type -t "$op")" = "function" ] && $op "$@"

# vim:set ts=2 sw=2 et:
