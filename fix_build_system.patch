--- pyproject.toml
+++ pyproject.toml

@@ -1,5 +1,5 @@ setuptools-scm now provides git-archive
 [build-system]
-requires = ["setuptools>=48.0", "wheel", "setuptools_scm[toml]>=3.4", "setuptools_scm_git_archive>=1.1"]
+requires = ["setuptools>=48.0", "wheel", "setuptools_scm[toml]>=3.4"]
 build-backend = "setuptools.build_meta"
 
 [tool.setuptools_scm]
