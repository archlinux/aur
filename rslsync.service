[Unit]
Description=Resilio Sync service
After=network.target
 
[Service]
Type=forking
User=rslsync
Group=rslsync
ExecStart=/usr/bin/rslsync --config /etc/rslsync.conf
Restart=on-abort

[Install]
WantedBy=multi-user.target
