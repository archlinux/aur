--- build.sh
+++ build.sh
@@ -38,12 +38,12 @@ if [[ "$SHOULD_BUILD" == "yes" ]]; then
   else # linux
     yarn gulp vscode-linux-${VSCODE_ARCH}-min-ci
 
-    yarn gulp "vscode-linux-${VSCODE_ARCH}-build-deb"
+    # yarn gulp "vscode-linux-${VSCODE_ARCH}-build-deb"
 
-    if [[ "$VSCODE_ARCH" == "x64" ]]; then
-      yarn gulp "vscode-linux-${VSCODE_ARCH}-build-rpm"
-    fi
-    . ../create_appimage.sh
+    # if [[ "$VSCODE_ARCH" == "x64" ]]; then
+    #   yarn gulp "vscode-linux-${VSCODE_ARCH}-build-rpm"
+    # fi
+    # . ../create_appimage.sh
   fi
 
   cd ..
