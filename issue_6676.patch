diff '--color=auto' -Naur src/connectionbenchmark/benchmarktaskdownload.cpp src2/connectionbenchmark/benchmarktaskdownload.cpp
--- src/connectionbenchmark/benchmarktaskdownload.cpp	2022-11-09 23:10:00.000000000 -0800
+++ src2/connectionbenchmark/benchmarktaskdownload.cpp	2022-11-15 18:54:49.568792969 -0800
@@ -47,10 +47,6 @@
     m_dnsLookup.setNameserver(QHostAddress(MULLVAD_DEFAULT_DNS));
 #endif
 
-#if QT_VERSION >= 0x060500
-#  error Check if QT added support for QDnsLookup::lookup() on Android
-#endif
-
 #if defined(MVPN_ANDROID) || defined(MVPN_WASM)
     NetworkRequest* request =
         NetworkRequest::createForGetUrl(this, m_fileUrl.toString());
diff '--color=auto' -Naur src/connectionbenchmark/benchmarktasktransfer.cpp src2/connectionbenchmark/benchmarktasktransfer.cpp
--- src/connectionbenchmark/benchmarktasktransfer.cpp	2022-11-09 23:10:00.000000000 -0800
+++ src2/connectionbenchmark/benchmarktasktransfer.cpp	2022-11-15 18:55:06.502399098 -0800
@@ -53,10 +53,6 @@
     m_dnsLookup.lookup();
 #endif
 
-#if QT_VERSION >= 0x060400
-#  error Check if QT added support for QDnsLookup::lookup() on Android
-#endif
-
     m_elapsedTimer.start();
   } else if (state == BenchmarkTask::StateInactive) {
     for (NetworkRequest* request : m_requests) {
