--- a/setup-desktop-entry.sh.old
+++ b/setup-desktop-entry.sh
@@ -1,31 +1,32 @@
 #!/usr/bin/env sh
 # Generate a desktop entry for Flashpoint Archive.
 
-[ `id -u` -ne 0 ] && cd -P -- "`dirname "$0"`" && [ -f Launcher/flashpoint-launcher ] || exit 1
+[ $(id -u) -eq 0 ] && cd -P -- "`dirname "$0"`" && [ -f "$(realpath "$(dirname "${BASH_SOURCE[0]}")")/Launcher/flashpoint-launcher" ] || exit 1
 
 [ "$ENTRY" ] || ENTRY="$HOME/.local/share/applications/flashpoint-archive.desktop"
 [ -e "${ENTRY%/*}" ] || mkdir -p "${ENTRY%/*}"
 
+# Set configurable Flashpoint root directory.
+# Default: absolute path to script's directory
+[ "$FP_DIR" ] || FP_DIR="$(realpath "$(dirname "${BASH_SOURCE[0]}")")"
+
 printf "[Desktop Entry]
 Type=Application
 Name=Flashpoint Archive
 Comment=An archive for games and animations from the web.
-Icon=$PWD/Launcher/icon.svg
+Icon=$FP_DIR/Launcher/icon.svg
 StartupWMClass=flashpoint-launcher
 Exec=/usr/bin/env -S " > "$ENTRY"
 if [ "`file -b --mime-type Libraries/libgtk-3.so.0`" = 'application/x-sharedlib' ]; then
-  printf "GALLIUM_DRIVER=\"i915,i965,iris,nouveau,nouveau_vieux,r200,r300,r600,radeon,radeonsi,zink,swrast\" GDK_PIXBUF_MODULE_FILE=\"$PWD/Libraries/loaders.cache\" GSETTINGS_SCHEMA_DIR=\"$PWD/Libraries\" LD_LIBRARY_PATH=\"$PWD/Libraries\" LIBGL_DRIVERS_PATH=\"$PWD/Libraries\" PATH=\"$PWD/Libraries" >> "$ENTRY" 
-  if [ ! -d FPSoftware/Wine/bin ]; then
+  printf "GALLIUM_DRIVER=\"i915,i965,iris,nouveau,nouveau_vieux,r200,r300,r600,radeon,radeonsi,zink,swrast\" GDK_PIXBUF_MODULE_FILE=\"$FP_DIR/Libraries/loaders.cache\" GSETTINGS_SCHEMA_DIR=\"$FP_DIR/Libraries\" LD_LIBRARY_PATH=\"$FP_DIR/Libraries\" LIBGL_DRIVERS_PATH=\"$FP_DIR/Libraries\" PATH=\"$FP_DIR/Libraries" >> "$ENTRY" 
+  if [ ! -d $FP_DIR/FPSoftware/Wine/bin ]; then
     CMD="`command -v wine`"
     [ "$CMD" ] && printf ":${CMD%/*}" >> "$ENTRY"
   fi
   printf '" ' >> "$ENTRY"
 fi
-printf "WINEPREFIX=\"$PWD/FPSoftware/Wine\" ./flashpoint-launcher --js-flags=--lite_mode --ozone-platform-hint=auto
-Path=$PWD/Launcher
+printf "WINEPREFIX=\"$FP_DIR/FPSoftware/Wine\" ./flashpoint-launcher --js-flags=--lite_mode --ozone-platform-hint=auto
+Path=$FP_DIR/Launcher
 Terminal=false
 Categories=Archiving;Game;
 SingleMainWindow=true" >> "$ENTRY"
-
-# This is for backwards compatibility, and will be removed in the future.
-ln "$ENTRY" Flashpoint.desktop
\ No newline at end of file
