post_install() {
	if [ ! -f /usr/bin/byteball ]; then
		ln -s /usr/share/byteball/Byteball /usr/bin/byteball
	fi
}

post_upgrade() {
	if [ ! -f /usr/bin/byteball ]; then
		ln -s /usr/share/byteball/Byteball /usr/bin/byteball
	fi
}

post_remove() {
	rm -f /usr/bin/byteball
}
