diff --git a/CMakeLists.txt b/CMakeLists.txt
index e165c19..221647f 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -344,7 +344,9 @@ include_directories("${CMAKE_CURRENT_SOURCE_DIR}/include")
 if (${SIM_PLATFORM} STREQUAL "Linux" OR ${SIM_PLATFORM} STREQUAL "Darwin")
     # libplatform.cpp needs OpenBLAS (<cblas.h>, openblas_set_num_threads)
     set(BLA_VENDOR OpenBLAS)
-    find_package(BLAS REQUIRED)
+    enable_language(Fortran)
+    find_package(OpenMP REQUIRED COMPONENTS Fortran C)
+    find_package(OpenBLAS REQUIRED)
     find_package(LAPACK REQUIRED)
 endif()
 
@@ -383,7 +385,8 @@ elseif (${SIM_PLATFORM} STREQUAL "Darwin")
 else()
     set(SIM_EXE_LIBRARIES
             dl m klu amd camd colamd ccolamd btf cholmod suitesparseconfig
-            ${LAPACK_LIBRARIES} ${BLAS_LIBRARIES}
+            OpenBLAS::OpenBLAS
+            ${LAPACK_LIBRARIES}
             ${Boost_LIBRARIES}
         CACHE STRING "External libraries for creating an executable."
         FORCE
diff --git a/lib/CMakeLists.txt b/lib/CMakeLists.txt
index c4491f6..34f135e 100644
--- a/lib/CMakeLists.txt
+++ b/lib/CMakeLists.txt
@@ -226,6 +226,7 @@ target_include_directories(simlib PUBLIC
     ${CMAKE_CURRENT_SOURCE_DIR}
     ${CMAKE_CURRENT_BINARY_DIR}
     ${FLEX_INCLUDE_DIRS}
+    ${OpenBLAS_INCLUDE_DIRS}
 )
 # target_compile_definitions(dlib PRIVATE BUILDING_SIM_DLL)
 
