# 贡献指南

感谢您对本项目的关注与贡献。

本仓库承担两个职责：

- 提供稳定、可复现的 Linux 发行版本来源（作为上游发布的统一镜像源）
- 维护对应的 AUR PKGBUILD

由于本仓库承担“再分发节点”的角色，请务必确保更新过程具备可验证性与可追溯性。

## 一、整体工作流程

1. 发现上游发布新版本
2. 验证上游发布来源真实性
3. 更新本仓库构建文件与 PKGBUILD
4. 提交 Pull Request
5. 审核通过并合并
6. 创建 Release
7. 更新并推送至 AUR

自动构建函数中统一采用二空格缩进，单行上限可依据易读性进行换行。

## 二、Pull Request 提交要求

所有版本更新必须通过 Pull Request 方式提交。

正常版本更新 `commit msg` 说明格式为：upload vxx.xx.xx

PR 内容需包含：

- 更新后的 `pkgver`
- 正确递增的 `pkgrel` （一般来说，无需；除非打包策略更改）
- 更新后的校验值（sha256sums 或其他）
- 在干净环境中执行 `makepkg -si` 的构建验证结果（.SRCINFO）
- 上游版本来源说明（官网链接或原始发布说明）

禁止提交：

- 跳过校验的构建方式
- 未说明来源的二进制替换
- 使用 `--skipchecksums` 且未说明原因

## 三、Release 发布策略

Release 创建属于受控操作，仅由维护者执行。

Release 的创建条件：

- PR 审核通过
- 构建验证通过
- 必要时完成手动测试确认

本地调试时使用 `PKGBUILD` 中注释的 `# source=("$_pkgname-$pkgver.appimage")` 完成构建测试。

验证无误并安装通过后，使用 PR 请求合并的同时，可将该二进制包发送至维护者邮箱。

```text
beanc904@gmail.com
```

如需测试构建产物，请使用 PR 关联的 CI 构建产物进行验证。

## 四、AUR 更新流程

Release 验证完成后：

- 更新 AUR 仓库
- 提交信息应与对应 Release 版本一致
- 确保 source 指向正确的 Release 地址

AUR 推送属于维护行为，需确保版本与 Release 保持一致。

## 五、安全与完整性要求

本仓库作为再分发源，必须确保：

- 来源清晰
- 校验完整
- 构建可复现

强烈建议：

- 优先使用上游签名源
- 明确记录指纹来源
- 不绕过验证步骤

若上游发行渠道不稳定，请在 PR 中说明具体下载来源及验证方式。

## 六、共同维护说明

长期稳定贡献者可申请成为共同维护者（AUR Co-maintainer）。

依据 AUR 仓库的传统，共同维护者请依据 `# Maintainer: xxx@xxx.com` 格式顺次签名，便于追溯联系。

由于本项目承担 Linux 版本再分发的任务，共同维护者需同时持有 AUR 与 GitHub 仓库的管理权限。

共同维护者应遵循本贡献规范。

权限授予基于历史贡献与稳定性评估。

## 七、沟通方式

如涉及结构调整、发布流程修改或重大构建变更，请优先提交 Issue 讨论后再发起 PR。

## 最终说明

本项目的目标是：

在上游发行不稳定的情况下，提供一个稳定、可验证、可持续维护的 Arch Linux 分发来源。

感谢您的参与与协作。
