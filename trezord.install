#!/bin/bash

post_install() {
  echo "To activate the service so that is starts automatically on boot, symlink it with:"
  echo "  ln -s /etc/runit/sv/trezord /run/runit/service"
  echo "Or to start it manually once now:"
  echo "  sudo sv start trezord"
}
