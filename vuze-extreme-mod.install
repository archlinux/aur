pkgname=vuze-extreme-mod

post_install() {
    gconfpkg --install "$pkgname"
    update-desktop-database -q
}

post_upgrade() {
    post_install
}

post_remove() {
    gconfpkg --uninstall "$pkgname"
    update-desktop-database -q
}
