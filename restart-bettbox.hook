[Trigger]
Operation = Upgrade
Type = Path
Target = usr/bin/bettbox
Target = usr/bin/Bettbox

[Action]
Description = Restarting Bettbox...
When = PostTransaction
Exec = /bin/sh -c 'for d in /run/user/*/; do u=${d#/run/user/}; u=${u%/}; su "$(id -nu "$u" 2>/dev/null)" -c "/usr/bin/bettbox --restart" 2>/dev/null || true; done'
