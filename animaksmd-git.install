post_install() {
  cat <<'EOF'
AnimaKSMD is installed.

Edit /etc/animaksm.toml, then start the daemon with:
  systemctl enable --now animaksm.service

The swap proxy service is experimental and remains disabled by default.
EOF
}

post_upgrade() {
  post_install
}
