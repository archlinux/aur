post_install() {
	echo "Config auto-generates on first use"
	echo "Optional: See 'omarchy-workspace-manager waybar --help' for Waybar integration"
}

pre_remove() {
	omarchy-workspace-manager setup uninstall 2>/dev/null || true
}
