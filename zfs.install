post_install() {
    dkms add zfs/${1%%[-_]*}
}

pre_upgrade() {
    pre_remove ${2%%[-_]*}
}

post_upgrade() {
    post_install ${1%%[-_]*}
}

pre_remove() {
    [ -n "${1%%[-_]*}" ] && dkms remove zfs/${1%%[-_]*} --all &>/dev/null || true
}
