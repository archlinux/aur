post_install() {
    dkms install zfs/${1%%[-_]*}
}

pre_upgrade() {
    pre_remove ${2%%[-_]*}
}

post_upgrade() {
    post_install ${1%%[-_]*}
}

pre_remove() {
    dkms remove zfs/${1%%[-_]*} --all
}
