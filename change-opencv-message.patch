--- findpeaks/utils/stats.py
+++ findpeaks/utils/stats.py
@@ -29,7 +29,7 @@
         import cv2
         return cv2
     except:
-        raise ImportError('cv2 must be installed manually. Try to: <pip install opencv-python>')
+        raise ImportError('The opencv, hdf5 and qt5-base packages must be installed for this feature.')
 
 
 # %% Scaling
