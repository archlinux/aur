--- a/setup.py
+++ b/setup.py
@@ -1,5 +1,5 @@
 import os
-from setuptools import setup
+from setuptools import find_namespace_packages, setup
 import re
 
 
@@ -39,8 +39,7 @@
     author='Destiny Peterson & Matthew Walker',
     author_email='arceusthe@gmail.com, mattgwwalker@gmail.com',
     license='GPL',
-    packages=[main_module],
-    py_modules=[main_module],
+    packages=find_namespace_packages(include=[main_module, f'{main_module}.*']),
     entry_points={
         'console_scripts': ['extract_msg = extract_msg.__main__:main',]
     },
--- a/requirements.txt
+++ b/requirements.txt
@@ -3,11 +3,11 @@
 # Modules using == are *intentional* and must be checked manually before
 # upgrading.
 olefile==0.47
-tzlocal>=4.2,<6
-compressed-rtf>=1.0.6,<2
-ebcdic>=1.1.1,<3
+tzlocal>=4.2
+compressed-rtf>=1.0.6
+ebcdic>=1.1.1
 # Beautiful soup can sometimes make weird versioning, so something *could*
 # break, but so long as they at least reasonably do sem-var this should be fine.
-beautifulsoup4>=4.11.1,<5
-RTFDE>=0.1.1,<0.2
-red-black-tree-mod>=1.20, <=1.23
+beautifulsoup4>=4.11.1
+RTFDE>=0.1.1
+red-black-tree-mod>=1.20
--- a/setup.cfg
+++ b/setup.cfg
@@ -1,5 +1,5 @@
 [bdist_wheel]
-python-tag=py3
+python_tag=py3
 
 [options.extras_require]
 all =
@@ -7,10 +7,10 @@
     extract-msg[image]
     extract-msg[encoding]
 mime =
-    python-magic>=0.4.27,<0.5
+    python-magic>=0.4.27
 image =
-    Pillow>=9.5.0,<10
+    Pillow>=9.5.0
 encoding =
-    chardet>=3.0.0,<6 # This can probably be unbound.
+    chardet>=3.0.0
 readthedocs =
     sphinx-rtd-theme
\ No newline at end of file
--- a/MANIFEST.in
+++ b/MANIFEST.in
@@ -1,6 +1,6 @@
 include requirements.txt
 include MANIFEST.in
-include README.md
+include README.rst
 include LICENSE.txt
 include CHANGELOG.md
 recursive-include extract_msg *
