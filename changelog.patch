--- "a/aurupbot"   2015-12-04 12:40:48.779721546 -0300
+++ "b/src/aurupbot-v1.3/aurupbot"       2015-12-04 12:41:33.942071535 -0300
@@ -146,7 +146,7 @@ get_bzr_revs() {
   REV_LOG=$( cd "$CLONE_DIR" &&
              bzr log --log-format line -r $FROM_REV.. |
              sed -n "s/ .*[0-9]\{4\}-[0-9]\{2\}-[0-9]\{2\} / /p" ) &&
-  CHANGELOG=$( printf "\n\nChangelog\n=========\n$REV_LOG\n" )
+  CHANGELOG=$( printf "\n\nChangelog\n=========\n"; cat <<< "$REV_LOG" )
 }
 
 get_git_commits() {
@@ -166,7 +166,7 @@ get_git_commits() {
                     2> /dev/null
                   fi; } ) &&
   { cat <<< "$COMMIT_LOG" > "$1"
-    CHANGELOG=$( printf "\n\nChangelog\n=========\n$COMMIT_LOG\n" ); }
+    CHANGELOG=$( printf "\n\nChangelog\n=========\n"; cat <<< "$COMMIT_LOG" ); }
 }
 
 get_hg_revs() {
@@ -174,7 +174,7 @@ get_hg_revs() {
   REV_LOG=$( cd "$CLONE_DIR" &&
              hg log --template "{desc | firstline} ({node | short})\n" \
                     -r $FROM_REV.. | tac ) &&
-  CHANGELOG=$( printf "\n\nChangelog\n=========\n$REV_LOG\n" )
+  CHANGELOG=$( printf "\n\nChangelog\n=========\n"; cat <<< "$REV_LOG" )
 }
 
 get_svn_revs() {
@@ -185,7 +185,7 @@ get_svn_revs() {
              sed -n "/^r[0-9]* | .*$/{N;p}" |
              sed "s/^\(r[0-9]*\) | .*$/\1: /" |
              sed ":a;N;\$!ba;s/: \n/: /g" | tac ) &&
-  CHANGELOG=$( printf "\n\nChangelog\n=========\n$REV_LOG\n" )
+  CHANGELOG=$( printf "\n\nChangelog\n=========\n"; cat <<< "$REV_LOG" )
 }
 
 get_user_packages() {
