--- Makefile.am	2019-08-31 15:08:09.000000000 +0300
+++ Makefile.am	2019-09-15 19:14:05.427652228 +0300
@@ -47,14 +47,15 @@
 install-data-local:
 	@$(NORMAL_INSTALL)
 	if test -d files/; then \
-	  install files/$(PACKAGE).desktop $(DESTDIR)/usr/share/applications/; \
-	  install files/$(PACKAGE).svg $(DESTDIR)/usr/share/pixmaps/; \
-	fi; \
-	if !(test -d $(DESTDIR)/usr/share/doc/$(PACKAGE)); then \
-	  mkdir $(DESTDIR)/usr/share/doc/$(PACKAGE)/; \
+	  $(MKDIR_P) $(DESTDIR)$(datarootdir)/applications;\
+	  $(MKDIR_P) $(DESTDIR)$(datarootdir)/pixmaps;\
+	  $(install_sh_DATA) files/$(PACKAGE).desktop $(DESTDIR)$(datarootdir)/applications/; \
+	  $(install_sh_DATA) files/$(PACKAGE).svg $(DESTDIR)$(datarootdir)/pixmaps/; \
 	fi; \
 	if test -d doc/; then \
-	  install doc/* $(DESTDIR)/usr/share/doc/$(PACKAGE)/; \
-	  install doc/$(PACKAGE).1.gz $(DESTDIR)/usr/share/man/man1/; \
+	  $(MKDIR_P) $(DESTDIR)$(docdir);\
+	  $(MKDIR_P) $(DESTDIR)$(mandir)/man1;\
+	  $(install_sh_DATA) doc/* $(DESTDIR)$(docdir)/; \
+	  $(install_sh_DATA) doc/$(PACKAGE).1.gz $(DESTDIR)$(mandir)/man1/; \
 	fi;
