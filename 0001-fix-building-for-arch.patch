diff --git a/CMakeLists.txt b/CMakeLists.txt
index 506a95e..d61893a 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -414,6 +414,10 @@ add_definitions(-DwxUSE_UNICODE -D_UNICODE -DUNICODE -DWXINTL_NO_GETTEXT_MACRO)
 # Disable unsafe implicit wxString to const char* / std::string and vice versa. This implicit conversion breaks the UTF-8 encoding quite often.
 add_definitions(-DwxNO_UNSAFE_WXSTRING_CONV)
 
+find_package(JPEG REQUIRED)
+add_library(libjpeg-turbo INTERFACE)
+target_link_libraries(libjpeg-turbo INTERFACE JPEG::JPEG)
+
 # Find and configure boost
 if(SLIC3R_STATIC)
     # Use static boost libraries.
