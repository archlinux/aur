# Read about the gemrc format at http://guides.rubygems.org/command-reference/#gem-environment
#
# --user-install is used to install to $HOME/.gem/ by default since we want to
# separate pacman installed gems and gem installed gems.
#
# --no-document is used to speed up gem installation
#
gem: --user-install --no-document
