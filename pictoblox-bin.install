post_install() {
  echo ""
  echo "PictoBlox installed."
  echo "For USB/Serial access, add your user to the uucp group:"
  echo "  sudo usermod -aG uucp \$USER"
  echo "Then log out and log back in."
  echo ""
}

post_upgrade() {
  post_install
}
