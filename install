post_install() {
    ## reload udev rules
    udevadm control --reload-rules

    ## reload systemd services
    systemctl daemon-reload
}

post_remove() {
    udevadm control --reload-rules

    systemctl daemon-reload
}
