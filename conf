# Prometheus exporter on localhost:8080
SCAPHANDRE_ARGS=prometheus --port 8080 --address 0.0.0.0 --containers

# Stdout example (for debugging):
#SCAPHANDRE_ARGS=stdout --step 60 --timeout 0

# If desired, limit the source hosts for incoming connections
SCAPHANDRE_IP_ALLOW=any
