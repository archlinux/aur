
commands to update system:

sudo pacman -Syy
sudo pacman -Su

command for chroot build:

extra-x86_64-build

generate SHA:

makepkg -g >> PKGBUILD

generate .SRCINFO:

makepkg --printsrcinfo > .SRCINFO

