post_install() {
  echo "Dont forget to add your user into the uucp group for serial access. To do that, run:"
  echo "> sudo usermod -aG uucp $USER"
  echo "(See https://wiki.archlinux.org/title/Users_and_groups#User_groups)"
}

post_upgrade() {
  post_install
}
