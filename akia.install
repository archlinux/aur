post_install() {
    cat <<'EOF'
==> Akia needs the PC/SC smart card daemon. Enable it with:
        systemctl enable --now pcscd.socket
==> User config/data is stored under ~/.local/share/akia/
    (or $XDG_DATA_HOME/akia if that variable is set)
EOF
}
