--- a/readcfg.c
+++ b/readcfg.c
@@ -2071,17 +2071,17 @@
       }
       if (i >= 7*24*60*60)
       {
-        printf("%dw", i/7*24*60*60);
+        printf("%dw", i/(7*24*60*60));
         i %= 7*24*60*60;
       }
       if (i >= 24*60*60)
       {
-        printf("%dd", i/24*60*60);
+        printf("%dd", i/(24*60*60));
         i %= 24*60*60;
       }
       if (i >= 60*60)
       {
-        printf("%dh", i/60*60);
+        printf("%dh", i/(60*60));
         i %= 60*60;
       }
       if (i >= 60)
