#!/bin/sh
if [ ! -d $HOME/.magarena/Magarena ] ; then
    mkdir -p $HOME/.magarena
    tar -xvzf /usr/share/magarena/default-userdata.tar.gz -C $HOME/.magarena/
fi
cd $HOME/.magarena
exec java -Xms256M -Xmx512M -noverify -jar /usr/share/magarena/Magarena.jar
