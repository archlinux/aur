--- piped-html-proxy.a/app.js	2025-10-15 21:26:05.900273943 +0200
+++ piped-html-proxy.b/app.js	2025-10-15 21:29:52.569398525 +0200
@@ -2,6 +2,7 @@
 const express = require('express')
 
 const { JSDOM } = require('jsdom')
+const fs = require('fs')
 
 const { createOgMeta } = require('./helpers')
 
@@ -57,4 +58,12 @@
   }
 })
 
-app.listen(process.env.PORT || 3000)
+if (fs.existsSync(process.env.HOST)) {
+	fs.unlinkSync(process.env.HOST)
+}
+if (process.env.PORT === undefined && process.env.HOST !== undefined)
+	app.listen(process.env.HOST, function(){
+		fs.chmodSync(process.env.HOST, '660')
+	})
+else
+	app.listen(process.env.PORT || 3000, process.env.HOST || '::1')
