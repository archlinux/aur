post_install() {
    cat <<EOF
==> Put this in your ~/.emacs file to use arduino-mode:

(autoload 'arduino-mode "arduino-mode" "Arduino editing mode." t)
(add-to-list 'auto-mode-alist '("\.ino$" . arduino-mode))
EOF
}
