@@ -86,9 +86,10 @@
                     attribute = attribute->Next();
                 }
             }
-
         }
 
+        delete document;
+
         if (Director::getInstance()->getRunningScene())
         {
             auto scene = Scene::create();
