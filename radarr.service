[Unit]
Description=Radarr Service
After=syslog.target network.target

[Service]
User=radarr
Group=radarr
ExecStart=/usr/bin/mono /usr/lib/radarr/Radarr.exe -nobrowser -data=/var/lib/radarr
Type=simple
TimeoutStopSec=20
KillMode=process
Restart=always

[Install]
WantedBy=multi-user.target
