post_install() {
  echo
  echo "gvm2 installed. Set up a per-user copy with:"
  echo "  gvm2-setup"
  echo "Then open a new shell (or source ~/.gvm/scripts/gvm) and run:"
  echo "  gvm install go1.22.12"
  echo
  echo "This package conflicts with AUR gvm2-git and gvm-git (moovweb/gvm)."
  echo
}

post_upgrade() {
  post_install
}
