diff --git a/external/tinycthread/tinycthread.h b/external/tinycthread/tinycthread.h
index b67a792..34c8871 100644
--- a/external/tinycthread/tinycthread.h
+++ b/external/tinycthread/tinycthread.h
@@ -457,8 +457,6 @@ int tss_set(tss_t key, void *val);
   } once_flag;
   #define ONCE_FLAG_INIT {0,}
 #else
-  #define once_flag pthread_once_t
-  #define ONCE_FLAG_INIT PTHREAD_ONCE_INIT
 #endif
 
 /** Invoke a callback exactly once
@@ -469,7 +467,6 @@ int tss_set(tss_t key, void *val);
 #if defined(_TTHREAD_WIN32_)
   void call_once(once_flag *flag, void (*func)(void));
 #else
-  #define call_once(flag,func) pthread_once(flag,func)
 #endif
 
 #ifdef __cplusplus
