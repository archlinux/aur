--- configure.ac.orig	2013-06-05 21:39:10.000000000 -0700
+++ configure.ac	2013-06-05 21:51:44.000000000 -0700
@@ -51,7 +51,16 @@
 
 dnl Check dependencies
 
-PKG_CHECK_MODULES(THUMBNAILER, swfdec-$SWFDEC_MAJORMINOR >= $SWFDEC_REQUIRED)
+PKG_CHECK_MODULES(THUMBNAILER, swfdec-$SWFDEC_MAJORMINOR >= $SWFDEC_REQUIRED, HAVE_SWFDEC8="yes", HAVE_SWFDEC8="no")
+
+if test "x$HAVE_SWFDEC8" = "xno"; then
+	SWFDEC_MAJORMINOR=0.9
+	AC_SUBST(SWFDEC_MAJORMINOR)
+	SWFDEC_REQUIRED=0.9.0
+
+	PKG_CHECK_MODULES(THUMBNAILER, swfdec-$SWFDEC_MAJORMINOR >= $SWFDEC_REQUIRED)
+fi
+
 PKG_CHECK_MODULES(PLAYER, gmodule-export-2.0 swfdec-$SWFDEC_MAJORMINOR >= $SWFDEC_REQUIRED swfdec-gtk-$SWFDEC_MAJORMINOR >= $SWFDEC_REQUIRED gtk+-2.0 >= $GTK_REQUIRED)
 
 AM_GCONF_SOURCE_2
