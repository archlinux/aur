diff -uprNEBZ --suppress-blank-empty a/build/python.prf b/build/python.prf
--- a/build/python.prf	2014-09-04 16:47:06.000000000 -0500
+++ b/build/python.prf	2015-11-16 00:17:29.091059686 -0600
@@ -36,6 +36,9 @@ macx {
   # on linux, python-config is used to autodetect Python.
   # make sure that you have installed a matching python-dev package.
   
-  unix:LIBS += $$system(python$${PYTHON_VERSION}-config --libs)
-  unix:QMAKE_CXXFLAGS += $$system(python$${PYTHON_VERSION}-config --includes)
+  # There's a symbolic link, so we don't need to specify a Python version
+  # unix:LIBS += $$system(python$${PYTHON_VERSION}-config --libs)
+  # unix:QMAKE_CXXFLAGS += $$system(python$${PYTHON_VERSION}-config --includes)
+  unix:LIBS += $$system(python-config --libs)
+  unix:QMAKE_CXXFLAGS += $$system(python-config --includes)
 }
diff -uprNEBZ --suppress-blank-empty a/build/PythonQt.prf b/build/PythonQt.prf
--- a/build/PythonQt.prf	2007-11-15 13:40:58.000000000 -0600
+++ b/build/PythonQt.prf	2015-11-16 01:14:23.352022344 -0600
@@ -12,4 +12,8 @@ CONFIG(debug, debug|release) {
 }
 
 win32::LIBS += $$PWD/../lib/PythonQt$${DEBUG_EXT}.lib
-unix::LIBS += -L$$PWD/../lib -lPythonQt$${DEBUG_EXT}
+
+# We're doing an out-of-source build
+# so we need to fix the path
+# unix::LIBS += -L$$PWD/../lib -lPythonQt$${DEBUG_EXT}
+unix::LIBS += -L$$PWD/../../build/lib -lPythonQt$${DEBUG_EXT}
\ No newline at end of file
diff -uprNEBZ --suppress-blank-empty a/build/PythonQt_QtAll.prf b/build/PythonQt_QtAll.prf
--- a/build/PythonQt_QtAll.prf	2009-04-09 09:39:56.000000000 -0500
+++ b/build/PythonQt_QtAll.prf	2015-11-16 01:16:52.412398122 -0600
@@ -1,6 +1,9 @@
 # profile for non-mevis users to link to PythonQt_QtAll
 
-INCLUDEPATH += $$PWD/../extensions/PythonQt_QtAll
+# We're doing an out-of-source build
+# so we need to fix the path
+# INCLUDEPATH += $$PWD/../extensions/PythonQt_QtAll
+INCLUDEPATH += $$PWD/../../build/extensions/PythonQt_QtAll
 
 # check if debug or release
 CONFIG(debug, debug|release) {
@@ -10,4 +13,8 @@ CONFIG(debug, debug|release) {
 }
 
 win32::LIBS += $$PWD/../lib/PythonQt_QtAll$${DEBUG_EXT}.lib
-unix::LIBS += -L$$PWD/../lib -lPythonQt_QtAll$${DEBUG_EXT}
+
+# We're doing an out-of-source build
+# so we need to fix the path
+# unix::LIBS += -L$$PWD/../lib -lPythonQt_QtAll$${DEBUG_EXT}
+unix::LIBS += -L$$PWD/../../build/lib -lPythonQt_QtAll$${DEBUG_EXT}
\ No newline at end of file
diff -uprNEBZ --suppress-blank-empty a/PythonQt.pro b/PythonQt.pro
--- a/PythonQt.pro	2009-04-09 11:32:54.000000000 -0500
+++ b/PythonQt.pro	2015-11-16 01:47:25.109518403 -0600
@@ -1,4 +1,7 @@
 TEMPLATE = subdirs
 
 CONFIG += ordered
-SUBDIRS = generator src extensions tests examples
+
+# Do not build tests nor examples
+# SUBDIRS = generator src extensions tests examples
+SUBDIRS = generator src extensions
\ No newline at end of file
