post_install() {
    cat <<'EOF'
BearDrive starts one sync daemon per initialized project.

Initialize or reconnect a project from its directory:

  bdrive init --no-autostart --no-hooks

After a reboot or manual daemon termination, restart registered projects with:

  bdrive resume
EOF
}

post_upgrade() {
    post_install
}
