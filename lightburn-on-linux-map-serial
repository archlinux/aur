#!/usr/bin/env bash
set -euo pipefail
if (( EUID == 0 )); then
    printf 'Run lightburn-on-linux-map-serial as a regular user, not root.\n' >&2
    exit 1
fi
exec bash /usr/share/lightburn-on-linux/scripts/map-serial.sh "$@"
