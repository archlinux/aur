[Unit]
Description=Miasma headless server

[Service]
RuntimeDirectory=miasma
ExecStart=/usr/bin/miasma --unix-socket /run/miasma/miasma.sock --link-prefix bots
ExecStop=/usr/bin/kill -INT $MAINPID
User=miasma

[Install]
WantedBy=multi-user.target
