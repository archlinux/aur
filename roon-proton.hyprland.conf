# Hyprland <= 0.54: tile only Roon's main XWayland window.
# Dialogs and transient popups retain the compositor's default behavior.
windowrule = tile, class:^(steam_app_0)$, title:^(Roon)$, xwayland:1
