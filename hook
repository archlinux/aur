#!/usr/bin/ash

run_hook() {
	modprobe -a -d dm-crypt >/dev/null 2>&1
	gpg --decrypt /rootkey.gpg | cryptsetup open /dev/disk/by-partlabel/random cryptroot -d -
    rm -rf /.gnupg
    /bin/killall gpg-agent scdaemon pcscd &> /dev/null
}

# vim: set ft=sh ts=4 sw=4 et:
