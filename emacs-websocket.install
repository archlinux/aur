post_install() {
    cat <<EOF
=> to use this package, add

(add-to-list 'load-path' "/usr/share/emacs/site-lisp/websocket/")    
(require 'websocket)

to the init file
EOF
}

post_upgrade() {
    post_install $1
}
