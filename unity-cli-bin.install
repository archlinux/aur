post_install() {
	cat <<'EOF'
The Unity CLI is experimental and does not yet have full parity with the Unity Hub UI.
This binary is managed by pacman; do not run 'unity upgrade' or 'unity self-uninstall'.
EOF
}

post_upgrade() {
	post_install
}
