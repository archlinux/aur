#!/usr/bin/env bash
cd "$HOME/.config" || exit

if test -e betterww
then
	:
else
	cp -r /opt/betterww betterww
fi

cd betterww || exit
python wwrando.py
wait

cd "$HOME/.config" || exit
rm -rf betterww
