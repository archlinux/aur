#!/usr/bin/env bash
cd "$HOME/.config" || exit

if ls | grep betterww 1>/dev/null
then
	echo File Already Exists 1>/dev/null
else
	mkdir betterww
fi

cd betterww || exit
python /opt/betterww/wwrando.py
