diff -r -U1 dev_one/Makefile dev_one.makeinstall/Makefile
--- dev_one/Makefile	2023-01-10 13:47:09.129791417 +0100
+++ dev_one.makeinstall/Makefile	2023-01-10 13:52:37.609781142 +0100
@@ -13,2 +13,5 @@
 
+install:
+	$(MAKE) -C $(BUILDSYSTEM_DIR) M=$(PWD) modules_install
+
 clean:
diff -r -U1 dev_one/README.md dev_one.makeinstall/README.md
--- dev_one/README.md	2023-01-10 13:56:31.599773823 +0100
+++ dev_one.makeinstall/README.md	2023-01-10 13:56:16.459774296 +0100
@@ -19,5 +19,6 @@
 2. `make build        # Compile`
-3. `make key          # Generate key for signing`
-4. `sudo make sign    # Sign driver module to permit MOK enforcement (security)`
-5. `sudo reboot now   # Reboot and enable Mok`
+3. `make install      # Install the kernel module`
+4. `make key          # Generate key for signing`
+5. `sudo make sign    # Sign driver module to permit MOK enforcement (security)`
+6. `sudo reboot now   # Reboot and enable Mok`
     1. A blue screen (MOK manager) will appear
@@ -28,5 +29,5 @@
     6. Choose "Reboot" (again)
-6. `sudo make load    # Load`
-7. `sudo make create  # Create /dev/one`
-8. `make test         # Test if all is ok`
+7. `sudo make load    # Load`
+8. `sudo make create  # Create /dev/one`
+9. `make test         # Test if all is ok`
 
