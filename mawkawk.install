# shellcheck shell=bash

post_install() {
  echo 'Relinking /usr/bin/awk to mawk'
  /usr/bin/ln -sfT mawk /usr/bin/awk
}

post_upgrade() {
  post_install "$@"
}

post_remove() {
  echo 'Relinking /usr/bin/awk to gawk'
  /usr/bin/ln -sfT gawk /usr/bin/awk
}

# eof
