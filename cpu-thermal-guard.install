post_install() {
    echo ":: cpu-thermal-guard installed. Enable and start the daemon with:"
    echo "     sudo systemctl enable --now cpu-thermal-guard.service"
    echo ":: Tune /etc/cpu-thermal-guard.conf, then restart the service."
}

post_upgrade() {
    post_install
}
