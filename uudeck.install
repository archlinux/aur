post_install() {
  echo "  :: 请运行 sudo systemctl start uuplugin.service 以启用加速器"
  echo "  :: 如果不希望每次开机都运行一次，请运行 sudo systemctl enable --now uuplugin.service 以使其开机自启"
}

pre_remove() {
  systemctl disable --now uuplugin.service 2>/dev/null || true
}
