# Alias de Spackit
alias spk-update="sudo pacman -Syu"
alias spk-clean="sudo pacman -Rns $(pacman -Qtdq)"

alias s="source /etc/profile.d/spackit.sh && echo 'spackit recargado'"

	echo "====================================================================="
	echo "					SPACKIT v1.4 BY JEICOBROOT-jrt					   "
	echo "====================================================================="
