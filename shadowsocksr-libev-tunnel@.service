[Unit]
Description=ShadowsocksR-Libev Client Service Tunnel Mode
After=network.target

[Service]
Type=simple
User=nobody
CapabilityBoundingSet=CAP_NET_BIND_SERVICE
ExecStart=/usr/bin/ssr-tunnel -c /etc/shadowsocksr/%i.json

[Install]
WantedBy=multi-user.target
