This packages applies a small patch to nemo.

It adds a gsetting that, when toggled on, will not ask for user input during conflicts between two folders, and will simply always merge them.

The setting is off by default, and must be enabled via gsettings.

There is currently no UI element for toggling it in the nemo preferences window.