--- epsonscan2.orig/src/Controller/Src/Finder/UsbFinder.cpp	2025-10-20 21:59:55.000000000 -0300
+++ epsonscan2.new/src/Controller/Src/Finder/UsbFinder.cpp	2026-05-03 20:18:43.819643125 -0300
@@ -142,6 +142,36 @@
                                     isValidDeviceInfo = true;
                                     
                                 }
+                                else if (dd.idProduct == 0x130)
+                                {
+                                    sprintf(const_cast<char*>(devInfo.displayName), "%s", "Perfection V500/GT-X770");
+                                    isValidDeviceInfo = true;
+                                    
+                                }
+                                else if (dd.idProduct == 0x133)
+                                {
+                                    sprintf(const_cast<char*>(devInfo.displayName), "%s", "GT-D1000/1500");
+                                    isValidDeviceInfo = true;
+                                    
+                                }
+                                else if (dd.idProduct == 0x13E)
+                                {
+                                    sprintf(const_cast<char*>(devInfo.displayName), "%s", "Perfection V19II");
+                                    isValidDeviceInfo = true;
+                                    
+                                }
+                                else if (dd.idProduct == 0x13F)
+                                {
+                                    sprintf(const_cast<char*>(devInfo.displayName), "%s", "Perfection V39II");
+                                    isValidDeviceInfo = true;
+                                    
+                                }
+                                else if (dd.idProduct == 0x153)
+                                {
+                                    sprintf(const_cast<char*>(devInfo.displayName), "%s", "GT-X830");
+                                    isValidDeviceInfo = true;
+                                    
+                                }
                                 else
                                 {
                                 sprintf((char*)strDescWithAddress, "%s:%s", strDesc, serialNumber);
