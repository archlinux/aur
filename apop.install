post_install() {
	echo "Create bin app..."
	ln -s "/usr/share/app-opener/app-opener.sh" "$pkgdir/usr/bin/app"
	echo "Create catalog apps..."
	mkdir "/usr/share/app-opener/apps"
	echo "Create catalog scripts..."
	mkdir" /usr/share/app-opener/scripts"
}

post_update() {
	post_install
}
