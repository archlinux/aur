# Prefer the vendor AIC Bluetooth transport for AIC8800D80 before generic btusb.
# Generic btusb can enumerate hci0 but HCI initialization times out on this combo card.
softdep btusb pre: aic_btusb

# Keep the generic fallback conservative if it is used.
options btusb enable_autosuspend=0 reset=0
