diff --git a/lib/std/os/linux/test.zig b/lib/std/os/linux/test.zig
index e38687dbde..b779521056 100644
--- a/lib/std/os/linux/test.zig
+++ b/lib/std/os/linux/test.zig
@@ -346,6 +346,7 @@ fn futex2_skip_if_unsupported() !void {
 }
 
 test "futex2_wait" {
+    if (true) return error.SkipZigTest; // ArchLinux's build chroot blocks this syscall
     var lock: std.atomic.Value(u32) = std.atomic.Value(u32).init(1);
     var rc: usize = 0;
     const mask = 0x1;
@@ -394,6 +395,7 @@ test "futex2_wait" {
 }
 
 test "futex2_wake" {
+    if (true) return error.SkipZigTest; // ArchLinux's build chroot blocks this syscall
     var lock: std.atomic.Value(u32) = std.atomic.Value(u32).init(1);
 
     try futex2_skip_if_unsupported();
@@ -403,6 +405,7 @@ test "futex2_wake" {
 }
 
 test "futex2_requeue" {
+    if (true) return error.SkipZigTest; // ArchLinux's build chroot blocks this syscall
     try futex2_skip_if_unsupported();
 
     const locks = [_]std.atomic.Value(u32){
