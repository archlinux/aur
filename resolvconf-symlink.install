post_install() {
  /usr/bin/mv /etc/resolv.conf /run/resolv.conf
  /usr/bin/ln -sfT /run/resolv.conf /etc/resolv.conf
}

post_upgrade() {
  post_install
}

post_remove() {
  /usr/bin/unlink /etc/resolv.conf
  /usr/bin/mv /run/resolv.conf /etc/resolv.conf
}
