diff --git a/src/ui/meson.build b/src/ui/meson.build
index b42b292..3d9d85d 100644
--- a/src/ui/meson.build
+++ b/src/ui/meson.build
@@ -67,9 +67,11 @@ valadocf = [
 
 if valadoc_dep.found ()
 	valadoc_icons_dir = valadoc_dep.get_pkgconfig_variable('datadir')
+	valadoc_version_array = valadoc_dep.version ().split('.')
+	valadoc_version_dir = 'valadoc-' + '.'.join([valadoc_version_array[0], valadoc_version_array[1]])
 
 	foreach f : valadocf
-		fi = join_paths(valadoc_icons_dir, 'valadoc', 'icons', f)
+		fi = join_paths(valadoc_icons_dir, valadoc_version_dir, 'icons', f)
 		fconf = configuration_data()
 		configure_file (
 			command: [
