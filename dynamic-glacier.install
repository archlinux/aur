post_install() {
  cat <<'EOF'

  Dynamic Glacier installed.
  Run "dynamic-glacier" to start. Autostart will be configured on first launch.

EOF
}

post_upgrade() {
  cat <<'EOF'

  Dynamic Glacier upgraded.

EOF
}
