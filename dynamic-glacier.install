post_install() {
  cat <<'EOF'
Dynamic Glacier installed.

Run it with:
  dynamic-glacier

For Hyprland autostart, add this to hyprland.conf:
  exec-once = dynamic-glacier
EOF
}

post_upgrade() {
  post_install
}
