post_install() {
  echo "Please address the documentation first at https://github.com/akoidan/stream-alert"
  echo "Run stream-alert --config ~/.local/share/stream-alert/config.json"
  echo "start the service with systemctl --user start stream-alert.service"
}

post_upgrade() {
  post_install
}
