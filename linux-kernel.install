post_install() {
	sudo mkinitcpio -p linux-kernel
}

post_remove() {
	sudo rm /boot/initramfs-linux-kernel.img /boot/initramfs-linux-kernel-fallback.img
}

post_upgrade() {
	sudo rm /boot/initramfs-linux-kernel.img /boot/initramfs-linux-kernel-fallback.img
	sudo mkinitcpio -p linux-kernel
}
