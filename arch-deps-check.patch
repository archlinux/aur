--- a/lib/dependencies.sh
+++ b/lib/dependencies.sh
@@ -15,6 +15,11 @@
    local -a depfiles=()
    local -a missing=()
 
+   # Dont check for debian deps on arch (patched by rpi-image-gen aur)
+   if [[ -f /etc/arch-release ]]; then
+      return 0
+   fi
+
    while [[ $# -gt 0 ]]; do
       case "$1" in
          --category)
