post_upgrade() {
  # a full forced directory scan is required here
  printf '%s' 'Rebuilding fontconfig cache...'
  /usr/bin/fc-cache -rs
  printf '%s\n' ' done.'
}

post_install() {
  post_upgrade $1 0
}
