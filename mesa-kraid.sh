# KRAID shader compiler for Panfrost/PanVK (login shells, SSH, and the display-manager session).
# KRAID is a new compiler for Arm Mali Valhall and newer; set this to 0 to use the previous
# compiler with the same Mesa build, e.g. to compare performance or work around a regression.
export PAN_USE_KRAID=1
