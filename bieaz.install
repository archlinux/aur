#!/bin/sh
pre_upgrade() {
	if [ -f /etc/bieaz.cfg ]; then
		mv /etc/bieaz.cfg /etc/bieaz.cfg.pacold
		echo 'old config moved to /etc/bieaz.cfg.pacold'
	fi
};
post_upgrade() {
	bieaz menu -g
}

post_install() {
	bieaz menu -g
}
