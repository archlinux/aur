# dbd-timer key bindings
# Key names from linux/input-event-codes.h
# Format: command = key1, key2, ...
#   Keys on the same line separated by commas are alternatives (OR).
#   Keys joined with + form a chord (must be held together, AND).
#   Example: quit = KEY_ESC + KEY_LEFTCTRL + KEY_LEFTSHIFT
# Commands: select1, select2, toggle, quit
# Mouse buttons: BTN_SIDE (MB4), BTN_EXTRA (MB5), BTN_BACK, BTN_FORWARD

select1 = KEY_F1
select2 = KEY_F2
toggle = KEY_F, BTN_SIDE
quit = KEY_ESC + KEY_LEFTCTRL + KEY_LEFTSHIFT
