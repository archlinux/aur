post_install() {
    if ! getent passwd wirelogd > /dev/null; then
        useradd -r -U wirelogd
    fi
}

pre_remove() {
    systemctl disable --now wirelogd.service
    userdel -f wirelogd
}

post_remove() {
    systemctl daemon-reload
}