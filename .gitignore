# makepkg artifacts from regenerating .SRCINFO / test builds — never commit.
/src/
/pkg/
# Stray makepkg git clone of the source repo (lands here if you build in-repo).
/kappastream/
/kappastream-*/
/*.pkg.tar*
/*.src.tar.gz
