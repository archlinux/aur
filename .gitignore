# makepkg working directories and downloaded/repackaged sources.
# The AUR repository itself carries only the packaging files, the patches and
# .SRCINFO; everything here is regenerated by makepkg.
/src/
/pkg/
*.pkg.tar.*
*.tar.gz
*.tar.xz
*.tar.bz2
*.tar.zst
