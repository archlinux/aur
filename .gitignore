# Archivos de construcción (Carpetas temporales)
pkg/
src/

# El paquete final (el binario que NO se sube)
*.pkg.tar.zst
*.pkg.tar.xz
*.pkg.tar.gz
*.tar.zst

# Archivos de descarga
*.tar.gz
*.tar.xz
*.zip
*.txt
*.patch
.BUILDINFO
.MTREE
.PKGINFO

# Excepción: NO ignores archivos que tú creaste manualmente
# (Si tienes un parche local o un .install, Git debe verlos)
