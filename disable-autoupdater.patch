diff --git a/GUI/app.py b/GUI/app.py
index 83707bc..deffeb0 100644
--- a/GUI/app.py
+++ b/GUI/app.py
@@ -108,12 +108,11 @@ class ModernApp(QMainWindow):
             self._show_default_page()

-        # Auto-check for updates in the background (silent — no popup if up-to-date)
-        self._startup_update_checker = None
-        QTimer.singleShot(2000, self._auto_check_for_updates)
+        # Updater is disabled in distro package builds.
+        self._startup_update_checker = None

     def _auto_check_for_updates(self):
-        """Silently check for updates at startup. Only shows UI if an update is found."""
-        from GUI.auto_updater import UpdateChecker
+        """Disabled in distro package builds."""
+        return

        checker = UpdateChecker(self)
        self._startup_update_checker = checker
diff --git a/GUI/widgets/settingsPage.py b/GUI/widgets/settingsPage.py
index 88fe8c1..0f6ca1d 100644
--- a/GUI/widgets/settingsPage.py
+++ b/GUI/widgets/settingsPage.py
@@ -960,9 +960,9 @@ class SettingsPage(QWidget):
         from settings import get_version
         self.version_row = ActionRow(
             f"iOpenPod v{get_version()}",
-            "Check for a newer version of iOpenPod.",
-            button_text="Check",
+            "Updates are managed by your distro package manager.",
+            button_text="Disabled",
         )
-        self.version_row.clicked.connect(self._check_for_updates)
+        self.version_row.action_btn.setEnabled(False)

         self.bug_report_row = ActionRow(
             "Report a Bug",
