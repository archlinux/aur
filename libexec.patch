From 1890ff4d8a8f91bbaf7c0f48243c93b0e6077e20 Mon Sep 17 00:00:00 2001
From: BarbUk <julien.virey@gmail.com>
Date: Sat, 18 Jul 2026 13:12:01 +0200
Subject: [PATCH 1/3] Add candidate for herlper on /usr/lib/rmux/libexec

---
 src/tiny_main/helper.rs | 1 +
 1 file changed, 1 insertion(+)

diff --git a/src/tiny_main/helper.rs b/src/tiny_main/helper.rs
index 64a40eb1..786445f7 100644
--- a/src/tiny_main/helper.rs
+++ b/src/tiny_main/helper.rs
@@ -234,6 +234,7 @@ fn full_helper_candidates(current_exe: &Path) -> Vec<PathBuf> {
             .join("libexec")
             .join("rmux")
             .join(helper_file_name()),
+        Path::new("/usr/lib/rmux/libexec").join(helper_file_name()),
     ]
 }
 

From 5f5c5ec00e7c06f5510280850351f0a136cd95cc Mon Sep 17 00:00:00 2001
From: BarbUk <julien.virey@gmail.com>
Date: Mon, 20 Jul 2026 10:22:06 +0400
Subject: [PATCH 2/3] Relative unix specific helper candidate

---
 src/tiny_main/helper.rs | 15 ++++++++++++---
 1 file changed, 12 insertions(+), 3 deletions(-)

diff --git a/src/tiny_main/helper.rs b/src/tiny_main/helper.rs
index 786445f7..83731b74 100644
--- a/src/tiny_main/helper.rs
+++ b/src/tiny_main/helper.rs
@@ -227,15 +227,24 @@ fn full_helper_candidates(current_exe: &Path) -> Vec<PathBuf> {
     let Some(parent) = current_exe.parent() else {
         return Vec::new();
     };
-    vec![
+    let mut candidates = vec![
         parent.join("libexec").join("rmux").join(helper_file_name()),
         parent
             .join("..")
             .join("libexec")
             .join("rmux")
             .join(helper_file_name()),
-        Path::new("/usr/lib/rmux/libexec").join(helper_file_name()),
-    ]
+    ];
+    #[cfg(unix)]
+    candidates.push(
+        parent
+            .join("..")
+            .join("lib")
+            .join("rmux")
+            .join("libexec")
+            .join(helper_file_name()),
+    );
+    candidates
 }
 
 #[cfg(not(windows))]

From adaf670073f64e8589a166517449077cce4bae15 Mon Sep 17 00:00:00 2001
From: BarbUk <julien.virey@gmail.com>
Date: Mon, 20 Jul 2026 10:22:43 +0400
Subject: [PATCH 3/3] Add the new candidate for claude_launcher

---
 src/cli/claude_launcher.rs | 6 ++++++
 1 file changed, 6 insertions(+)

diff --git a/src/cli/claude_launcher.rs b/src/cli/claude_launcher.rs
index 623545c5..8682ce64 100644
--- a/src/cli/claude_launcher.rs
+++ b/src/cli/claude_launcher.rs
@@ -1295,6 +1295,12 @@ fn unix_full_helper_candidates(public: &Path) -> Vec<PathBuf> {
             .join("libexec")
             .join("rmux")
             .join(rmux_file_name()),
+        parent
+            .join("..")
+            .join("lib")
+            .join("rmux")
+            .join("libexec")
+            .join(rmux_file_name()),
     ]
 }
 
