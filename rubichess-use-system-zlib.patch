--- a/src/Makefile
+++ b/src/Makefile
@@ -156,12 +156,8 @@ ifeq ($(COMP),ndk)
 	endif
 	LDFLAGS += -static-libstdc++
 endif
 
-
-ZLIBDIR=zlib
-CXXFLAGS += -Izlib
-LDFLAGS += -Lzlib
 zlib = yes
 
 ifeq (,$(CPUFLAGS))
 ifeq ($(ARCH),native)
@@ -382,9 +378,9 @@ endif
 $(ZLIBDIR)/libz.a:
 	@echo Compiling zlib...
 	@cd $(ZLIBDIR); $(MYCC) $(CFLAGS) $(ARCHFLAGS) -w -c *.c; $(AR) rcs libz.a *.o; cd ..
 
-compile: $(ZLIBDIR)/libz.a
+compile:
 	@echo $(MESSAGE)
 	$(CXX) $(CXXFLAGS) $(EXTRACXXFLAGS) $(ARCHFLAGS) *.cpp $(LDFLAGS) $(PTHREADLIB) $(EXTRALDFLAGS) $(GITDEFINE) $(NETDEF) $(NETOBJ) -o $(EXE)
 
 objclean:
