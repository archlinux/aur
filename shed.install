post_install() {
  grep -qx /bin/shed /etc/shells || echo /bin/shed >> /etc/shells
  grep -qx /usr/bin/shed /etc/shells || echo /usr/bin/shed >> /etc/shells
}

post_upgrade() {
  post_install
}

post_remove() {
  sed -i -r '/^(\/usr)?\/bin\/shed$/d' etc/shells
}

# vim:set ts=2 sw=2 et:
