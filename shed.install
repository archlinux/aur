# shellcheck shell=bash
# -*- sh -*-

post_install() {
  grep -qx '/usr/bin/shed' etc/shells ||
    echo '/usr/bin/shed' >> etc/shells
}

post_upgrade() {
  post_install "$@"
}

post_remove() {
  sed -i '/^\/usr\/bin\/shed$/d' etc/shells
}

# eof
