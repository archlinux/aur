post_install() {
	cat <<'EOF'
websudo installs a user systemd service for the approval server.
Run this as the desktop user that will use websudo:

  websudo-systemd-setup
EOF
}

post_upgrade() {
	post_install
}
