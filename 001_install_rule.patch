diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 7448c3e..6363324 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -236,6 +236,6 @@ set_target_properties("${PROGRAM_PREFIX}setup" PROPERTIES
 
 include(GNUInstallDirs)
 
-install(TARGETS "${PROGRAM_PREFIX}doom" "${PROGRAM_PREFIX}heretic" "${PROGRAM_PREFIX}hexen" "${PROGRAM_PREFIX}setup" RUNTIME
+install(TARGETS "${PROGRAM_PREFIX}hexen" "${PROGRAM_PREFIX}setup" RUNTIME
     DESTINATION $<IF:$<BOOL:${WIN32}>,.,${CMAKE_INSTALL_BINDIR}>
     PERMISSIONS OWNER_READ OWNER_WRITE OWNER_EXECUTE GROUP_READ GROUP_WRITE GROUP_EXECUTE WORLD_READ WORLD_WRITE WORLD_EXECUTE)
