--- a/.env.sample	2026-08-24 21:12:29.523518306 +0200
+++ b/.env.sample 2026-08-24 21:14:06.753569143 +0200
@@ -13,15 +13,10 @@
 
 ## Volume mount paths on the host ##
 # These host directories must be owned by the PUID:PGID set below.
-CONFIG_PATH=./config
-SSH_PATH=./ssh
-SSH_HOST=./ssh_host
-BORG_REPOSITORY_PATH=./repos
-
-## User / group the app runs as inside the container ##
-# Must match the owner of the volume directories above. Must be > 0 (root is not allowed).
-PUID=1000
-PGID=1000
+CONFIG_PATH=/etc/webapps/borgwarehouse/config
+SSH_PATH=/var/lib/borgwarehouse/ssh
+SSH_HOST=/var/lib/borgwarehouse/ssh_host
+BORG_REPOSITORY_PATH=/var/lib/borgwarehouse/repos
 
 ## Public hostname / URL ##
 # FQDN shown to users in the generated backup commands.
