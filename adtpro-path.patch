diff -rauN adtpro-vanilla/build/adtprobase.sh adtpro-path-patch/build/adtprobase.sh
--- adtpro-vanilla/build/adtprobase.sh	2025-02-01 09:33:13.001895757 +0100
+++ adtpro-path-patch/build/adtprobase.sh	2025-02-01 10:36:14.945359136 +0100
@@ -12,9 +12,10 @@
 #
 # Set default ADTPRO_HOME to be the fully qualified
 # current working directory.
-export ADTPRO_HOME="`dirname \"$0\"`"
-cd "$ADTPRO_HOME"
-export ADTPRO_HOME="`pwd`/"
+#export ADTPRO_HOME="`dirname \"$0\"`"
+#cd "$ADTPRO_HOME"
+#export ADTPRO_HOME="`pwd`/"
+export ADTPRO_HOME="/usr/share/java/adtpro"
 
 # Uncomment and modify one or both of the lines below if you
 # want to specify a particular location for Java or ADTPro.
@@ -32,7 +33,7 @@
     if [ ! -f /usr/bin/xvfb-run ]; then
       echo "Headless operation requires xvfb."
     else
-      echo "usage: adtpro.sh [ headless ] [ serial | ethernet | audio | localhost ]"
+      echo "usage: adtpro [ headless ] [ serial | ethernet | audio | localhost ]"
     fi
     exit 1
   else
@@ -40,4 +41,4 @@
   fi
 fi
 
-$HEADLESS"$MY_JAVA_HOME"java -Xms256m -Xmx512m -cp "$ADTPRO_HOME"lib/%ADTPRO_VERSION%:"$ADTPRO_HOME"lib/AppleCommander/AppleCommander-%AC_VERSION%.jar:"$ADTPRO_HOME"lib/jssc/jssc-%JSSC_VERSION%.jar:"$ADTPRO_HOME"lib/jssc/slf4j-nop-%SLF4J_VERSION%.jar org.adtpro.ADTPro $*
+$HEADLESS"$MY_JAVA_HOME"java -Dadtpro.properties.file=$HOME/.config/adtpro.properties -Xms256m -Xmx512m -cp "$ADTPRO_HOME"/%ADTPRO_VERSION%:"$ADTPRO_HOME"/AppleCommander-%AC_VERSION%.jar:"$ADTPRO_HOME"/jssc-%JSSC_VERSION%.jar:"$ADTPRO_HOME"/slf4j-nop-%SLF4J_VERSION%.jar org.adtpro.ADTPro $*
