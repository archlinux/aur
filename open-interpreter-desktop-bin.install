post_install() {
  cat <<'EOF'
open-interpreter-desktop-bin:
  Upstream provides a moving "latest" AppImage URL (no stable versioned releases).
  Updates require rebuilding the package to re-download the latest AppImage:
    makepkg -Csf
EOF
}

post_upgrade() {
  post_install
}