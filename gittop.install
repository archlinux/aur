# gittop.install - Post-installation hooks for gittop-bin

post_install() {
    gtk-update-icon-cache -q -t -f /usr/share/icons/hicolor 2>/dev/null || true
    update-desktop-database -q 2>/dev/null || true
}

post_upgrade() {
    post_install
}

post_remove() {
    gtk-update-icon-cache -q -t -f /usr/share/icons/hicolor 2>/dev/null || true
    update-desktop-database -q 2>/dev/null || true
    echo "Note: User settings in ~/.config/GitTop were preserved."
    echo "To remove them: rm -rf ~/.config/GitTop"
}
