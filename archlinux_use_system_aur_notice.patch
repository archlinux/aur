diff --git a/MANIFEST.in b/MANIFEST.in
index 09cfe02..d99428e 100644
--- a/MANIFEST.in
+++ b/MANIFEST.in
@@ -23,3 +23,7 @@ recursive-exclude cecli/website/assets *
 recursive-exclude cecli/website *.js
 recursive-exclude cecli/website *.html
 recursive-exclude cecli/website *.yml
+recursive-exclude cecli/resources *.yml
+recursive-exclude cecli/resources *.json
+recursive-include cecli/prompts *.yml
+recursive-include cecli/tui *.tcss
diff --git a/cecli/help.py b/cecli/help.py
index 22ae6b0..5a93c2b 100755
--- a/cecli/help.py
+++ b/cecli/help.py
@@ -20,16 +20,10 @@ os.environ["TOKENIZERS_PARALLELISM"] = "true"
 
 
 async def install_help_extra(io):
-    pip_install_cmd = [
-        "cecli-dev[help]",
-        "--extra-index-url",
-        "https://download.pytorch.org/whl/cpu",
-    ]
-    res = await utils.check_pip_install_extra(
+    res = await utils.check_aur_package(
         io,
         "llama_index.embeddings.huggingface",
-        "To use interactive /help you need to install the help extras",
-        pip_install_cmd,
+        "python-llama-index-embeddings-huggingface",
     )
     return res
 
diff --git a/cecli/models.py b/cecli/models.py
index d680d7e..c875617 100644
--- a/cecli/models.py
+++ b/cecli/models.py
@@ -26,7 +26,7 @@ from cecli.helpers.nested import deep_merge
 from cecli.helpers.requests import model_request_parser
 from cecli.llm import litellm
 from cecli.sendchat import sanity_check_messages
-from cecli.utils import check_pip_install_extra
+from cecli.utils import check_archlinux_package, check_aur_package
 
 RETRY_TIMEOUT = 60
 COPY_PASTE_PREFIX = "cp:"
@@ -1279,16 +1279,9 @@ async def check_for_dependencies(io, model_name):
         model_name: The name of the model to check dependencies for
     """
     if model_name.startswith("bedrock/"):
-        await check_pip_install_extra(
-            io, "boto3", "AWS Bedrock models require the boto3 package.", ["boto3"]
-        )
+        await check_archlinux_package(io, "boto3", "python-boto3")
     elif model_name.startswith("vertex_ai/"):
-        await check_pip_install_extra(
-            io,
-            "google.cloud.aiplatform",
-            "Google Vertex AI models require the google-cloud-aiplatform package.",
-            ["google-cloud-aiplatform"],
-        )
+        await check_aur_package(io, "google.cloud.aiplatform", "python-aiplatform")
 
 
 def get_chat_model_names():
diff --git a/cecli/scrape.py b/cecli/scrape.py
index 0722e11..adb4790 100755
--- a/cecli/scrape.py
+++ b/cecli/scrape.py
@@ -34,7 +34,7 @@ async def check_chromium():
 
         try:
             async with async_playwright() as p:
-                browser = await p.chromium.launch()
+                browser = await p.chromium.launch(executable_path="/usr/bin/chromium")
                 await browser.close()
                 has_chromium = True
         except Exception as e:
@@ -50,38 +50,30 @@ async def install_playwright(io):
 
     if has_playwright and has_chromium:
         return True
-
-    pip_cmd = utils.get_pip_install(["cecli-dev[playwright]"])
-    chromium_cmd = "-m playwright install --with-deps chromium"
-    chromium_cmd = [sys.executable] + chromium_cmd.split()
-
-    cmds = ""
+    
+    packages = []
     if not has_playwright:
-        cmds += " ".join(pip_cmd) + "\n"
+        packages.append("python-playright")
     if not has_chromium:
-        cmds += " ".join(chromium_cmd) + "\n"
+        packages.append("chromium")
+    pacman_cmd = ["sudo", "pacman", "-Syu", "--asdeps", "--noconfirm", *packages]
+   
+    text = f"""For the best web scraping, run pacman as follows:
 
-    text = f"""For the best web scraping, install Playwright:
-
-{cmds}
+{" ".join(pacman_cmd)}
 See {urls.enable_playwright} for more info.
 """
 
     io.tool_output(text)
-    if not await io.confirm_ask("Install playwright?", default="y"):
+    if not await io.confirm_ask(f"Install {" and ".join(packages)}?", default="y"):
         return
-
-    if not has_playwright:
-        success, output = utils.run_install(pip_cmd)
+    
+    if packages:
+        success, output = utils.run_install(pacman_cmd)
         if not success:
             io.tool_error(output)
             return
 
-    success, output = utils.run_install(chromium_cmd)
-    if not success:
-        io.tool_error(output)
-        return
-
     return True
 
 
@@ -159,7 +151,7 @@ class Scraper:
 
         async with async_playwright() as p:
             try:
-                browser = await p.chromium.launch()
+                browser = await p.chromium.launch(executable_path="/usr/bin/chromium")
             except Exception as e:
                 self.playwright_available = False
                 self.print_error(str(e))
diff --git a/cecli/utils.py b/cecli/utils.py
index aac9b20..b4f2c50 100644
--- a/cecli/utils.py
+++ b/cecli/utils.py
@@ -286,31 +286,10 @@ def split_chat_history_markdown(text, include_tool=False):
     return messages
 
 
-def get_pip_install(args):
-    cmd = [
-        sys.executable,
-        "-m",
-        "pip",
-        "install",
-        "--upgrade",
-        "--upgrade-strategy",
-        "only-if-needed",
-    ]
-    cmd += args
-    return cmd
-
-
 def run_install(cmd):
     print()
     print("Installing:", printable_shell_command(cmd))
 
-    # First ensure pip is available
-    ensurepip_cmd = [sys.executable, "-m", "ensurepip", "--upgrade"]
-    try:
-        subprocess.run(ensurepip_cmd, capture_output=True, check=False)
-    except Exception:
-        pass  # Continue even if ensurepip fails
-
     try:
         output = []
         process = subprocess.Popen(
@@ -385,7 +364,7 @@ def touch_file(fname):
         return False
 
 
-async def check_pip_install_extra(io, module, prompt, pip_install_cmd, self_update=False):
+async def check_aur_package(io, module, aur_package):
     if module:
         try:
             __import__(module)
@@ -393,20 +372,24 @@ async def check_pip_install_extra(io, module, prompt, pip_install_cmd, self_upda
         except (ImportError, ModuleNotFoundError, RuntimeError):
             pass
 
-    cmd = get_pip_install(pip_install_cmd)
+    io.tool_error(f"""
+    This feature requires the following package from the AUR:
 
-    if prompt:
-        io.tool_warning(prompt)
+        {aur_package}
+""")
 
-    if self_update and platform.system() == "Windows":
-        io.tool_output("Run this command to update:")
-        print()
-        print(printable_shell_command(cmd))  # plain print so it doesn't line-wrap
-        return
+async def check_archlinux_package(io, module, archlinux_package):
+    if module:
+        try:
+            __import__(module)
+            return True
+        except (ImportError, ModuleNotFoundError, RuntimeError):
+            pass
+
+    cmd = ["sudo", "pacman", "-Syu", "--asdeps", "--noconfirm", archlinux_package]
+    io.tool_warning("Using this feature requires a package to be installed.")
 
-    if not await io.confirm_ask(
-        "Run pip install?", default="y", subject=printable_shell_command(cmd)
-    ):
+    if not io.confirm_ask("Run pacman?", default="y", subject=printable_shell_command(cmd)):
         return
 
     success, output = run_install(cmd)
diff --git a/cecli/versioncheck.py b/cecli/versioncheck.py
index 05d13fe..372b33b 100644
--- a/cecli/versioncheck.py
+++ b/cecli/versioncheck.py
@@ -17,12 +17,10 @@ async def install_from_main_branch(io):
     """
     Install the latest version of cecli from the main branch of the GitHub repository.
     """
-    return await utils.check_pip_install_extra(
+    return await utils.check_aur_package(
         io,
         None,
-        "Install the development version of cecli from the main branch?",
-        ["git+https://github.com/dwash96/cecli.git"],
-        self_update=True,
+        "cecli-git",
     )
 
 
@@ -34,18 +32,12 @@ async def install_upgrade(io, latest_version=None):
         new_ver_text = f"Newer cecli version v{latest_version} is available."
     else:
         new_ver_text = "Install latest version of cecli?"
-    docker_image = os.environ.get("CECLI_DOCKER_IMAGE")
-    if docker_image:
-        text = f"\n{new_ver_text} To upgrade, run:\n\n    docker pull {docker_image}\n"
-        io.tool_warning(text)
-        return True
-    success = await utils.check_pip_install_extra(
-        io, None, new_ver_text, ["cecli-dev"], self_update=True
-    )
-    if success:
-        io.tool_output("Re-run cecli to use new version.")
-        sys.exit()
-    return
+
+    text = f"""
+{new_ver_text} To upgrade, use the latest version available on the AUR.
+"""
+    io.tool_warning(text)
+    return True
 
 
 async def check_version(io, just_check=False, verbose=False):
