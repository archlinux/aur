--- a/ULWGL/ulwgl_util.py	2024-03-01 01:14:09.012524430 +0100
+++ b/ULWGL/ulwgl_util.py	2024-03-01 01:37:44.509507483 +0100
@@ -2,6 +2,7 @@
 import tarfile
 import subprocess
 import re
+import stat
 
 from errno import EXDEV, ENOSYS, EINVAL
 from ulwgl_consts import CONFIG
@@ -191,7 +192,7 @@
     """
     cp: Callable[Path, Path] = None
 
-    if hasattr(os, "copy_file_range"):
+    if False:
         log.debug("CoW filesystem detected")
         cp = copyfile_reflink
     else:
@@ -208,6 +209,7 @@
     # Reaper
     print(f"Copying reaper -> {local}", file=stderr)
     cp(root.joinpath("reaper"), local.joinpath("reaper"))
+    os.chmod(local.joinpath("reaper"), stat.S_IREAD | stat.S_IWUSR | stat.S_IEXEC)
 
     # Runtime platform
     setup_runtime(root, json)
@@ -219,6 +221,7 @@
             cp(file, local.joinpath(file.name))
 
     local.joinpath("ulwgl-run").symlink_to("ulwgl_run.py")
+    os.chmod(local.joinpath("ulwgl_run.py"), stat.S_IREAD | stat.S_IWUSR | stat.S_IEXEC)
 
     # Runner
     steam_compat.mkdir(parents=True, exist_ok=True)
@@ -255,7 +258,7 @@
     """
     cp: Callable[Path, Path] = None
 
-    if hasattr(os, "copy_file_range"):
+    if False:
         log.debug("CoW filesystem detected")
         cp = copyfile_reflink
     else:
@@ -279,6 +282,7 @@
                 )
 
                 cp(root.joinpath("reaper"), local.joinpath("reaper"))
+                os.chmod(local.joinpath("reaper"), stat.S_IREAD | stat.S_IWUSR | stat.S_IEXEC)
 
             # Update
             if val != reaper:
@@ -286,6 +290,7 @@
 
                 local.joinpath("reaper").unlink(missing_ok=True)
                 cp(root.joinpath("reaper"), local.joinpath("reaper"))
+                os.chmod(local.joinpath("reaper"), stat.S_IREAD | stat.S_IWUSR | stat.S_IEXEC)
 
                 json_local["ulwgl"]["versions"]["reaper"] = val
         elif key == "pressure_vessel":
@@ -350,6 +355,7 @@
                 # Symlink
                 local.joinpath("ulwgl-run").unlink(missing_ok=True)
                 local.joinpath("ulwgl-run").symlink_to("ulwgl_run.py")
+                os.chmod(local.joinpath("ulwgl_run.py"), stat.S_IREAD | stat.S_IWUSR | stat.S_IEXEC)
 
                 json_local["ulwgl"]["versions"]["launcher"] = val
         elif key == "runner":

