<IfModule alias_module>
    Alias /aria-ng /usr/share/aria-ng
    <Directory "/usr/share/aria-ng">
        AllowOverride None
        Options None
        Order allow,deny
        #Allow from all
        Allow from 127.0.0.1 localhost

        #AuthType Basic
        #AuthName "Restricted Files"
        #AuthUserFile htpasswd
        #Require user foo
    </Directory>
</IfModule>
