# -*- sh -*-

post_install() {
  cd /usr/share/jed/lib || exit 1

  /usr/bin/jed -batch -n -l preparse.sl
}

post_upgrade() {
  post_install "$@"
}

post_remove() {
  cd /usr/share/jed/lib || exit 1

  rm -vf *.slc *.dfa
}

# eof
