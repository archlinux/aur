post_install() {
  for U in $(w -h | awk '{print $1}' | sort -u); do
    systemctl -M "$U@" --user daemon-reload
  done
  udevadm control --reload
}

post_upgrade() {
  post_install "$1"
}

post_remove() {
  post_install "$1"
}
