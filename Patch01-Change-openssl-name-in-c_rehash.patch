--- aws-lc-1.54.0/tool-openssl/c_rehash.sh	2025-06-29 17:49:57.000000000 +0200
+++ aws-lc-1.54.0/tool-openssl/c_rehash.sh.new	2025-06-29 17:59:45.011864817 +0200
@@ -1,6 +1,9 @@
 #!/usr/bin/env bash
 # Copyright Amazon.com, Inc. or its affiliates. All Rights Reserved.
 # SPDX-License-Identifier: Apache-2.0 OR ISC
+#
+# Adapted by Edmund Lodewijks for Arch Linux, where the binary
+# has a different name.
 
 set -exo pipefail
 
@@ -8,9 +11,9 @@
 SCRIPT_DIR=$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)
 
 # Check for openssl binary in same directory
-if [ -x "${SCRIPT_DIR}/openssl" ]; then
-    "${SCRIPT_DIR}/openssl" rehash "$@"
+if [ -x "${SCRIPT_DIR}/aws-lc-openssl" ]; then
+    "${SCRIPT_DIR}/aws-lc-openssl" rehash "$@"
 else
-    echo "Error: AWS-LC openssl binary not found at '${SCRIPT_DIR}/openssl'"
+    echo "Error: AWS-LC openssl binary not found at '${SCRIPT_DIR}/aws-lc-openssl'"
     exit 1
 fi
