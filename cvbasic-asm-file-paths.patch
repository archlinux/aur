diff --git a/cvbasic.c b/cvbasic.c
index e7a4dda..3c18a92 100644
--- a/cvbasic.c
+++ b/cvbasic.c
@@ -5112,12 +5112,15 @@ int main(int argc, char *argv[])
             fprintf(output, "\tforg $00000\n");
         }
     }
+
+	const char *prologue_path;
     if (target == CPU_6502)
-        prologue = fopen("cvbasic_6502_prologue.asm", "r");
+		prologue_path = "/usr/share/cvbasic/cvbasic_6502_prologue.asm";
     else
-        prologue = fopen("cvbasic_prologue.asm", "r");
+        prologue_path = "/usr/share/cvbasic/cvbasic_prologue.asm";
+	prologue = fopen(prologue_path, "r");
     if (prologue == NULL) {
-        fprintf(stderr, "Unable to open cvbasic_prologue.asm.\n");
+        fprintf(stderr, "Unable to open %s.\n", prologue_path);
         exit(2);
     }
     while (fgets(line, sizeof(line) - 1, prologue)) {
@@ -5152,11 +5155,12 @@ int main(int argc, char *argv[])
     remove(TEMPORARY_ASSEMBLER);
     
     if (target == CPU_6502)
-        prologue = fopen("cvbasic_6502_epilogue.asm", "r");
+        prologue_path = "/usr/share/cvbasic/cvbasic_6502_epilogue.asm";
     else
-        prologue = fopen("cvbasic_epilogue.asm", "r");
+        prologue_path = "/usr/share/cvbasic/cvbasic_epilogue.asm";
+	prologue = fopen(prologue_path, "r");
     if (prologue == NULL) {
-        fprintf(stderr, "Unable to open cvbasic_epilogue.asm.\n");
+        fprintf(stderr, "Unable to open %s.\n", prologue_path);
         exit(2);
     }
     while (fgets(line, sizeof(line) - 1, prologue)) {
