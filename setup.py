from distutils.core import setup
import sys, os

setup(name='sierrachartfeed',
      version='1.0',
      description='Sierrachart feed bridge for bitcoincharts.com',
      author='slush',
      url='https://github.com/slush0/sierrachartfeed',
      py_modules=['sierrachartfeed'],
      packages=['scid']
     )
