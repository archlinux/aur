post_install() {
	echo "Defaulting to Wayland in the current discord-canary install"
	rm /usr/bin/discord-canary
	echo -e "#! /bin/sh\n\nexec /opt/discord-canary/DiscordCanary --ozone-platform-hint=auto \"\$@\"" > /usr/bin/discord-canary
	chmod +x /usr/bin/discord-canary
}
