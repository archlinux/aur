diff --git a/build.sbt b/build.sbt
index 4f7002bb..7b701a2f 100644
--- a/build.sbt
+++ b/build.sbt
@@ -14,0 +14,0 @@ val MUnitFramework = new TestFramework("munit.Framework")

 inThisBuild(
   List(
-    version ~= { dynVer =>
-      if (isCI) dynVer
-      else localSnapshotVersion // only for local publishing
-    },
+    version := "0.9.0",
     scalaVersion := V.scala212,
     crossScalaVersions := List(V.scala212),
     scalacOptions ++= List(
