# /etc/gw2api/gw2api.conf
# Environment file read by gw2api.service (systemd EnvironmentFile format).

# Port the API listens on
GW2API_PORT=8083

# Admin token for the built-in dashboard. CHANGE THIS before exposing the service.
GW2API_ADMIN_TOKEN=change-me
