post_install(){
    chmod -R o+w /opt/svstudio 
} 

pre_upgrade(){
    chmod -R o-w /opt/svstudio
}
post_upgrade() {
    post_install
}

pre_remove() {
    pre_upgrade
}

post_remove() {
    if [ -d /opt/svstudio ]; then
        rm -rf /opt/svstudio/
    fi
}
