#!/bin/sh

PREFIX=/usr
MAIN=

#findPrefix
if [ -x /bin/rpm ]
then
  MAIN=`rpm -ql metamorphose2|grep metamorphose2.py`
  if [ -f "$MAIN" ];then
    exec python3 $MAIN "$@"
    exit
  fi
fi
#findPrefixEnd

#run
  exec python3 /usr/share/metamorphose2/metamorphose2.py "$@"
