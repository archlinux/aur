post_install(){
    echo " >> Enabling paccache systemd timer"
    echo " >> Run 'systemctl disable paccache.timer' to disable it"
    systemctl enable paccache-leave3.timer
}

pre_remove() {
    echo " >> Disabling paccache systemd timer"
    systemctl disable paccache-leave3.timer
}
