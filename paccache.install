post_install(){
    echo " >> Enabling pacman files systemd timer"
    echo " >> Run 'systemctl disable paccache.timer' to disable it"
    systemctl enable paccache.timer
}

pre_remove() {
    echo " >> Disabling pacman files systemd timer"
    systemctl disable paccache.timer
}
