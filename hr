#!/usr/bin/env bash
HELMRELEASE="$(dirname "$0")/helmrelease"

if [[ -o xtrace ]]; then
  export XTRACE=true
fi

if [[ "$1" == "--help" || "$1" == "-h" ]]; then
  "$HELMRELEASE" --help template
  exit 0
fi
exec "$HELMRELEASE" "template" "$@"
