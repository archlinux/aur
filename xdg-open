#!/bin/sh

echo "Running KTS3wrap..."

# Creates a new clean environment, but passing the dbus bus address
env -i DBUS_SESSION_BUS_ADDRESS=$DBUS_SESSION_BUS_ADDRESS \
    qdbus org.kde.klauncher5 /KLauncher org.kde.KLauncher.kdeinit_exec "xdg-open" "$@" "" ""
