[Unit]
Description=Prometheus service
After=network.target

[Service]
User=prometheus
Group=prometheus
ExecStart=/usr/bin/prometheus -storage.local.path=/var/lib/prometheus

[Install]
WantedBy=multi-user.target
