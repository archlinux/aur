post_install() {
    cat <<EOF
==> Add this code to your Emacs init to use meson-mode:

(autoload 'meson-mode "meson-mode.el"
   "Major mode for editing Meson files" t)
(add-to-list 'auto-mode-alist '("meson.build\'" . meson-mode))

EOF
}


post_upgrade() {
	post_install $1
}
