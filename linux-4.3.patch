--- A/NVIDIA-Linux-x86_64-355.11-no-compat32/kernel/nvidia/nv-procfs.c	2015-12-16 09:31:48.706486828 +0100
+++ B/NVIDIA-Linux-x86_64-355.11-no-compat32/kernel/nvidia/nv-procfs.c	2015-12-16 09:33:16.803511604 +0100
@@ -359,8 +359,9 @@
         nvl = NV_GET_NVL_FROM_NV_STATE(nv);
     registry_keys = ((nvl != NULL) ?
             nvl->registry_keys : nv_registry_keys);
-
-    return seq_printf(s, "Binary: \"%s\"\n", registry_keys);
+ 
+    seq_printf(s, "Binary: \"%s\"\n", registry_keys);
+    return 0;
 }
 
 static ssize_t
@@ -560,7 +561,8 @@
     void *v
 )
 {
-    return seq_puts(s, s->private);
+    seq_puts(s, s->private);
+    return 0;
 }
 
 NV_DEFINE_PROCFS_SINGLE_FILE(text_file);
