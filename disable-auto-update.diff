diff --git a/app/ui/window.ts b/app/ui/window.ts
index 2f00146..253f54a 100644
--- a/app/ui/window.ts
+++ b/app/ui/window.ts
@@ -110,12 +110,6 @@ export function newWindow(
     (app.windowCallback || fn)(window);
     app.windowCallback = undefined;
     fetchNotifications(window);
-    // auto updates
-    if (!isDev) {
-      updater(window);
-    } else {
-      console.log('ignoring auto updates during dev');
-    }
   });
 
   function createSession(extraOptions: any = {}) {
