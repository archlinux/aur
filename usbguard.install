post_upgrade() {
	echo "WARNING: USBGuard 0.7.0 introduces a backwards incompatible change to the device hash computation for root hub devices."
	echo "For certain configurations this may require an update of the hashes in rules.conf before restarting usbguard.service."
}
