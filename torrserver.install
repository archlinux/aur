post_install() {
	mkdir /var/lib/torrserver
	# Load service file
	systemctl --quiet daemon-reload
}

post_upgrade() {
	mkdir /var/lib/torrserver
	# Reload service file
	systemctl --quiet daemon-reload
}

pre_remove() {
	# Stop service
	systemctl --quiet --no-reload disable --now torrserver.service
}

post_remove() {
	# Unload service
	systemctl --quiet daemon-reload
}

