post_install() {
	pacman --query docker &> /dev/null && runsc install
}

pre_upgrade() {
	pacman --query docker &> /dev/null && runsc uninstall
}

post_upgrade() {
	pacman --query docker &> /dev/null && runsc install
}

pre_remove() {
	pacman --query docker &> /dev/null && runsc uninstall
}
