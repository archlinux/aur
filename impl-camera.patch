diff --git i/video-opencv/impl-camera.cpp w/video-opencv/impl-camera.cpp
index 7abc7660..4b3f24b8 100644
--- i/video-opencv/impl-camera.cpp
+++ w/video-opencv/impl-camera.cpp
@@ -37,6 +37,9 @@ bool cam::start(info& args)
     cv::utils::logging::setLogLevel(cv::utils::logging::LogLevel::LOG_LEVEL_WARNING);
     cap.emplace(idx, video_capture_backend);
 
+    if (args.use_mjpeg)
+        cap->set(cv::CAP_PROP_FOURCC, cv::VideoWriter::fourcc('M', 'J', 'P', 'G'));
+
     if (args.width > 0 && args.height > 0)
     {
         cap->set(cv::CAP_PROP_FRAME_WIDTH,  args.width);
@@ -45,9 +48,6 @@ bool cam::start(info& args)
     if (args.fps > 0)
         cap->set(cv::CAP_PROP_FPS, args.fps);
 
-    if (args.use_mjpeg)
-        cap->set(cv::CAP_PROP_FOURCC, cv::VideoWriter::fourcc('M', 'J', 'P', 'G'));
-
     if (!cap->isOpened())
         goto fail;
 
