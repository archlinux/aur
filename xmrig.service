[Unit]
Description=XMRig Daemon for %i
After=network.target

[Service]
Type=forking
ExecStart=/usr/bin/xmrig -B -c %i.conf
WorkingDirectory=/etc/xmrig
Restart=always

[Install]
WantedBy=multi-user.target
