diff --git a/MODULE.bazel b/MODULE.bazel
index 7846ae953b..8de9e27788 100644
--- a/MODULE.bazel
+++ b/MODULE.bazel
@@ -153,6 +153,13 @@ crate.annotation(
     build_script_env = {"CFLAGS": "-UNDEBUG"},
 )
 
+# Prevent the tree-sitter-haskell strict-aliasing crash:
+# https://github.com/tree-sitter/tree-sitter-haskell/issues/144
+crate.annotation(
+    crate = "tree-sitter-haskell",
+    build_script_env = {"CFLAGS": "-fno-strict-aliasing"},
+)
+
 # `generate-bindings` is enabled for darwin so miniaudio's Rust bindings match
 # its CoreAudio C layout. Build scripts execute for the host, however, so a
 # macOS-hosted Linux cross-build also sees that feature. Use Cargo's TARGET to
