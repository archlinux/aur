#!/bin/sh

NAME=nordvpn
DAEMON=/usr/sbin/${NAME}d
SOCKET_DIR=/run/$NAME
NORDVPN_GROUP="nordvpn"

if [[ ! -d "$SOCKET_DIR" ]]; then
    mkdir -m 0750 "$SOCKET_DIR"
    chown root:"$NORDVPN_GROUP" "$SOCKET_DIR"
fi

exec su -g "$NORDVPN_GROUP" -c "$DAEMON" 2>&1
