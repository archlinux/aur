post_install() {
    udevadm control --reload-rules 2>/dev/null
    udevadm trigger 2>/dev/null
    echo ">> Settings are writable for the 'wheel' group. Add yourself if needed: sudo usermod -aG wheel \$USER"
}
post_upgrade() { post_install; }
