post_install() {
    id "prelockd" &>/dev/null || useradd -r -M -s /bin/false prelockd
}

post_upgrade() {
    id "prelockd" &>/dev/null || useradd -r -M -s /bin/false prelockd
}

post_remove() {
    userdel prelockd
}
