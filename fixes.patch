--- src/plink-ng/2.0/build_dynamic/Makefile.orig	2026-04-26 16:19:06.289776199 +0530
+++ src/plink-ng/2.0/build_dynamic/Makefile	2026-04-26 16:30:34.403875354 +0530
@@ -18,26 +18,26 @@
 #     FORCE_32BIT (warning: you may need to add a zconf.h symlink to make that
 #     work)
 #   Debug symbols: change DEBUG to -g (still incompatible with -flto?)
-NO_AVX2 = 1
-NO_SSE42 = 1
-CPU_CHECK = 1
-NO_LAPACK =
-ZSTD_O2 = 1
-STATIC_ZLIB =
-STATIC_ZSTD = 1
-STATIC_GMP =
-GMPROOT = /opt/homebrew
-DYNAMIC_MKL =
-MKLROOT = /opt/intel/oneapi/mkl/2025.1
-MKL_IOMP5_DIR = /opt/intel/oneapi/compiler/2025.1/lib
-STATIC_AOCL =
-AOCLROOT = /home/admin/aocl/5.1.0/gcc
-FORCE_32BIT =
+NO_AVX2 ?=
+NO_SSE42 ?=
+CPU_CHECK ?= 1
+NO_LAPACK ?=
+ZSTD_O2 ?= 1
+STATIC_ZLIB ?=
+STATIC_ZSTD ?= 1
+STATIC_GMP ?=
+GMPROOT ?= /opt/homebrew
+DYNAMIC_MKL ?=
+MKLROOT ?= /opt/intel/oneapi/mkl/latest
+MKL_IOMP5_DIR ?= /opt/intel/oneapi/compiler/latest/lib
+STATIC_AOCL ?=
+AOCLROOT ?=
+FORCE_32BIT ?=
 # Giving up on -flto default for now (Mar 2026) due to the high frequency of
 # gcc 10.2 crashes.  Will try again after next upgrade of my own build
 # machines.
-# DEBUG = -flto
-DEBUG =
+# DEBUG ?= -flto
+DEBUG ?=
 CC ?= gcc
 CXX ?= g++
 
@@ -59,7 +59,7 @@
   ZSTD_O2 = 1
   BASEFLAGS += -msse -mfpmath=sse
   ARCH32 = -m32 -march=i686
-  CXXFLAGS = -std=c++17
+  CXXFLAGS += -std=c++17
 else
   ifdef NO_AVX2
     ifndef NO_SSE42
@@ -79,7 +79,7 @@
   # plink2_base tries to use C++ <execution> header when compiled with C++20 or
   # newer; this typically requires something like -ltbb to be added to
   # LINKFLAGS.
-  CXXFLAGS = -std=c++17
+  CXXFLAGS += -std=c++17
 endif
 BASEFLAGS += ${ARCH32}
 
@@ -199,6 +199,13 @@
 
 SFX ?= ""
 
+# Installation defaults
+MKDIR     ?= mkdir
+INSTALL   ?= install
+STRIP_CMD ?= strip
+PREFIX    ?= /usr/local
+DESTDIR   ?= .
+
 all: plink2$(SFX) pgen_compress$(SFX)
 
 plink2$(SFX): $(CSRC2) $(GCSRC2) $(ZCSRC2) $(ZSSRC2) $(CCSRC2) ../plink2_cpu.cc
@@ -229,12 +236,16 @@
 static_plink2lib_test: plink2lib.a ../pgen_compress.cc
 	$(CXX) $(CXXFLAGS) -o $@ ../pgen_compress.cc -L. plink2lib.a
 
-.PHONY: clean
+install-strip: install
+	$(STRIP_CMD) $(DESTDIR)$(PREFIX)/bin/*
+
+install:
+	$(MKDIR) -p $(DESTDIR)$(PREFIX)/bin
+	$(INSTALL) plink2 pgen_compress $(DESTDIR)$(PREFIX)/bin
+
 clean:
 	rm -f *.o
-	rm -f plink2
-	rm -f pgen_compress
-	rm -f pgenlib.a
-	rm -f plink2lib.a
-	rm -f static_pgenlib_test
-	rm -f static_plink2lib_test
+	rm -f plink2*
+	rm -f pgen_compress*
+	rm -f *.a
+	rm -f static_*_test
