post_install() {
	echo 'Before running kak-lsp, copy or link the kak-lsp.toml configuration file in /etc/ to $HOME/.config/kak-lsp/'
	echo 'If desired, enable the kak-lsp systemd user service for client auto-start/restart:'
	echo '    systemctl --user enable --now kak-lsp'
}
