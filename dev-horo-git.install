# old version (without -$pkgrel): ${1%%-*}
# new version (without -$pkgrel): ${2%%-*}

post_install() {
    depmod -a
    modprobe horok
}

pre_upgrade() {
    modprobe -r horok
}

post_upgrade() {
    post_install
}

pre_remove() {
    pre_remove
}
