diff --git a/opt/chromium.org/chromium-unstable/chromium-browser-unstable b/opt/chromium.org/chromium-unstable/chromium-browser-unstable
index eaceda2..bc848e6 100755
--- a/opt/chromium.org/chromium-unstable/chromium-browser-unstable
+++ b/opt/chromium.org/chromium-unstable/chromium-browser-unstable
@@ -39,6 +39,12 @@ export CHROME_VERSION_EXTRA="unstable"
 # We don't want bug-buddy intercepting our crashes. http://crbug.com/24120
 export GNOME_DISABLE_CRASH_DIALOG=SET_BY_GOOGLE_CHROME

+# Allow users to make command-line options permanent
+xdg_conf_dir="${XDG_CONFIG_HOME:-$HOME/.config}"
+
+[ -f "$xdg_conf_dir/chromium-unstable-flags.conf" ] && \
+  CHROME_USER_FLAGS="$(grep -v '^#' $xdg_conf_dir/chromium-unstable-flags.conf)"
+
 # Sanitize std{in,out,err} because they'll be shared with untrusted child
 # processes (http://crbug.com/376567).
 exec < /dev/null
@@ -46,4 +52,4 @@ exec > >(exec cat)
 exec 2> >(exec cat >&2)

 # Note: exec -a below is a bashism.
-exec -a "$0" "$HERE/chrome" "$@"
+exec -a "$0" "$HERE/chrome" $CHROME_USER_FLAGS "$@"
