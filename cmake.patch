diff --git a/AprilTagTrackers/CMakeLists.txt b/AprilTagTrackers/CMakeLists.txt
index 1252d1a..944f01d 100644
--- a/AprilTagTrackers/CMakeLists.txt
+++ b/AprilTagTrackers/CMakeLists.txt
@@ -2,15 +2,22 @@
 # project specific logic here.
 #
 cmake_minimum_required (VERSION 3.8)
+project ("AprilTagTrackers")
 
 if (UNIX)
     add_definitions (-D__LINUX__)
 endif (UNIX)
 
+find_library(OPENVR_LIB openvr_api)
+find_library(APRILTAG_LIB apriltag)
+
 find_package( OpenCV REQUIRED )
-include_directories( ${OpenCV_INCLUDE_DIRS} )
-include_directories( ${APRILTAG_INCLUDE_DIR} )
-include_directories( ${OPENVR_INCLUDE_DIR} )
+# include_directories( ${OpenCV_INCLUDE_DIRS} )
+include_directories( "/usr/include/apriltag/" )
+include_directories( "/usr/include/openvr/" )
+
+find_package(wxWidgets COMPONENTS core base net REQUIRED)
+include(${wxWidgets_USE_FILE})
 
 # Add source to this project's executable.
 file(GLOB source "*.cpp" "*.h" "*.hpp")
@@ -20,5 +27,5 @@ add_executable (AprilTagTrackers ${source})
 
 target_link_libraries( AprilTagTrackers ${OPENVR_LIB} )
 target_link_libraries( AprilTagTrackers ${OpenCV_LIBS} )
-target_link_libraries( AprilTagTrackers apriltags)
-target_link_libraries( AprilTagTrackers wx::base wx::core wx::net)
+target_link_libraries( AprilTagTrackers ${APRILTAG_LIB})
+target_link_libraries( AprilTagTrackers ${wxWidgets_LIBRARIES})
