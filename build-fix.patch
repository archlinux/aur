diff --git a/mrbuild/Makefile.common.footer b/mrbuild/Makefile.common.footer
index e1ec16e..54405a4 100644
--- a/mrbuild/Makefile.common.footer
+++ b/mrbuild/Makefile.common.footer
@@ -257,10 +257,14 @@ $(if $(PY3_MODULE_PATH),,$(error "Couldn't find the python3 module path!"))
 endif
 
 USE_DEBIAN_PATHS := $(wildcard /etc/debian_version)
+USE_ARCH_PATHS := $(wildcard /etc/arch-release)
 ifneq (,$(USE_DEBIAN_PATHS))
   # we're a debian-ish box, use the multiarch dir
   DEB_HOST_MULTIARCH := $(shell dpkg-architecture -qDEB_HOST_MULTIARCH 2>/dev/null)
   USRLIB             := usr/lib/$(DEB_HOST_MULTIARCH)
+else ifneq(,$(USE_ARCH_PATHS))
+  # we're on arch linux
+  USRLIB := /usr/lib
 else
   # we're something else. Do what CentOS does.
   # If /usr/lib64 exists, use that. Otherwise /usr/lib
diff --git a/mrcal-calibrate-cameras b/mrcal-calibrate-cameras
index 33f93c5..d3af860 100755
--- a/mrcal-calibrate-cameras
+++ b/mrcal-calibrate-cameras
@@ -695,7 +695,7 @@ for icam in range(len(models)):
     filename_base = \
         f'{args.outdir}/camera{icam//2}-{icam%2}' \
         if args.pairs \
-        else f'{args.outdir}/camera-{icam}
+        else f'{args.outdir}/camera-{icam}'
 
     cameramodelfile = filename_base + '.cameramodel'
     models[icam].write(cameramodelfile,
diff --git a/mrcal-pywrap.c b/mrcal-pywrap.c
index f68cb77..b20ffe2 100644
--- a/mrcal-pywrap.c
+++ b/mrcal-pywrap.c
@@ -8,7 +8,7 @@
 #include <dogleg.h>
 
 #if (CHOLMOD_VERSION > (CHOLMOD_VER_CODE(2,2)))
-#include <suitesparse/cholmod_function.h>
+#include <cholmod_function.h>
 #endif
 
 #include "mrcal.h"
