post_upgrade() {
    echo 
    echo "Remove ~/.deepinwine/Spark-TIM to generate a new bottle."
    echo 
}

post_remove() {
    echo "Deleting existing TIM bottles..."
    find /home -maxdepth 2 -name ".deepinwine" -exec rm -rf \{\}/Spark-TIM/ \;
}
