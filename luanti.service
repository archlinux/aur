[Unit]
Description=Luanti server (%i)
After=network.target

[Service]
User=luanti
ExecStart=/usr/bin/luantiserver --config /etc/luanti/%i.conf --map-dir /var/lib/luanti/%i/

[Install]
WantedBy=multi-user.target
