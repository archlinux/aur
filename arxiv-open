#!/bin/sh

if [[ "$@" =~ ^ar[xX]iv:(//){0,1} ]]; then
    xdg-open $(echo $@ | sed 's#ar[xX]iv:\(//\)\{0,1\}#https://arxiv.org/abs/#')
else
    echo "Provided link does not appear to be a valid arXiv link." >&2
fi
