#!/usr/bin/fontforge
i=1
while ( i<$argc )
    Open($argv[i])

    # copy space glyph (32) to nonbreaking space (160)
    Select(32);
    Copy();
    Select(160);
    Paste();

    Generate($argv[i])
    i = i+1
endloop
