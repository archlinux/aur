--- a/Anaconda-2.0.0-Linux-x86.sh	2014-05-29 01:35:31.087202509 +0200
+++ b/Anaconda-2.0.0-Linux-x86.sh	2014-05-29 01:36:33.366571125 +0200
@@ -73,9 +73,9 @@
     fi
 fi
 # verify the size of the installer
-wc -c "$THIS_PATH" | grep 312856983 >/dev/null
+wc -c "$THIS_PATH" | grep 312856990 >/dev/null
 if (( $? )); then
-    echo "ERROR: size of $THIS_FILE should be 312856983 bytes" >&2
+    echo "ERROR: size of $THIS_FILE should be 312856990 bytes" >&2
     exit 1
 fi
 
@@ -381,7 +381,7 @@
 extract_dist _cache-0.0-x0
 
 mkdir $PREFIX/envs
-mkdir $HOME/.continuum 2>/dev/null
+#mkdir $HOME/.continuum 2>/dev/null
 
 PYTHON="$PREFIX/pkgs/python-2.7.6-2/bin/python -E"
 $PYTHON -V
@@ -391,7 +391,7 @@
     uname -a
     exit 1
 fi
-
+exit 0
 echo "creating default environment..."
 CONDA_INSTALL="$PREFIX/pkgs/conda-3.5.2-py27_0/lib/python2.7/site-packages/conda/install.py"
 $PYTHON $CONDA_INSTALL --prefix=$PREFIX --pkgs-dir=$PREFIX/pkgs --link-all || exit 1
