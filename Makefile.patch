diff --git a/makefile.gcc b/Makefile
similarity index 84%
rename from makefile.gcc
rename to Makefile
index 58aff43..d9b162b 100644
--- a/makefile.gcc
+++ b/Makefile
@@ -38,17 +38,16 @@ MV = mv
 RM = rm -f
 
 # flags
-CFLAGS = $(DEFINES) $(TFLAG) $(CDEBUG) -I$(INCLUDE_PATH) -c -Wall -Wextra
+CFLAGS += $(DEFINES) $(TFLAG) -I$(INCLUDE_PATH) -c -Wall -Wextra
 INSTALLBINFLAGS = -m 755
 INSTALLDATAFLAGS = -m 644
-LDFLAGS = $(LDEBUG)
 LNFLAGS = -sf
 RMRECURFLAGS = -rf
 
 # directories
 BINDIR = /usr/bin
 LIBDIR = lib
-MANDIR = /usr/man/man1
+MANDIR = /usr/share/man/man1
 
 # filenames
 FROMDOS = fromdos
@@ -81,12 +80,12 @@ clobber: clean
 	$(RM) $(FROMDOS) $(TODOS)
 
 install: installman
-	$(INSTALL) $(INSTALLBINFLAGS) $(FROMDOS) $(BINDIR)
-	($(CD) $(BINDIR) ; $(LN) $(LNFLAGS) fromdos todos)
+	install -Dm755 fromdos -t $(DESTDIR)$(BINDIR)
+	ln -sf fromdos $(DESTDIR)$(BINDIR)/todos
 
 installman: $(FROMDOSMAN)
-	$(INSTALL) $(INSTALLDATAFLAGS) $(FROMDOSMAN) $(MANDIR)
-	($(CD) $(MANDIR) ; $(LN) $(LNFLAGS) fromdos.1 todos.1)
+	install -Dm644 fromdos.1 -t $(DESTDIR)$(MANDIR)
+	ln -sf fromdos.1 $(DESTDIR)$(MANDIR)/todos.1
 
 
 # real rules
