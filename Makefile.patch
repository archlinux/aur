--- Makefile.old	2026-02-22 19:34:23.000000000 -0600
+++ Makefile	2026-02-23 14:09:40.360257527 -0600
@@ -18,8 +18,7 @@
 
 LIBDIRS := /usr/local/lib
 
-LIBS := -lSDL3
-STATIC_LIBS := -lfdk-aac
+LIBS := -lSDL3 -lfdk-aac
 
 ifeq ($(shell uname),Darwin)
 	CPPFLAGS += -isystem $(shell brew --prefix)/include
@@ -56,7 +55,7 @@
 	LDFLAGS += -mwindows -static -Wl,--stack,8388608 -fuse-ld=lld
 endif
 
-SRCS := $(shell find $(SRC_DIR) -name '*.c' -or -name '*.cpp') 
+SRCS := $(shell find $(SRC_DIR) -name '*.c' -or -name '*.cpp')
 SRCS := $(SRCS:$(SRC_DIR)/%=%)
 
 BUILD_ROOT := $(BUILD_DIR)
@@ -72,7 +71,7 @@
 OBJS := $(SRCS:%=$(BUILD_DIR)/%.o)
 DEPS := $(OBJS:.o=.d)
 
-$(BUILD_ROOT)/$(TARGET_EXEC): $(OBJS) $(STATIC_LIBS)
+$(BUILD_ROOT)/$(TARGET_EXEC): $(OBJS)
 	@echo linking $@...
 	@$(CXX) -o $@ $(CFLAGS) $(CPPFLAGS) $^ $(LDFLAGS)
 	@echo done
