--- a/Makefile	2024-03-30 20:05:40.482464599 +0100
+++ b/Makefile	2024-03-30 20:05:34.902464429 +0100
@@ -1058,32 +1058,6 @@
 	fi
 	$(INSTALL) $(INSTALLFLAGS) $(INSTPGMFLAGS)  xsysinfo $(DESTDIR)$(BINDIR)/xsysinfo
 
-all:: xsysinfo.$(MANNEWSUFFIX)
-
-xsysinfo.$(MANNEWSUFFIX): xsysinfo.$(MANSRCSUFFIX)
-	$(RM) $@
-	if test -z "$(PREPROCESSMANPAGES)" ; then \
-	   cd `dirname xsysinfo` && \
-	   $(LN) `basename xsysinfo.$(MANSRCSUFFIX)` `basename $@`; \
-	else \
-	      	     $(RAWCPP)  $(MANDEFS) $(EXTRAMANDEFS) \
-	     < xsysinfo.$(MANSRCSUFFIX) | sed -e '/^#  *[0-9][0-9]*  *.*$$/d' 			-e '/^#line  *[0-9][0-9]*  *.*$$/d' 			-e '/^[ 	]*XCOMM$$/s/XCOMM/#/' 			-e '/^[ 	]*XCOMM[^a-zA-Z0-9_]/s/XCOMM/#/' 			-e '/^[ 	]*XHASH/s/XHASH/#/' 			-e '/\@\@$$/s/\@\@$$/\\/' >$@; \
-	fi
-
-cleandir::
-	$(RM) xsysinfo.$(MANNEWSUFFIX)
-
-install.man:: xsysinfo.$(MANNEWSUFFIX)
-	@if [ -d $(DESTDIR)$(MANDIR) ]; then \
-		set +x; \
-	else \
-		if [ -h $(DESTDIR)$(MANDIR) ]; then \
-			(set -x; rm -f $(DESTDIR)$(MANDIR)); \
-		fi; \
-		(set -x; $(MKDIRHIER) $(DESTDIR)$(MANDIR)); \
-	fi
-	$(INSTALL) $(INSTALLFLAGS) $(INSTMANFLAGS) xsysinfo.$(MANNEWSUFFIX) $(DESTDIR)$(MANDIR)/xsysinfo.$(MANSUFFIX)
-
 depend::
 	$(DEPEND) $(DEPENDFLAGS) -- $(ALLDEFINES) $(DEPEND_DEFINES) -- $(SRCS)
 
