ARG REGISTRY

FROM "$REGISTRY/common/gitlab-helper/archlinux-yay:master"

RUN (\
    yay -Syuq --noconfirm --needed plplot hdf4 eccodes udunits \
    && yes | yay -Scc --noconfirm \
    && rm -rf ~/.cache/yay \
    && sudo rm -rf /var/cache/pacman/pkg/* \
)
