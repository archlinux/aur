#!/bin/sh

post_install() {
    echo
    echo "==> I added a desktop entry and its icon but I don't use a Desktop Environment"
    echo "==> So, since I can't test it, don't hesitate to leave a comment on AURWeb"
    echo
}

post_upgrade() {
    post_install $@
}
