post_install() {
    echo "Configure your username and password in /etc/campus-ics-feed.conf. You also"
    echo "have to set CAMPUS_CONFIG_FILE=/etc/campus-ics-feed.conf for the webapp to"
    echo "find its config file."
}
