diff --git a/tests/cli/textual_ui/test_chat_input_word_drag.py b/tests/cli/textual_ui/test_chat_input_word_drag.py
index e630d30a..0fb3d395 100644
--- a/tests/cli/textual_ui/test_chat_input_word_drag.py
+++ b/tests/cli/textual_ui/test_chat_input_word_drag.py
@@ -6,6 +6,7 @@ from textual.widgets.text_area import Selection
 
 from tests.conftest import build_test_vibe_app
 from vibe.cli.commands import CommandRegistry
+from vibe.cli.textual_ui.app import VibeApp
 from vibe.cli.textual_ui.widgets.chat_input import ChatTextArea
 
 
@@ -13,6 +14,15 @@ def _make_text_area(text: str) -> ChatTextArea:
     return ChatTextArea(command_registry=CommandRegistry(), text=text)
 
 
+def _build_app() -> VibeApp:
+    app = build_test_vibe_app()
+    # Real time between pilot events is ~0.5s here, which straddles the
+    # default click chain threshold on a loaded machine and breaks the chain.
+    # Chain expiry itself is covered by the test that rewinds the clock.
+    app.CLICK_CHAIN_TIME_THRESHOLD = 60.0
+    return app
+
+
 def _mouse_move(ta: ChatTextArea, x: int, y: int = 0) -> events.MouseMove:
     origin = ta.region.offset
     return events.MouseMove(
@@ -65,7 +75,7 @@ def test_snap_paragraph_drag_backward_spans_whole_paragraphs() -> None:
 
 @pytest.mark.asyncio
 async def test_double_click_selects_word_at_mouse_down() -> None:
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -86,7 +96,7 @@ async def test_double_click_selects_word_at_mouse_down() -> None:
 
 @pytest.mark.asyncio
 async def test_double_click_then_drag_forward_extends_by_word() -> None:
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -114,7 +124,7 @@ async def test_double_click_then_drag_forward_extends_by_word() -> None:
 
 @pytest.mark.asyncio
 async def test_double_click_then_drag_backward_extends_by_word() -> None:
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -142,7 +152,7 @@ async def test_double_click_then_drag_backward_extends_by_word() -> None:
 
 @pytest.mark.asyncio
 async def test_triple_click_then_drag_extends_by_line() -> None:
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -173,7 +183,7 @@ async def test_triple_click_then_drag_extends_by_line() -> None:
 
 @pytest.mark.asyncio
 async def test_single_click_drag_uses_char_granularity() -> None:
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -191,7 +201,7 @@ async def test_single_click_drag_uses_char_granularity() -> None:
 
 @pytest.mark.asyncio
 async def test_drag_end_resets_chain_so_next_click_is_char() -> None:
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -219,7 +229,7 @@ async def test_drag_end_resets_chain_so_next_click_is_char() -> None:
 
 @pytest.mark.asyncio
 async def test_click_chain_loops_after_triple_click() -> None:
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -259,7 +269,7 @@ async def test_click_chain_loops_after_triple_click() -> None:
 
 @pytest.mark.asyncio
 async def test_pause_after_double_click_resets_to_char() -> None:
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -277,7 +287,7 @@ async def test_pause_after_double_click_resets_to_char() -> None:
         await pilot.mouse_up(ta, offset=(2, 0))
         await pilot.pause(0.05)
 
-        ta._last_down_time = (ta._last_down_time or 0.0) - 1.0
+        ta._last_down_time = (ta._last_down_time or 0.0) - 120.0
 
         await pilot.mouse_down(ta, offset=(2, 0))
         await pilot.pause(0.05)
@@ -292,7 +302,7 @@ async def test_jitter_within_word_does_not_break_click_chain() -> None:
     _expand_drag_selection set _dragged=True unconditionally on any move,
     which made on_mouse_up reset _click_chain to 0.
     """
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -332,7 +342,7 @@ async def test_drag_back_to_anchor_shrinks_selection() -> None:
     target != anchor guard skipped _expand_drag_selection, leaving the
     extended selection stuck.
     """
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
@@ -365,7 +375,7 @@ async def test_right_click_does_not_advance_click_chain() -> None:
     the cursor in ChatTextArea. Without the guard, a right-click would
     promote the next left-click into a word/paragraph select.
     """
-    app = build_test_vibe_app()
+    app = _build_app()
     async with app.run_test(size=(80, 24)) as pilot:
         await pilot.pause(0.2)
         ta = app.query_one(ChatTextArea)
