--- a/cutter/cut-loader-customizer-factory-builder.c
+++ b/cutter/cut-loader-customizer-factory-builder.c
@@ -74,7 +74,7 @@
         object = klass->constructor(type, n_props, props);
         the_builder = CUT_LOADER_CUSTOMIZER_FACTORY_BUILDER(object);
     } else {
-        object = g_object_ref(the_builder);
+        object = G_OBJECT(g_object_ref(the_builder));
     }
 
     return object;

--- a/cutter/cut-report-factory-builder.c
+++ b/cutter/cut-report-factory-builder.c
@@ -90,7 +90,7 @@
         object = klass->constructor(type, n_props, props);
         the_builder = CUT_REPORT_FACTORY_BUILDER(object);
     } else {
-        object = g_object_ref(the_builder);
+        object = G_OBJECT(g_object_ref(the_builder));
     }
 
     return object;

--- a/cutter/cut-stream-factory-builder.c
+++ b/cutter/cut-stream-factory-builder.c
@@ -91,7 +91,7 @@
         object = klass->constructor(type, n_props, props);
         the_builder = CUT_STREAM_FACTORY_BUILDER(object);
     } else {
-        object = g_object_ref(the_builder);
+        object = G_OBJECT(g_object_ref(the_builder));
     }
 
     return object;

--- a/cutter/cut-ui-factory-builder.c
+++ b/cutter/cut-ui-factory-builder.c
@@ -77,7 +77,7 @@
         object = klass->constructor(type, n_props, props);
         the_builder = CUT_UI_FACTORY_BUILDER(object);
     } else {
-        object = g_object_ref(the_builder);
+        object = G_OBJECT(g_object_ref(the_builder));
     }
 
     return object;
