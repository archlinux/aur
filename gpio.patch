--- a/gpio.cpp
+++ b/gpio.cpp
@@ -339,6 +339,7 @@ void gpio_write(int fd, unsigned char value) {
 
 	if (1 != write(fd, &value_str[LOW==value?0:1], 1)) {
 		DEBUG_PRINT("failed to write value on pin ");
+		exit(1);
 	}
 }
 
