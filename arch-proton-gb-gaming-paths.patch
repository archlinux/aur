--- a/greenboost_proton/gb_proton_main.py
+++ b/greenboost_proton/gb_proton_main.py
@@ -525,6 +525,8 @@
     try:
         for _p in (os.path.expanduser("~/.local/lib/greenboost-gaming"),
                    "/usr/local/lib/greenboost-gaming",
+                   # Arch packaging: the Suite installs the backend here.
+                   "/usr/lib/greenboost-gaming",
                    os.path.join(os.path.dirname(os.path.abspath(__file__)), "..")):
             if _p not in sys.path:
                 sys.path.insert(0, _p)
@@ -621,6 +623,9 @@
         _script_dir = os.path.dirname(os.path.abspath(__file__))
         for _p in (
             "/usr/local/lib/greenboost",
+            # Arch packaging: the greenboost core package keeps its Python
+            # helpers under /usr/share/greenboost/lib.
+            "/usr/share/greenboost/lib",
             os.path.join(_script_dir, "..", "..", "greenboost"),
         ):
             if _p not in sys.path:
@@ -1041,6 +1046,8 @@
     _df_script_dir = os.path.dirname(os.path.abspath(__file__))
     _df_candidates = [
         "/usr/local/lib/greenboost",
+        # Arch packaging: see the gb_dataflux import above.
+        "/usr/share/greenboost/lib",
         os.path.join(_df_script_dir, "..", "..", "greenboost"),
     ]
     _df_saved_path = list(sys.path)
@@ -2306,6 +2313,8 @@
             # from main(), before any of them have touched sys.path.
             for _p in (os.path.expanduser("~/.local/lib/greenboost-gaming"),
                        "/usr/local/lib/greenboost-gaming",
+                       # Arch packaging: see the game-lifecycle bootstrap above.
+                       "/usr/lib/greenboost-gaming",
                        os.path.join(os.path.dirname(os.path.abspath(__file__)), "..")):
                 if _p not in sys.path:
                     sys.path.insert(0, _p)
@@ -2721,6 +2730,8 @@
     try:
         sys.path.insert(0, os.path.expanduser("~/.local/lib/greenboost-gaming"))
         sys.path.insert(0, "/usr/local/lib/greenboost-gaming")
+        # Arch packaging: the Suite's system-wide install location.
+        sys.path.insert(0, "/usr/lib/greenboost-gaming")
         from gb_gaming import global_settings as _gs
         for _gk, _gv in _gs.as_env_dict().items():
             os.environ.setdefault(_gk, _gv)
