--- src/nemo-window-manage-views.c.orig	2022-06-10 13:40:24.163601093 -0400
+++ src/nemo-window-manage-views.c	2022-06-10 13:51:46.293628485 -0400
@@ -69,6 +69,20 @@
  */
 #include "nemo-desktop-window.h"
 
+#include <unistd.h>
+
+int
+chdir_g_file(GFile *file_path)
+{
+    char * path = g_file_get_path(file_path);
+    int result;
+    if (path != NULL) {
+	result = chdir(path);
+        g_free(path);
+    }
+    return result;
+}
+
 /* This number controls a maximum character count for a URL that is
  * displayed as part of a dialog. It's fairly arbitrary -- big enough
  * to allow most "normal" URIs to display in full, but small enough to
@@ -113,6 +127,8 @@
         GFile *bookmark_location;
         gboolean recreate;
 
+	chdir_g_file(location);
+
         if (slot->current_location_bookmark == NULL || location == NULL) {
                 recreate = TRUE;
         } else {
