post_install() {
  if type -P systemd-tmpfiles &> /dev/null; then
    systemd-tmpfiles --create zabbix-java-gateway.conf
  fi
}

post_upgrade() {
  if type -P systemd-tmpfiles &> /dev/null; then
    systemd-tmpfiles --create zabbix-java-gateway.conf
  fi
}
