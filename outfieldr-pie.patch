# build with PIE
diff --git a/build.zig b/build.zig
index e06ba7c..ea10015 100644
--- a/build.zig
+++ b/build.zig
@@ -10,6 +10,7 @@ pub fn build(b: *Builder) !void {
     exe.setBuildMode(mode);
     exe.install();
     exe.setOutputDir("bin");
+    exe.pie = true;
 
     var code: u8 = undefined;
     const git_tag = try b.execAllowFail(
