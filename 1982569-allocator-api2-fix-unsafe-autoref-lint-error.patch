--- a/third_party/rust/allocator-api2/src/stable/vec/mod.rs	2025-08-29 13:35:32.252621404 -0400
+++ b/third_party/rust/allocator-api2/src/stable/vec/mod.rs	2025-08-29 13:36:34.899288150 -0400
@@ -1899,7 +1899,7 @@
     #[cfg(not(no_global_oom_handling))]
     #[inline(always)]
     unsafe fn append_elements(&mut self, other: *const [T]) {
-        let count = unsafe { (*other).len() };
+        let count = other.len();
         self.reserve(count);
         let len = self.len();
         unsafe { ptr::copy_nonoverlapping(other as *const T, self.as_mut_ptr().add(len), count) };
